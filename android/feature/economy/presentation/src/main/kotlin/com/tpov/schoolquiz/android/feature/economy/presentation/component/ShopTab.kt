package com.tpov.schoolquiz.android.feature.economy.presentation.component

/** The shop's screens. Labels are resolved in the UI, not carried here. */
enum class ShopTab {
    STORE,
    REFERRALS,
    SUPPORT,
    NICKNAMES,
}
