Готово.

Changed:

- [01-architecture.md](/Volumes/EXTERNAL/schoolquiz3.0/docs/features/settings-app-version-footer/01-architecture.md)

Fix summary:

- Renamed **“Real Gradle Dependency Graph”** to **“Feature-Relevant Gradle Dependency Graph”** so it is explicitly feature-scoped, not exhaustive.
- Added `:apps:android-next -> :android:core:designsystem` to the graph.
- Added a **Gradle dependency audit** table covering the requested Gradle files/line ranges and clarifying omitted existing deps are unchanged and not SCH-2-owned.
- Fixed L2 theming model: `MainActivity` applies `SchoolQuizTheme`; `DesignSettingsScreen` consumes `MaterialTheme` ambient and `SchoolQuizDesignBackground`; `SchoolQuizTheme` usage in settings is preview-only.

No production code touched. `02-behavior.md` did not need changes.

To continue this run, execute `"/opt/homebrew/bin/kent" run --continue "dfd362a4-f528-407a-9aa3-b426b9645b07" "follow-up"`.
