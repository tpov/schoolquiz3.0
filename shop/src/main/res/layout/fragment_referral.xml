<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/back_main"
    tools:context=".presentation.ReferralFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Content will be restored/added here based on plan -->
        <!-- TextView for TPOV ID -->
        <TextView
            android:id="@+id/tv_tpov_id_label"
            style="@style/ShopTextViewStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Your TPOV ID:" />

        <TextView
            android:id="@+id/tv_tpov_id"
            style="@style/ShopTextViewStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:textStyle="bold"
            tools:text="abcdef123456"/>

        <!-- Buttons, Info Text, RecyclerView, Reward Status will be added -->
        <!-- For now, a placeholder for where RecyclerView will go -->
         <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_referred_users"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            tools:listitem="@layout/item_referral_user"
            tools:itemCount="6"/>

        <LinearLayout
            android:id="@+id/ll_reward_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingTop="8dp"
            android:paddingBottom="8dp">

            <TextView
                android:id="@+id/tv_reward_text"
                style="@style/ShopTextViewStyle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Invite 6 players and receive x50"/>

            <ImageView
                android:id="@+id/iv_reward_icon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_box"/> <!-- ic_box will be restored -->
        </LinearLayout>

    </LinearLayout>
</ScrollView>
