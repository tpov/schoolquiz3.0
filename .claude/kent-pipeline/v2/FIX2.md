# Директива правок №2 (18 остаточных замечаний)

## E0. ПАРАЛЛЕЛИЗМ — единая формулировка во ВСЕ 9 файлов (закрывает #2, #10, #9)

Вставить в контракт спавна каждого файла дословно этот блок:

```
ПАРАЛЛЕЛЬНОСТЬ. `kent run --agent` и `kent run --session` — БЛОКИРУЮЩИЕ: возвращают финальный отчёт
работника. Чтобы поднять нескольких работников ОДНОВРЕМЕННО, запускай их фоновыми процессами shell
и жди все разом:
  kent run --agent test-dev   "<задание>" > run/agents/test-dev.out   2>&1 &
  kent run --agent backend-dev "<задание>" > run/agents/backend-dev.out 2>&1 &
  wait
  # затем прочитай .out-файлы — это финальные отчёты работников
Последовательный шаг (когда нужен результат до следующего действия) — обычный вызов без `&`.
`kent run wait <session-id>` НУЖЕН только для повторного опроса уже известной сессии; после
блокирующего вызова отчёт уже получен — второй раз ждать не нужно.
Session-id работника бери из вывода его рана (Kent печатает готовую команду `run steer`), записывай
в реестр роль→session-id в Run Ledger и используй для re-check через `kent run --session <id>`.
```

Убрать из всех файлов формулировки «дождись через kent run wait» сразу после блокирующего вызова
(plan.md:64, skeleton.md:31,36, research.md:151 и аналогичные).

## E1. implement.md — один владелец ревью на фазу (закрывает #1, #14, #10, #8)
- **Убрать `run/build-baton.md` полностью.**
- Владелец ревью-цикла фазы детерминирован: **backend-dev**; если фаза чисто frontend (нет backend.md) —
  **frontend-dev**. Он и только он: после своего PASS поднимает ревьюеров и крутит re-check.
  Второй coder (если он есть в фазе) по завершении своей части возвращает лиду отчёт со списком changed files;
  лид передаёт этот список владельцу ревью через `kent run --session <id владельца>` ДО поднятия ревьюеров.
  Владелец обязан дождаться этого списка (лид сообщает: «второй coder завершил, changed files: …»).
- Порядок спавна фазы (ШАГ 2.1): лид одним фоновым батчем (см. E0) поднимает test-dev и coder-ов;
  дождавшись `wait`, читает отчёты. Правило источника «test-dev работает ПАРАЛЛЕЛЬНО с devs» этим и выполняется.
- Если находка ревьюера касается ТЕСТОВОГО кода — владелец ревью поднимает **новый ран** `kent run --agent test-dev`
  с текстом находки (session-id прошлого рана test-dev не требуется). Убрать формулировки «если ты знаешь id test-dev».
- В таблице AGENT SCALING заменить остаточные «поднять 2 агента» на «сделать второй ран той же роли с другим scope».

## E2. design.md — скилл читать, флаги не применять (закрывает #11)
Заменить «CLI-флаги из скилла (в т.ч. references/cli-protocol.md) не применяй» на:
«прочитай `.claude/skills/adversarial-review/SKILL.md` и `references/cli-protocol.md` — оттуда бери протокол линз,
шкалу severity и правило „вывод всегда в файл“; НЕ применяй только сами CLI-флаги (`-o`, `-c file=path`) —
их роль выполняет задание субагенту роли crossmodel-reviewer».
То же самое проверить в plan.md (там ссылка на plan-review-lens.md должна остаться обязательной).

## E3. Единая политика при недоступности cross-model (закрывает #7)
В spec.md (spec_verdict) привести к виду plan.md/crossphase.md: недоступность роли crossmodel-reviewer =
СТОП + ask_question с вариантами (A) продолжить с явной пометкой known gap в 0-spec.md и README,
(B) остановиться до восстановления, (C) заменить усиленным same-model ревью с записью known gap.
Убрать «пропусти и сообщи в сводке».

## E4. Остаточные следы внешнего CLI и слэш-команд (закрывает #4, #5)
- spec.md: убрать упоминание `codex exec --full-auto -s read-only` (и «ошибка запуска/PATH») — заменить на
  «в источнике это делала другая модель через внешний CLI; здесь — субагент роли crossmodel-reviewer».
- crossphase.md: убрать слэши в провенансе: «источник — команда feature-implement, Шаг 2.5 / Шаг 3 / Шаги 3.5, 4, 4.5, 5»;
  строку «запускать ли команду feature-retrospective» заменить на «переходить ли к ноде retrospective».

## E5. debug-ветка: базовые правила и мелочи (закрывает #3, #6, #16)
- В КАЖДОЕ задание субагенту в debug.md (doc-analyst, log-reader, diagnostics, code-analyst, web-researcher,
  а также dev/test-dev/code-reviewer в dbg_apply) добавить строку:
  «Прочитай базовые правила проекта: CLAUDE.md, а если его нет — AGENTS.md в корне репо».
- dbg_apply: добавить абзац-обоснование по образцу cp_review: «Здесь ревьюера поднимаешь ТЫ (лид): это точечный
  фикс scope=small с двумя работниками и без владельца-фазы; per-phase механика ноды implement сюда не переносится».
- dbg_apply: шкалу severity у code-reviewer привести к **blocker / high / medium / low**.

## E6. Честные маркеры (закрывает #12, #13, #15)
- plan.md: перед блоком FIX-SPEC-DRIVEN PASS и у исхода `respec` —
  `[V2-ДОБАВЛЕНО: в источнике plan всегда шёл от 2-grounding.md; вход из debug по fix-spec — потребность графа,
  review-цикл при этом не сокращается]`.
- spec.md, Task Splitting вариант A — `[V2-ОТКЛОНЕНИЕ: в источнике spec сразу писал 0-spec.md на КАЖДУЮ часть;
  один ран графа ведёт ровно одну часть, остальные оформляются отдельными задачами Kent]`.
- design.md, требование «ADR обязан содержать Alternatives Considered» — `[V2-ДОБАВЛЕНО: в источнике альтернативы
  проверяла только линза Skeptic; вынесено в требование к документу]`.

## E7. Пути (закрывает #17)
plan.md: основной путь design-доков — `docs/features/<slug>/01-architecture.md` (и остальные там же);
подкаталог `design/` упомянуть только как возможный legacy-вариант.

## E8. GRAPH.md привести в соответствие (закрывает #18)
- нода 8: «planner возвращает разбиение по вертикалям; лид поднимает дополнительные раны роли `planner`»
  (убрать «сам спавнит sub-planner'ов»).
- нода 10: «масштабирование = повторный ран той же роли; метки -2 только во внутреннем реестре лида».
- нода 15: убрать «(Agent без Teams)» → «(субагенты Kent)».
- в разделе «Что это возвращает из источника» убрать `sub-planner'ы` и `backend-dev-2, test-dev-2, code-reviewer-2`,
  заменив на корректные формулировки; добавить строку про параллелизм фоновыми ранами (E0).
