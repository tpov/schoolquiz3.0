'use strict';
const { sc, mc, ord, fb, op4, op5, op6, items4, cand5 } = require('../_helpers');

// ===========================================================================
// LESSON 7.2.1 (C2 — Mastery) — Theme: Expert technical domains
// Topic: Compiler and runtime trade-offs. Low-level execution vocabulary —
// *JIT* vs *AOT* compilation, *GC* pauses and pressure, *inlining*,
// allocation, runtime overhead, warm-up, zero-cost abstractions. Register is
// formal/strategic — performance reviews, RFCs, profiling write-ups, design
// docs. Tech facts must be correct; near-native phrasing.
// ===========================================================================
module.exports = [
  // ---- SingleChoice EASY (5) ----
  sc(0, 'EASY', 'In a perf review you explain why the first few thousand requests are slow on the *JVM*. You say: "Throughput climbs once the *JIT* ___ and the hot paths get compiled to native code."',
    op4('warms up', 'cools down', 'rolls back', 'opts out'), 'a',
    'Идиома "the JIT warms up" описывает разогрев: горячие участки кода компилируются в нативный после серии прогонов.'),
  sc(1, 'EASY', 'A teammate asks why latency spikes every few seconds under load. You point at the runtime: "Those spikes are *GC* ___ — the collector stops the world to reclaim memory."',
    op4('leaks', 'pauses', 'caches', 'patches'), 'b',
    '"GC pauses" — остановки приложения, пока сборщик мусора освобождает память.'),
  sc(2, 'EASY', 'You want the compiler to drop the call overhead of a tiny accessor. The optimisation you are hoping for is ___: the body is spliced into the caller.',
    op4('inlining', 'batching', 'sharding', 'polling'), 'a',
    '*Inlining* — подстановка тела маленькой функции прямо в место вызова, чтобы убрать накладные расходы на вызов.'),
  sc(3, 'EASY', 'You note that a hot loop creates a short-lived object on every iteration. The concern you raise is excessive ___ on the fast path.',
    op4('allocation', 'indentation', 'duplication', 'pagination'), 'a',
    'Excessive allocation — частое выделение памяти под недолговечные объекты в горячем цикле, создающее нагрузку на сборщик.'),
  sc(4, 'EASY', 'A reviewer praises an abstraction that compiles away to the same machine code as a hand-written loop. You call it a ___ abstraction.',
    op4('costly', 'leaky', 'zero-cost', 'blocking'), 'c',
    '"Zero-cost abstraction" — абстракция, которая после компиляции не добавляет накладных расходов во время выполнения.'),

  // ---- SingleChoice HARD (5) ----
  sc(5, 'HARD', 'Which statement about *AOT* versus *JIT* compilation is correct as written?',
    op4('*AOT* compiles ahead of time, so there is no warm-up, but it cannot use runtime profiles to specialise hot code.', '*AOT* always produces faster steady-state code than a mature *JIT*.', '*JIT* compiles everything once at build time and never recompiles.', '*AOT* eliminates *GC* entirely by construction.'), 'a',
    'AOT компилирует заранее (нет разогрева), но лишён профиля времени выполнения для специализации горячих участков, в отличие от JIT.'),
  sc(6, 'HARD', 'You must explain *GC* pressure precisely. The most accurate phrasing is:',
    op4('*GC* pressure means the garbage collector has a memory leak.', '*GC* pressure is the rate of allocation that forces the collector to run more often, stealing CPU from the application.', '*GC* pressure is the latency added by a single full collection.', '*GC* pressure only matters in *AOT*-compiled binaries.'), 'b',
    '"GC pressure" — темп выделения памяти, заставляющий сборщик срабатывать чаще и отнимать процессорное время у приложения.'),
  sc(7, 'HARD', 'A profiler shows a method *JIT*-compiled but never inlined. Which explanation is technically sound?',
    op4('Inlining is impossible once a method is *JIT*-compiled.', 'The method exceeded the inlining size threshold, so the compiler kept the call rather than splicing the body in.', 'Inlining only happens in interpreted code, never after compilation.', 'A method that is *JIT*-compiled is, by definition, already inlined everywhere.'), 'b',
    'Компилятор отказывается встраивать метод, превысивший порог размера для инлайнинга, и оставляет обычный вызов.'),
  sc(8, 'HARD', 'Which claim about runtime overhead is the most defensible?',
    op4('A managed runtime has exactly zero overhead versus native code.', 'Bounds checks and *GC* add overhead, but a good *JIT* can elide many checks it proves redundant.', 'Runtime overhead is fixed and cannot be reduced by the compiler.', 'Removing bounds checks always makes a program faster and safer.'), 'b',
    'Накладные расходы среды (проверки границ, GC) реальны, но хороший JIT может убрать проверки, которые докажет избыточными.'),
  sc(9, 'HARD', 'You argue that an abstraction is *not* zero-cost. The phrasing that states this most precisely is:',
    op4('It leaks an allocation and a virtual call that survive optimisation, so it carries runtime cost.', 'It is slow because abstractions are always slow.', 'It cannot compile, therefore it has a cost.', 'It is zero-cost only on Tuesdays.'), 'a',
    'Абстракция не бесплатна, если после оптимизаций остаются аллокация и виртуальный вызов, добавляющие стоимость во время выполнения.'),

  // ---- MultipleChoice EASY (5) ----
  mc(10, 'EASY', 'Mark the phrases that describe *runtime* behaviour (not build-time):',
    op6('the *JIT* warms up', '*GC* pauses', 'compiled ahead of time', 'allocation on the hot path', 'static linking at build', 'collector reclaims memory'),
    ['a', 'b', 'd', 'f'],
    'К поведению во время выполнения относятся: разогрев JIT, паузы GC, аллокация на горячем пути, освобождение памяти сборщиком.'),
  mc(11, 'EASY', 'Which sentences use the low-level vocabulary *correctly*?',
    op6('The *JIT* warms up after a few thousand iterations.', 'A *GC* pause stops the world briefly.', 'Inlining adds a function-call overhead.', 'Allocation pressure makes the collector run more often.', '*AOT* compilation needs a long warm-up.', '*GC* compiles the source to bytecode.'),
    ['a', 'b', 'd'],
    'Верно: JIT разогревается, пауза GC останавливает мир, давление аллокаций учащает сборку. Инлайнинг убирает вызов, AOT не разогревается, GC не компилирует.'),
  mc(12, 'EASY', 'Pick the terms tied to *garbage collection*:',
    op6('collection pause', 'allocation rate', 'register', 'heap', 'semicolon', 'stop-the-world'),
    ['a', 'b', 'd', 'f'],
    'С сборкой мусора связаны: пауза сборки, темп аллокаций, куча (heap) и остановка мира (stop-the-world).'),
  mc(13, 'EASY', 'Mark the statements that correctly describe *inlining*:',
    op6('It splices the callee body into the caller.', 'It removes the call overhead.', 'It frees heap memory.', 'It can enable further optimisations.', 'It runs the garbage collector.', 'It compiles ahead of time only.'),
    ['a', 'b', 'd'],
    'Инлайнинг подставляет тело вызываемой функции в место вызова, убирает накладные расходы вызова и открывает путь дальнейшим оптимизациям.'),
  mc(14, 'EASY', 'Which contrasts between *AOT* and *JIT* are stated correctly?',
    op5('*AOT* has no warm-up; *JIT* does.', '*JIT* can use runtime profiles; *AOT* cannot.', '*AOT* compiles at run time; *JIT* at build time.', '*JIT* recompiles hot code; *AOT* is fixed once built.', '*AOT* always beats *JIT* at steady state.'),
    ['a', 'b', 'd'],
    'Верно: AOT без разогрева, JIT использует профиль времени выполнения, JIT перекомпилирует горячий код, а AOT фиксирован после сборки.'),

  // ---- MultipleChoice HARD (5) ----
  mc(15, 'HARD', 'In a profiling write-up, mark the claims that are *technically accurate*:',
    op6('A tiered *JIT* may recompile a hot method at a higher optimisation level.', '*GC* pressure rises with the allocation rate on the hot path.', 'Inlining a megamorphic call site is always free.', 'Escape analysis can let the *JIT* stack-allocate an object that never escapes.', '*AOT* binaries warm up faster than *JIT* because they recompile sooner.', 'A zero-cost abstraction compiles down to the same code as the hand-written version.'),
    ['a', 'b', 'd', 'f'],
    'Корректно: многоуровневый JIT перекомпилирует горячий метод агрессивнее; давление GC растёт с темпом аллокаций; escape-анализ позволяет разместить не убегающий объект на стеке; zero-cost абстракция компилируется в тот же код.'),
  mc(16, 'HARD', 'Mark the *legitimate* ways to cut *GC* pressure on a hot path:',
    op6('Reuse a buffer instead of allocating per iteration.', 'Replace short-lived objects with stack-allocated values.', 'Allocate twice as much on every call.', 'Pool objects to avoid churn.', 'Disable the garbage collector and never free memory.', 'Box every primitive into an object.'),
    ['a', 'b', 'd'],
    'Снижают давление GC: переиспользование буфера, размещение на стеке вместо кучи и пулинг объектов; больше аллокаций, отключение сборщика и боксинг — наоборот.'),
  mc(17, 'HARD', 'Which trade-offs between *AOT* and *JIT* are framed correctly?',
    op6('*AOT* gives predictable startup; *JIT* pays a warm-up cost first.', '*JIT* can specialise to the actual workload; *AOT* commits at build time.', '*AOT* peak throughput can trail a mature *JIT* that exploits runtime profiles.', '*JIT* needs no memory for compiled code at run time.', '*AOT* removes the need for a garbage collector.', '*AOT* recompiles hot loops while running.'),
    ['a', 'b', 'c'],
    'Корректные компромиссы: AOT даёт предсказуемый старт, JIT платит за разогрев; JIT специализируется под нагрузку; пиковая пропускная способность AOT может уступать зрелому JIT с профилем.'),
  mc(18, 'HARD', 'Mark the sentences that use *zero-cost abstraction* *accurately*:',
    op5('An iterator that compiles to the same loop as hand-written code is a zero-cost abstraction.', 'A zero-cost abstraction adds a guaranteed allocation at run time.', 'If the abstraction optimises away entirely, it is zero-cost.', 'Zero-cost means it always runs the garbage collector.', 'Zero-cost holds only when the optimiser actually removes the overhead.'),
    ['a', 'c', 'e'],
    'Верно: абстракция бесплатна, если компилируется в тот же код и оптимизатор реально убирает накладные расходы; гарантированная аллокация или запуск GC противоречат определению.'),
  mc(19, 'HARD', 'A reviewer overstated runtime claims. Mark the *honest, bounded* rewrites:',
    op6('"It has no overhead" → "The bounds check is elided on the paths the *JIT* proved safe."', '"It has no overhead" → "It is infinitely fast, no asterisks."', '"GC never fires" → "Allocation is low enough that *GC* rarely fires under our load."', '"GC never fires" → "We disabled memory entirely."', '"It inlines everywhere" → "It inlines below the size threshold the compiler enforces."', '"It inlines everywhere" → "Inlining is unconditional and unlimited."'),
    ['a', 'c', 'e'],
    'Честные ограниченные формулировки: проверка границ убрана лишь на доказанно безопасных путях; GC срабатывает редко при низкой аллокации; инлайнинг происходит ниже порога размера компилятора.'),

  // ---- Ordering EASY (5) ----
  ord(20, 'EASY', 'Build a runtime claim: "The JIT warms up after the loop".',
    items4('The JIT', 'warms up', 'after', 'the loop'),
    'Идиома "the JIT warms up" описывает разогрев компилятора после прогрева горячего цикла.'),
  ord(21, 'EASY', 'Build: "GC pauses stop the world briefly".',
    items4('GC pauses', 'stop the', 'world', 'briefly'),
    '"GC pauses" кратко останавливают приложение (stop the world), пока работает сборщик.'),
  ord(22, 'EASY', 'Build: "Inlining removes the call overhead".',
    items4('Inlining', 'removes', 'the call', 'overhead'),
    '*Inlining* убирает накладные расходы на вызов функции, подставляя её тело.'),
  ord(23, 'EASY', 'Build: "Allocation pressure makes the collector run".',
    items4('Allocation pressure', 'makes the', 'collector', 'run'),
    'Давление аллокаций (GC pressure) вынуждает сборщик мусора срабатывать чаще.'),
  ord(24, 'EASY', 'Build: "A zero-cost abstraction adds no overhead".',
    items4('A zero-cost', 'abstraction', 'adds no', 'overhead'),
    '"Zero-cost abstraction" не добавляет накладных расходов во время выполнения.'),

  // ---- Ordering HARD (5) ----
  ord(25, 'HARD', 'Build a trade-off: "AOT skips warm-up but loses runtime profiles?".',
    items4('AOT skips warm-up', 'but loses', 'runtime', 'profiles?'),
    'Компромисс AOT: нет разогрева, но теряется профиль времени выполнения для специализации.'),
  ord(26, 'HARD', 'Build: "Escape analysis lets the JIT stack-allocate the object?".',
    items4('Escape analysis lets', 'the JIT', 'stack-allocate', 'the object?'),
    'Escape-анализ позволяет JIT разместить не убегающий объект на стеке вместо кучи.'),
  ord(27, 'HARD', 'Build: "The hot path allocates too much per iteration?".',
    items4('The hot path', 'allocates too much', 'per', 'iteration?'),
    'Чрезмерная аллокация на горячем пути в каждой итерации повышает давление на сборщик.'),
  ord(28, 'HARD', 'Build: "A tiered JIT recompiles hot code more aggressively?".',
    items4('A tiered JIT', 'recompiles', 'hot code', 'more aggressively?'),
    'Многоуровневый JIT перекомпилирует горячий код на более высоком уровне оптимизации.'),
  ord(29, 'HARD', 'Build: "The bounds check is elided on proven paths?".',
    items4('The bounds check', 'is elided', 'on proven', 'paths?'),
    'JIT убирает проверку границ только на путях, которые он доказал безопасными.'),

  // ---- FillBlank EASY (5) ----
  fb(30, 'EASY', 'Throughput rises once the *JIT* ___ up.',
    [{ id: 'b1', correctCandidateId: 'c2' }], cand5('cools', 'warms', 'rolls', 'shuts', 'gives'),
    'Идиома "the JIT warms up" — компилятор разогревается, и пропускная способность растёт.'),
  fb(31, 'EASY', 'The latency spikes are *GC* ___ that stop the world.',
    [{ id: 'b1', correctCandidateId: 'c3' }], cand5('leaks', 'caches', 'pauses', 'patches', 'queues'),
    '"GC pauses" — паузы сборки мусора, останавливающие приложение.'),
  fb(32, 'EASY', 'The compiler does ___ to remove the call overhead.',
    [{ id: 'b1', correctCandidateId: 'c4' }], cand5('sharding', 'polling', 'batching', 'inlining', 'paging'),
    '*Inlining* подставляет тело функции в место вызова и убирает накладные расходы вызова.'),
  fb(33, 'EASY', 'Too much ___ on the hot path raises *GC* pressure.',
    [{ id: 'b1', correctCandidateId: 'c1' }], cand5('allocation', 'indentation', 'pagination', 'duplication', 'rotation'),
    'Частая allocation на горячем пути увеличивает нагрузку на сборщик (GC pressure).'),
  fb(34, 'EASY', 'It compiles away, so it is a ___ abstraction.',
    [{ id: 'b1', correctCandidateId: 'c5' }], cand5('costly', 'leaky', 'blocking', 'managed', 'zero-cost'),
    '"Zero-cost abstraction" не добавляет накладных расходов после компиляции.'),

  // ---- FillBlank HARD (5) ----
  fb(35, 'HARD', 'Unlike *JIT*, *AOT* has no warm-up but cannot use runtime ___.',
    [{ id: 'b1', correctCandidateId: 'c3' }], cand5('threads', 'caches', 'profiles', 'sockets', 'mutexes'),
    'AOT лишён профиля времени выполнения (runtime profiles), которым пользуется JIT для специализации.'),
  fb(36, 'HARD', '*GC* pressure is the ___ rate that forces the collector to run.',
    [{ id: 'b1', correctCandidateId: 'c2' }], cand5('refresh', 'allocation', 'sampling', 'request', 'commit'),
    '"GC pressure" определяется темпом аллокаций, который заставляет сборщик срабатывать чаще.'),
  fb(37, 'HARD', 'Escape analysis lets the *JIT* ___ an object that never escapes.',
    [{ id: 'b1', correctCandidateId: 'c4' }], cand5('serialise', 'encrypt', 'replicate', 'stack-allocate', 'deprecate'),
    'Escape-анализ позволяет разместить на стеке (stack-allocate) объект, который не убегает за пределы метода.'),
  fb(38, 'HARD', 'The method exceeded the inlining size ___, so the call stayed.',
    [{ id: 'b1', correctCandidateId: 'c1' }], cand5('threshold', 'register', 'manifest', 'checksum', 'namespace'),
    'Метод превысил порог размера (size threshold) для инлайнинга, поэтому вызов не был встроен.'),
  fb(39, 'HARD', 'A good *JIT* can ___ a bounds check it proves redundant.',
    [{ id: 'b1', correctCandidateId: 'c5' }], cand5('inflate', 'duplicate', 'serialise', 'schedule', 'elide'),
    'Хороший JIT может убрать (elide) проверку границ, которую докажет избыточной.'),
];
