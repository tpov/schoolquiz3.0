# Summary
Verdict: CONTESTED

Counts: 1 HIGH, 4 MEDIUM, 1 LOW.

# Findings
1. **Severity:** HIGH. **Category:** InvariantViolation. **Location:** `01-architecture.md:397-411, 842-920`; `02-behavior.md:33-38, 61, 267, 604-626`.  
**Evidence:** The design models `ObserveAppShellStateUseCase` as `invoke(currentStateProvider: () -> AppShellState)` and even lists a phase-01 domain edit: “`parameter initialState: AppShellState → currentStateProvider: () -> AppShellState`” (`01-architecture.md:919-920`). The real code still is `operator fun invoke(initialState: AppShellState): Flow<AppShellState> = ... map { stats -> initialState.copy(userStats = stats) }` ([ObserveAppShellStateUseCase.kt:29](/home/Programming/Android/schoolquiz4.0/shared/feature/app-shell/domain/src/commonMain/kotlin/com/tpov/schoolquiz/shared/feature/app_shell/domain/use_case/ObserveAppShellStateUseCase.kt:29)). Grounding says phase-01 is “`adapter-integration + infrastructure wiring`” ([2-grounding.md:16](/home/Programming/Android/schoolquiz4.0/docs/features/app-shell-menu/2-grounding.md:16)).  
**Impact:** The docs do not describe the current 229-test Walking Skeleton; they require a domain rewrite plus test edits before the runtime diagrams become true. That breaks the stated phase-01 boundary.

2. **Severity:** MEDIUM. **Category:** ModelMismatch. **Location:** `02-behavior.md:306-309, 816-875`.  
**Evidence:** The sequences describe tab switching as restoring “`ранее сохранённый ... TabState (или default если первый визит)`” and explicitly call `visibleSections(INTERNET, UserStats.guest())` / `visibleSections(EVENTS, UserStats.guest())` inside `onSwitchTab` (`02-behavior.md:307-308, 834-840, 869-874`). The real code precomputes all per-tab states up front in `AppShellState.default(...)` with `internetState = initialInternetTabState(stats)` and `eventsState = initialEventsTabState(stats)` ([AppShellState.kt:59](/home/Programming/Android/schoolquiz4.0/shared/feature/app-shell/domain/src/commonMain/kotlin/com/tpov/schoolquiz/shared/feature/app_shell/domain/state/AppShellState.kt:59)). Actual `onSwitchTab` only does `TransitionResult(state.copy(activeTab = targetTab, isDrawerOpen = false))` ([AppShellTransitions.kt:148](/home/Programming/Android/schoolquiz4.0/shared/feature/app-shell/domain/src/commonMain/kotlin/com/tpov/schoolquiz/shared/feature/app_shell/domain/logic/AppShellTransitions.kt:148)).  
**Impact:** The journey traces for first entry into INTERNET/EVENTS do not follow the real runtime path. An implementer following the diagrams would add lazy-init logic the domain does not have.

3. **Severity:** MEDIUM. **Category:** SDKContract. **Location:** `01-architecture.md:41-43, 598-602, 606-617`.  
**Evidence:** L2 still says `MainActivity` “`Вызывает startKoin{}`” (`01-architecture.md:43`), but the later Koin section says “`startKoin — AppApplication.onCreate()`” (`01-architecture.md:601`) and the code sample moves it into `AppApplication` (`01-architecture.md:609-617`). The current scaffold has no application class in the manifest ([AndroidManifest.xml:3](/home/Programming/Android/schoolquiz4.0/apps/android-next/src/main/AndroidManifest.xml:3)) and `MainActivity` currently contains only a `TextView` stub ([MainActivity.kt:9](/home/Programming/Android/schoolquiz4.0/apps/android-next/src/main/java/com/tpov/schoolquiz/apps/android_next/MainActivity.kt:9)). The official Koin docs show the standard Android setup “`From your Application class`” with `startKoin { androidContext(this@MainApplication) ... }` ([Koin docs](https://insert-koin.io/docs/reference/koin-android/start/)).  
**Impact:** The architecture doc has two different startup owners. If phase-01 follows the C4/L2 text literally, the DI bootstrap will land in the wrong place or the manifest registration will be missed.

4. **Severity:** MEDIUM. **Category:** MissingTrace. **Location:** `01-architecture.md:242-256, 661-668`.  
**Evidence:** The doc first places the contract at `.../domain/model/Navigator.kt` (`01-architecture.md:245`), then later at `.../domain/navigation/Navigator.kt` (`01-architecture.md:666-668`). In real code, the only domain artifact is the concept mention in `Destination.kt`: “`The single Navigator method is: goTo(destination: Destination)`” ([Destination.kt:7](/home/Programming/Android/schoolquiz4.0/shared/feature/app-shell/domain/src/commonMain/kotlin/com/tpov/schoolquiz/shared/feature/app_shell/domain/model/Destination.kt:7)). Grounding confirms “`no Navigator interface file exists`” ([2-grounding.md:204](/home/Programming/Android/schoolquiz4.0/docs/features/app-shell-menu/2-grounding.md:204)). The spec requires that feature modules import `Navigator` from domain (`0-spec.md:34,45`).  
**Impact:** NFR #3 is not traceable to a single package path. Phase-01 consumers cannot derive a stable import or Koin binding target from the architecture doc.

5. **Severity:** MEDIUM. **Category:** ModelMismatch. **Location:** `01-architecture.md:72-76, 510-557`.  
**Evidence:** L2 models `UserStatsRepositoryImpl` as “`Kotlin / androidMain`” (`01-architecture.md:73-76`), but the later class diagram marks `UserStatsRepositoryImpl` as `<<data/commonMain>>` (`01-architecture.md:542-548`). The actual data module is a KMP module today ([data/build.gradle.kts:1](/home/Programming/Android/schoolquiz4.0/shared/feature/app-shell/data/build.gradle.kts:1)). The prior-art doc explicitly calls out the tradeoff: androidMain-only impl means “`JVM target не работает`” (`05-prior-art.md:209-213`).  
**Impact:** The doc is not a reliable source for source-set ownership. Following the wrong half of it can break the KMP/JVM story or make the class diagram false on day one of phase-01.

6. **Severity:** LOW. **Category:** ModelMismatch. **Location:** `02-behavior.md:105, 522`; `01-architecture.md:465-467`.  
**Evidence:** Behavior uses invented model members: `state.activeSection?.displayName ?: state.activeTab.displayName` (`02-behavior.md:105`), but `Tab` only defines enum constants ([Tab.kt:7](/home/Programming/Android/schoolquiz4.0/shared/feature/app-shell/domain/src/commonMain/kotlin/com/tpov/schoolquiz/shared/feature/app_shell/domain/model/Tab.kt:7)) and `DrawerSection` only exposes `tab` and `requiredRoles` ([DrawerSection.kt:18](/home/Programming/Android/schoolquiz4.0/shared/feature/app-shell/domain/src/commonMain/kotlin/com/tpov/schoolquiz/shared/feature/app_shell/domain/model/DrawerSection.kt:18)). The same doc later renders `LocalScreenComponent.DesignCatalog(config=DesignCatalogRoot)` (`02-behavior.md:522`), but the architecture doc defines only `LocalScreenComponent.Placeholder(val config: LocalConfig)` (`01-architecture.md:465-467`).  
**Impact:** Parts of the behavior doc are not compilable against the current model surface. A UI mapping layer may be fine, but it is not modeled explicitly here.

# Positive Notes
- The docs are strong on the pure-domain core: `InitializeAppShellUseCase.invoke()`, `NavigateUseCase.invoke(state, destination)`, `OnTabRetapUseCase.invoke(state, tab)`, `TransitionResult`, `RetapOutcome`, `UserStatsRepository.observeStats()/currentStats()`, and the visibility helpers all line up with the current domain files.
- The progressive-unlock model is grounded well. `actualLevel`, `isVisible`, `visibleSections`, `rootOf`, `emptyRootFor`, and the `DrawerSection.requiredRoles` thresholds match the real code and the legacy-derived rules.
- The MVP state-saving stance is internally coherent now: the code keeps `@Serializable` out of `TabConfig`, the spec defers state restoration to a future phase, and the Decompose docs explicitly allow `serializer = null` to disable navigation state saving.
- Most SDK usage decisions are directionally correct: Decompose’s `defaultComponentContext()` in `Activity.onCreate`, unique keys for multiple child stacks, Koin parameter injection for `ComponentContext`, and `ModalNavigationDrawer` controlled through suspend `DrawerState.open()/close()` all match the official docs: [Koin Android Start](https://insert-koin.io/docs/reference/koin-android/start/), [Koin Injection Parameters](https://insert-koin.io/docs/reference/koin-core/injection-parameters/), [Decompose Child Stack](https://arkivanov.github.io/Decompose/navigation/stack/overview/), [Decompose Component Overview](https://arkivanov.github.io/Decompose/component/overview/), [Android Drawer docs](https://developer.android.com/develop/ui/compose/components/drawer).