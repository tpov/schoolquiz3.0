**Blocker**
- `shared/core/sync/src/commonMain/kotlin/com/tpov/schoolquiz/shared/core/sync/CascadingSyncOrchestrator.kt:119` advances the `quests` cursor before the downstream section/theme/lesson/question cascade succeeds, and the TODO at `:120` explicitly says this loses pending children. Same pattern repeats at `:140`, `:161`, `:182`, `:199`. This violates the partial-cascade recovery contract in `docs/features/home-and-my-quests/0-spec.md:765`, where retry must continue from the old child cursor after a lower-step failure. `SyncStateRepository.markCascadeInProgress()` exists at `shared/core/sync/src/commonMain/kotlin/com/tpov/schoolquiz/shared/core/sync/SyncStateRepository.kt:24` but is not used in production code, so the recovery abstraction is currently orphaned.

- `shared/feature/section/data/src/commonMain/kotlin/com/tpov/schoolquiz/shared/feature/section/data/SectionRepositoryImpl.kt:32` returns every fetched section id as a cascade parent before checking whether `contentsVersion` actually grew. Same issue in `ThemeRepositoryImpl.kt:31`, `LessonRepositoryImpl.kt:31`, and `QuestRepositoryImpl.kt:55`. The SSoT says recursion happens only when `dto.contentsVersion > local.contentsVersion` at `docs/features/home-and-my-quests/0-spec.md:889` and `:906`. Current code will cascade stale/no-op parents and deleted parents, so ADR-CMP-49/ADR-CMP-56 parent-id semantics are not implemented.

**High**
- `android/feature/quest/presentation/src/main/kotlin/com/tpov/schoolquiz/android/feature/quest/presentation/di/QuestPresentationModule.kt:26` requires `(ComponentContext, Navigator)` parameters, but canonical §13 says `factory<MyQuestsComponent> { (ctx: ComponentContext) -> DefaultMyQuestsComponent(ctx, get(), get(), get(), get()) }` at `docs/features/home-and-my-quests/06-api-contract.md:801`. The workaround is then pushed into `AppShellScreen` via `getKoin()` at `android/feature/app-shell/presentation/src/main/kotlin/com/tpov/schoolquiz/android/feature/app_shell/presentation/ui/AppShellScreen.kt:335`, which the file itself marks as a DI anti-pattern at `:332`. Either update §13 or move component creation back to the composition/root component path.

- `android/feature/app-shell/presentation/src/main/kotlin/com/tpov/schoolquiz/android/feature/app_shell/presentation/component/DefaultRootComponent.kt:18` imports the concrete platform `SyncWorker`, and `:231` builds/enqueues WorkManager requests directly from presentation. That pulls `platform/android-services` into app-shell presentation (`android/feature/app-shell/presentation/build.gradle.kts:13`) instead of hiding sync scheduling behind a domain/application interface. This crosses the clean layer boundary: presentation now knows the worker implementation class.

**Medium**
- `apps/android-next/src/test/java/com/tpov/schoolquiz/apps/android_next/KoinModuleWiringTest.kt:171` only wires `testDataSourceModule`, `testDaoModule`, `testWorkManagerModule`, `appShellDataModule`, and `appShellPresentationModule`. It does not smoke-test the real graph additions from `AppApplication.kt:86` through `:110` (`questPresentationModule`, new data modules, Firebase modules, `syncModule`). That leaves the §13 Koin drift above untested.

**Open Questions**
- None blocking on ADR-HMQ-06 import direction: I found no reverse content-chain imports (`quest -> section/theme/...`, `section -> theme/...`, etc.). The app-shell presentation exception is documented at `docs/features/home-and-my-quests/03-decisions.md:747`.