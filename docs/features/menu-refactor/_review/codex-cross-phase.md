- [blocker] `shared/feature/app-shell/data/src/commonMain/kotlin/com/tpov/schoolquiz/shared/feature/app_shell/data/UserStatsRepositoryImpl.kt:21` — `observeStats()` captures `currentUid()` once and then stays on `userStatsDao.observeByUid(uid)` forever. After the Room migration, the old auth-change resubscribe contract from `platform/firebase/.../FirebaseUserStatsDataSource.kt:25-38` is gone, but `DefaultRootComponent` still assumes the flow will reconnect on auth changes (`android/feature/app-shell/presentation/.../DefaultRootComponent.kt:167-185`). On logout/account-switch, the shell can keep rendering the previous user’s cached row, including nickname and qualification-gated menu visibility. — Suggested fix: make auth state explicit in the repository (`Flow<String?>` + `flatMapLatest { uid -> uid?.let(dao::observeByUid) ?: flowOf(null) }`) and emit guest/null-state on logout; optionally clear the old row on sign-out.

- [blocker] `shared/feature/app-shell/data/src/commonMain/kotlin/com/tpov/schoolquiz/shared/feature/app_shell/data/UserStatsRepositoryImpl.kt:31` — dev-mode activation depends on a preexisting `user_stats` row, but the feature never guarantees one exists. `setLocalDeveloperLevel()` does a targeted `UPDATE` only; if Room is empty, it silently writes nothing. At the same time, cold start reads only Room (`shared/feature/app-shell/domain/.../InitializeAppShellUseCase.kt:20-22`), and `AppApplication` schedules only periodic sync (`apps/android-next/src/main/java/com/tpov/schoolquiz/apps/android_next/AppApplication.kt:50-63`) with no eager bootstrap. On a fresh install / first login, the shell stays on `UserStats.guest()` and 10-tap “activation” does not persist `developer=100`. — Suggested fix: enqueue an immediate one-shot profile refresh after auth/app start, and make `setLocalDeveloperLevel()` upsert-or-create the row when it is missing instead of relying on `UPDATE` alone (`shared/core/persistence/.../UserStatsDao.kt:20-21`).

- [high] `apps/android-next/src/main/java/com/tpov/schoolquiz/apps/android_next/MainActivity.kt:28` — `MainActivity` and `AppShellScreen` both collect `rootComponent.events` (`android/feature/app-shell/presentation/.../ui/AppShellScreen.kt:123-145`), but `DefaultRootComponent` exposes that stream from a single-consumer `Channel.receiveAsFlow()` and documents it as such (`android/feature/app-shell/presentation/.../component/DefaultRootComponent.kt:98-103`). Result: `DevModeActivated` / `SyncStarted` can be consumed by the activity’s no-op `TODO Phase 07` branches (`MainActivity.kt:33-35`) instead of the screen snackbar handler, and `SystemBack` ownership is split nondeterministically. — Suggested fix: keep exactly one consumer for `events` or change the contract to a multicast `SharedFlow` if both activity and composable must observe the same event.

- [medium] `shared/core/catalog/domain/src/commonMain/kotlin/com/tpov/schoolquiz/shared/core/catalog/domain/di/CatalogDomainModule.kt:9` — the app registers `catalogDomainModule` in production (`apps/android-next/src/main/java/com/tpov/schoolquiz/apps/android_next/AppApplication.kt:45-47`), but `CreateQuestUseCase` requires a `QuestRepository` that has no production binding anywhere in the graph. The only `QuestRepository` in the feature is explicitly marked temporary (`shared/core/catalog/domain/.../repository/QuestRepository.kt:6-10`). This leaves a latent `NoBeanDefFoundException` in a “completed” module and is a classic orphaned abstraction from an earlier phase. — Suggested fix: remove `CreateQuestUseCase` from the production Koin module until quiz integration exists, or wire a real `QuestRepository` from the owning quiz module before registering `catalogDomainModule`.

- [medium] `android/core/designsystem/src/main/kotlin/com/tpov/schoolquiz/android/core/designsystem/model/CatalogDisplayItem.kt:23` — phase 05 resolves and caches `CatalogEntity.pictureUrl` (`shared/core/catalog/data/.../CatalogRepositoryImpl.kt:27-37`), but phase 07 drops that information again: `CatalogEntity.toDomain()` strips it (`shared/core/catalog/data/.../mapper/CatalogMapper.kt:7-11`), and the only UI mapper hardcodes `pictureUrl = null`. There is no production bridge from the cached Room URL to `CatalogDisplayItem`, so the new grid/spinner foundation cannot ever show the pre-resolved images without bypassing the documented repository/domain path. — Suggested fix: add the missing display-facing flow/mapper from persisted entities to `CatalogDisplayItem` and remove or rename the current nulling `Catalog.toDisplayItem()` helper so callers cannot accidentally discard URLs.

- [low] `shared/feature/app-shell/data/src/commonMain/kotlin/com/tpov/schoolquiz/shared/feature/app_shell/data/di/AppShellDataModule.kt:19` — the sync DI story is internally inconsistent across phases: this module exports `single<Syncable>` for user-stats, but the actual worker graph ignores it and hand-builds `List<Syncable>` from repository interfaces in `apps/android-next/src/main/java/com/tpov/schoolquiz/apps/android_next/di/SyncModule.kt:14-18`; catalog contributes no matching `Syncable` singleton at all. The extra binding is currently orphaned and easy to let drift from the real worker topology. — Suggested fix: choose one pattern and use it everywhere: either remove the unused `single<Syncable>` binding, or register both repositories as `Syncable` and resolve them via `getAll<Syncable>()`.

Assumptions: static review only; I did not run Gradle/tests in this read-only sandbox.