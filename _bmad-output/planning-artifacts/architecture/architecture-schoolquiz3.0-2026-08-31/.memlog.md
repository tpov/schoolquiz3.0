---
scope: Синхронизация приложения: клиент и сервер — журнал изменений, очереди отправки, каденции, push
purpose: build-substrate
altitude: feature
updated: 2026-09-01T10:22
---

- (direction) Пользователь выбрал Coaching path и все три артефакта: спайн + наглядная страница + разбивка работы по кускам.
- (constraint) Унаследовано из SPEC-sync, не пере-решается: журнал sync_changes канонический; общая очередь для новых действий, три существующие не переписываются; push несёт повод, а не данные; идемпотентность обязательна; курсор двигается только после успешного применения.
- (constraint) Унаследовано из PROJECT-CONTEXT.md: KMP слои domain/data/presentation/ui; Koin, не Hilt; Decompose Component вместо ViewModel; Compose не резолвит Koin; фейки вместо моков, без Turbine; гейт ./gradlew ciCheck --no-configuration-cache.
- (constraint) Унаследовано из docs/invariants.md: инвариант 8 (auth-scoped Flow re-subscribe) распространяется на состояние sync; инвариант 1 (чистота domain); инвариант 5 (уникальность Koin-биндинга).
- (note) PROJECT-CONTEXT.md Known Debts поправлен: строка про cursor strategy через CascadingSyncOrchestrator устарела (класс удалён 1cb9f366), действующая стратегия setCursor(max(changedAtMs)).
- (event) Запущена разведка кодовой базы: 5 параллельных читателей (outbox-паттерн, четыре фичи без очереди, Room-схема и миграции, callable-адаптер, сервер) плюс критик полноты.
- (note) РАЗВЕДКА: очередей три, не две — lesson_result_attempt_outbox, quest_rating_outbox (форма sent_at_ms-надгробие) и quest_arena_submission_outbox (форма DELETE-on-sent). Две несовместимые формы «отправлено» уже сосуществуют в коде.
- (note) РАЗВЕДКА: транспортов тоже два — lesson идёт через callable submitLessonResultEvents, arena пишет напрямую в Firestore .set(). Прецедент «как отправлять» неоднозначен.
- (note) РАЗВЕДКА, критично: на сервере НЕТ инфраструктуры ключей идемпотентности — 0 совпадений requestId/mutationId/idempotency в functions/index.js. Единственный дедуп — doc-id по клиентскому attemptId/ratingId. Из восьми мутаций без очереди безопасна для ретрая одна (unlockLesson, естественная идемпотентность).
- (note) РАЗВЕДКА: пять мутаций нельзя отложить не из-за идемпотентности, а потому что цена или награда рождается на сервере в момент вызова и показывается на экране — цена сердца зависит от текущего числа сердец, цена ника от реестра, лот может уйти другому покупателю.
- (assumption) ОТМЕНЕНО ПРЕДПОЛОЖЕНИЕ SPEC: смена аккаунта не латентна. FirebaseGoogleSignInRepository делает signInWithCredential (не link) и возвращает GoogleLinkOutcome.SWITCHED; единственный обработчик — тост. Записи очереди с прежним userId после switch будут вечно получать отказ сервера.
- (note) РАЗВЕДКА: механизма порядка клиент/сервер нет вообще — нет минимальной версии клиента, нет Remote Config, сервер не знает версию клиента, клиент не шлёт schemaVersion, деплой функций ручной npm-скрипт, CI для functions отсутствует.
- (note) РАЗВЕДКА: Room — версия 3, 23 entity; схема 1.json идентична 2.json (совпадает identityHash), то есть история миграций недостоверна; ни одна миграция не покрыта тестом, runMigrationsAndValidate в репозитории не встречается.
- (note) РАЗВЕДКА: три легальных источника времени в коде (лямбда nowMs, kotlinx Clock, доменный TimestampProvider) и три места регистрации sync-классов в Koin (app-модуль, commonMain фичи, androidMain фичи). Оба — готовые точки расхождения.
- (note) РАЗВЕДКА: три несовместимые формы документа sync_change уже в проде, у читателя защитный fallback. Плюс три способа записи quests.version: клиентский localRevision, read-modify-write +1, FieldValue.increment(1).
- (note) РАЗВЕДКА: сети никто не детектирует — 0 совпадений ConnectivityManager/NetworkMonitor/isOnline. Отсутствие сети и «не хватает золота» доходят до UI одинаково, как error.message. FirebaseFunctions без setTimeout, спиннер держится 70 секунд.
- (note) РАЗВЕДКА: getLocalContentsVersion реализован в трёх LocalDataSource и трёх RepositoryImpl, но ни разу не вызывается из production-кода. При этом firestore.rules требует contentsVersion == 0 при create квеста — удалить поле с клиента нельзя, пока правила не изменены. Порядок удаления имеет значение.
- (event) Разведка завершена: 6 агентов, 238 фактов с привязкой к file:line, 15 дыр совместимости от критика.
- (decision) AD: сервер получает ключ идемпотентности (mutationId) для мутаций очереди. Клиент генерирует id при постановке в очередь, сервер хранит выполненные и отвергает повтор. Без этого очередь для любых денежных действий невозможна — сегодня дедуп есть только у прохождений и оценок через doc-id.
- (decision) AD: мутации, чья цена или награда рождается на сервере в момент вызова (покупка сердца, ника, лота, логотипа, смена ника), остаются синхронными. Без сети действие недоступно и объяснено словами, а не спиннером на 70 секунд. Оптимистичное списание отвергнуто: клиент не может предсказать цену.
- (decision) AD: отправленная запись очереди удаляется локально. Очередь — это очередь, не архив. История остаётся на сервере, при необходимости архивируется там. Побеждает форма arena-очереди (DELETE-on-sent), форма надгробия из lesson-очереди отменяется — обе существующие таблицы приводятся к ней.
- (decision) AD: смена аккаунта возможна только онлайн. Перед сменой очередь сливается; если слить не удалось, игрок получает явное предупреждение, что последние действия могут не сохраниться и во вход можно не попасть. Оффлайн-мультиаккаунт отложен.
- (direction) Пользователь: «он не сможет сменить аккаунт без интернета, при выходе уведомляем — если нет интернета, последние действия могут отмениться и на аккаунт не зайти; потом продумаем оффлайн-мультиакк».
- (question) Отложено: оффлайн-мультиаккаунт — несколько игроков на одном устройстве с раздельными очередями.
- (decision) AD: одна таблица отложенных действий на всё — id, mutationId, тип операции, payload строкой, счётчик попыток, время следующей попытки, владелец (uid). Новый тип действия не требует правки схемы. Отвергнут вариант «таблица на тип»: типов будет много, а миграции в проекте уже недостоверны.
- (decision) AD: живых установок приложения нет (подтверждено владельцем), поэтому локальная база пересоздаётся одной чистой схемой — очередь, удаление contentsVersion и починка фиктивной истории миграций делаются разом, без цепочки. С этого момента миграции обязательны и покрыты тестом runMigrationsAndValidate.
- (decision) AD: обратная совместимость клиента не поддерживается — механизм минимальной версии не строится, сервер не принимает старый формат. Основание: приложение существует только локально у владельца.
- (constraint) Серверные данные (курсы, вопросы, профили) НЕ удаляются: изменение формы данных делается backfill-скриптом, а не пересозданием коллекций. Прецеденты: scripts/backfill-catalogs.js, scripts/backfill-sync-changes.js.
- (decision) AD: пуши делаются после первой волны фиксов; место под них в архитектуре закладывается сразу.
- (decision) AD: движок очереди живёт в НОВОМ модуле shared/core/outbox, а не в shared/core/sync. Основание: core/sync уже тянет domain-репозитории шести фич (CatalogSyncListOrchestrator), и движок очереди унаследовал бы этот вес. outbox остаётся чистым: только Kotlin и контракты хранения.
- (decision) AD: единственный транспорт отложенной мутации — один callable-приёмник на сервере. Прямая запись в Firestore из очереди (как сегодня делает QuestArenaSubmissionSync) запрещена: проверить mutationId можно только там, где есть код.
- (decision) AD: mutationId генерирует клиент при постановке в очередь. Сервер хранит выполненные mutationId и на повтор возвращает сохранённый результат, а не выполняет заново.
- (decision) AD: один источник времени на весь sync — доменный контракт в core, инжектится. Три существующих способа (лямбда nowMs, kotlinx Clock, QuestAuthoringTimestampProvider) приводятся к нему.
- (decision) AD: регистрация в Koin — движок и таблица очереди в composition root (app-модуль), описание типов мутаций в модуле своей фичи. Сегодня регистрация размазана по трём разным местам.
- (decision) AD: одна форма документа sync_change — {type, id, changedAtMs}. Три сосуществующие формы приводятся к ней, защитный fallback у читателя убирается.
- (decision) AD: version сущности пишет только сервер. Клиент перестаёт слать localRevision при публикации.
- (decision) AD: порядок удаления contentsVersion строго — firestore.rules, затем сервер, затем клиент. Правила сегодня требуют contentsVersion == 0 при create квеста; обратный порядок ломает создание.
- (decision) AD: состояние синхронизации выходит наружу отдельным репозиторием — Flow с временем последнего успеха, числом неотправленных и последней ошибкой. Auth-scoped по инварианту 8. Читают шторка и настройки.
- (decision) AD: вводится различение ошибок и детектор сети. Сегодня их нет вообще: нет сети и «не хватает золота» доходят до UI одинаково, как error.message, а спиннер держится 70 секунд. Ошибка callable мапится в доменный тип: НетСети / Отказ(причина) / Неизвестно.
- (decision) AD: токен устройства для push живёт в users/{uid}/devices/{token}, пишется клиентом при регистрации, читается сервером при отправке. Push несёт повод синхронизироваться, не данные.
- (event) Coaching-этап завершён: 16 архитектурных решений зафиксировано, переход к дистилляции спайна.
- (event) Спайн отрендерен: 21 AD, диаграмма направления зависимостей, операционный контур, карта CAP-1..CAP-10, 9 пунктов Deferred. lint_spine.py — 4 low, все ложные (шаблоны путей {type}_{id} и users/{uid}/devices/{token}). Запущена сверка со входами.
- (event) Сверка со входами: все три вердикта «ЕСТЬ ПРОТИВОРЕЧИЯ». Семь пропусков закрыто правками спайна, три противоречия сняты обновлением SPEC (CAP-2 сузился, constraint про миграции и про три очереди пересмотрены, предположение о латентности смены аккаунта опровергнуто).
- (decision) AD-22 добавлен: серверно-защищённые поля не меняются локально. Найдено сверкой: AD-9 как общее правило подталкивал к оптимистичной записи полки и квалификации, что запрещено constraint спеки.
- (decision) AD-23 добавлен: удаление выражается отсутствием id в ответе refreshByIds, пустым visibleOn и archived; поле deleted не вводится. Найдено сверкой: фактический контракт удаления жил только в коде.
- (decision) AD-24 добавлен: конфликт версий — отдельная ветвь ошибки, не отказ, в карантин не уходит. Найдено сверкой: AD-12 давал только серверный инкремент, а обещал предотвратить молчаливую перезапись — инкремент сам по себе конфликт не ловит.
- (decision) AD-11 ослаблен по замечанию сверки: инвариантом объявлены «один документ на узел» и «id без времени», а конкретная схема id остаётся за каждым из четырёх журналов — они уже разделены путём. Прежняя формулировка требовала {type}_{id} везде и противоречила трём работающим журналам.
- (decision) AD-4 расширен: колонки entity_ref (пометка «не отправлено» для конкретной сущности, без разбора payload ядром) и expected_version.
- (event) Прогон возобновлён после гибели процесса. Coaching не переигрывался: спайн отрендерен из журнала, 21 AD.
- (event) СВЕРКА СО ВХОДАМИ (шаг, на котором прогон умер) выполнена. Найдено четыре расхождения между 21 AD и SPEC-sync.
- (decision) Расхождение 1, ОТМЕНА, зафиксирована явно в AD-16: решение о пересоздании локальной базы отменяет ограничение SPEC-sync «схема Room меняется миграцией, а не пересозданием базы — прецедент a6df38ce». Отмена законна (владелец подтвердил отсутствие живых установок) и записана с явным сроком годности: действует, пока установок нет, истекает в момент первой публикации.
- (decision) Расхождение 2, ЧАСТИЧНАЯ ОТМЕНА: SPEC-sync требует «три существующие очереди не переписываются», но AD-4 приводит обе несовместимые формы «отправлено» к DELETE-on-sent, то есть quest_rating_outbox переписывается. Отмена узкая — меняется форма завершения записи, не механика самих очередей. Записана в AD-4 как приведение к общей форме.
- (note) Расхождение 3, В ПОЛЬЗУ СПЕКИ: SPEC-sync помечал «version инкрементирует сервер» как assumption, не подтверждённый владельцем. AD-12 подтверждает его как решение. Предположение повышено до решения, спеку можно обновить.
- (question) Расхождение 4, ДЫРА: ограничение SPEC-sync о пагинации выборки изменений (fetchChangedSince без limit и без пагинации) не покрыто ни одним из 21 AD. Форма продолжения не решалась. Записано в спайн отдельной секцией «Что этот спайн не защищает» и в разбивку как блокер до эпика E7.
- (question) Дыра 2: CAP-4 (принудительный полный ресинк) не покрыта ни одним AD. Сегодня путь есть только как хардкод для одного каталога. Блокер до эпика E2.
- (note) Карта покрытия CAP: CAP-1 закрыта до этого прогона (SPEC-sync помечает 0004-sync-contract.md приведённым в соответствие 2026-08-31), поэтому эпика на неё нет. CAP-9 (каденция запускает то, что называет) намеренно оставлена без AD — это дефект планировщика, инварианта не требует.
- (event) lint_spine.py: 5 low, все ложные — реальные шаблоны путей users/{uid}/devices/{token} и ключ курсора {область}:{id}. Структурных замечаний нет. Журнал предсказывал 4; пятое от лишнего вхождения того же пути.
- (event) Отрендерены ARCHITECTURE-SPINE.md (21 AD, диаграмма направления зависимостей, карта CAP-1..CAP-10, 9 Deferred, секция незакрытых дыр) и epic-breakdown.md (9 эпиков E1-E9, mermaid-граф порядка, что может уехать само, шов, два блокера до эпиков).
- (note) Разбивка явно называет, какой кусок разблокирует соседнюю спеку: E3 (приёмник и идемпотентность mutationId) поверх E2 (форма очереди) открывает spec-charges задачи 6 и 8.
- (event) Отрендерена наглядная страница (третий из трёх запрошенных артефактов) как HTML-артефакт: две стороны синхронизации, 21 AD по группам, девять эпиков с отметкой разблокирующего, три находки сверки, две дыры. URL https://claude.ai/code/artifact/9a7b1199-3a5d-4f2a-bfc3-9a6a02a7e4dd
- (event) spine finalized — status: final, updated 2026-08-31. Все три запрошенных артефакта на месте. Следующий шаг по BMAD: принять спайн компаньоном в spec-sync через bmad-spec (id AD стабильны, downstream может на них ссылаться), затем эпики.
- (event) ПРОИСШЕСТВИЕ: файл спайна был перезаписан во время работы гейта — правки после сверки со входами (AD про read-only поля, семантику удаления, конфликт версий, карантин, транзакционность, entity_ref, таблицу версий, операционный контур) пропали. Восстановлено переписыванием начисто из этого журнала с сохранением нумерации AD-1..AD-21 и добавлением AD-22..AD-31.
- (decision) AD-22..AD-31 добавлены по итогам гейта: карантин и неломающийся цикл; транзакционность локального изменения и записи очереди; конфликт версий отдельной ветвью; серверно-защищённые поля read-only; семантика удаления; отсутствие гарантий порядка с проверкой предусловия на сервере; терминальность карантина с передачей последствия фиче; серверный приёмник отдельным тестируемым модулем; принудительный ресинк; пагинация выборки.
- (decision) AD-3 переформулирован по замечанию проверки реальности: критерий — не «где вычисляется цена», а «зависит ли результат от состояния, которого у клиента нет». Прежняя формулировка относила разблокировку урока к синхронным (её цена считается на сервере из allocated seconds), противореча собственному списку. Следствие: формула цены разблокировки переезжает в shared/core.
- (decision) AD-1 уточнён по замечанию целостности данных: запись выполненного mutationId и эффект — одна транзакция; где не умещается — id резервируется до эффекта; срок хранения id строго больше максимального возраста записи очереди.
- (decision) AD-2 ужесточён по адверсариалу: ключ рождается вместе с намерением (значит и синхронный путь защищён), и идентифицирует неизменяемое содержимое — смена payload или expected_version порождает новую мутацию.
- (decision) AD-8 уточнён: точка слива — до вызова signInWithCredential; предупреждение есть согласие на удаление чужих записей; личные курсоры и локальные строки прежнего uid удаляются.
- (decision) AD-13 переформулирован через совместимость: послабление правил первым, ужесточение последним. Прежний порядок «правила, сервер, клиент» безопасен только для послаблений, а в объёме есть ужесточение — запрет прямой записи очереди в Firestore.
- (decision) AD-16 получил границу: пересоздание базы допустимо только при пустой очереди и опубликованных черновиках; данные без серверного двойника названы поимённо.
- (decision) AD-20 дополнен: firestore.rules сегодня не сопоставляют подколлекции users/{uid}/…, поэтому клиент не может записать токен — правка правил входит в объём.
- (event) Проверка гейта: обе линзы вернули «ОТКРЫЛИСЬ НОВЫЕ» — 4 незакрытых находки и 6 новых дыр от самих правок. Все закрыты.
- (decision) AD-27 переписан после самой тяжёлой находки: цепочка «разблокировал урок → прошёл его» в авиарежиме теряла прохождение. Очередь не даёт порядка, прохождение могло приехать раньше разблокировки, сервер отказывал, отказ шёл в карантин, карантин обязывал откатить локальное прохождение. Решение: сервер обязан отличать «ещё нет» от «нет» — введена ветвь ПредусловиеНеВыполнено, она повторяется, а не отвергается.
- (decision) AD-5 получил колонку state с закрытым набором из пяти значений (ЖДЁТ, ЖДЁТ_ПРЕДУСЛОВИЯ, КОНФЛИКТ, КАРАНТИН, СИНХРОННАЯ). Без неё состояние «ждёт решения по конфликту» было невыразимо, и три разработчика закодировали бы его тремя способами — через next_retry_at_ms=MAX, через переполнение счётчика попыток или никак.
- (decision) AD-5: operation и entity_ref живут в пространстве имён фичи (<фича>.<ИМЯ>), реестр отвергает занятое имя при старте. Две фичи с одинаковым operation давали молчаливую подмену обработчика, которую ядро не заметит, потому что payload не разбирает.
- (decision) AD-2: ключ синхронной мутации хранится в той же таблице со state=СИНХРОННАЯ и переживает смерть процесса. Прежде было сказано, что синхронный путь тоже защищён ключом, но не сказано, где ключ живёт между попытками.
- (decision) AD-22: возрастное условие перенесено в правило выборки. Прежде AD-1 объявлял карантин по возрасту, а единственное правило выборки о возрасте молчало — у проверки не было владельца, и запись, пережившая срок хранения mutationId, ушла бы на сервер как новая операция.
- (decision) AD-31: выбран курсор-пара (changedAtMs, docId), перекрытие окна отвергнуто с основанием. Прежняя формулировка перечисляла два механизма с разной формой значения — четыре курсора делали бы разные люди и получили бы два несовместимых формата в одной таблице.
- (decision) AD-11: назначена судьба уже записанного — журналы приводятся к инвариантам backfill-скриптом ДО снятия защитного fallback у читателя. AD-18 уточнён: запрет на удаление касается содержательных коллекций, журнал изменений производный.
- (decision) AD-30: ресинк лечит только сторону чтения; локальные эффекты живых записей очереди не переигрываются, а расхождение из-за карантина — забота AD-28, не ресинка.
- (decision) AD-14: числа отдаются по каждому значению state отдельно, иначе одна цифра «ожидает» значит у разных фич разное.
- (decision) Диаграмма зависимостей: убраны рёбра с подписью «НЕ зависит» — в графе зависимостей это рёбра ровно тех зависимостей, которые AD-7 запрещает, отрицание жило только в подписи.
- (decision) SPEC constraint про отсутствие принудительной потери данных уточнён: карантин — единственное исключение, и оно всегда видимо игроку.
- (event) Спайн финализирован: 31 AD.
- (event) Критик покрытия историй нашёл 4 обратные межэпичные зависимости — эпик требовал того, что делает более поздний. Разбивка пересобрана.
- (decision) Эпики 4 и 5 меняются местами: видимость состояния синхронизации идёт ПЕРЕД переездом новых действий в очередь. Основание: три истории про новые действия требовали, чтобы последствие карантина было видно игроку, а поверхность показа рождалась только в следующем эпике.
- (decision) Карантин по возрасту переезжает из эпика очереди в эпик идемпотентности: предельный возраст — общий параметр клиента и сервера (NFR9), а серверная сторона рождается только там. Эпик очереди получает карантин только по числу отказов и остаётся самостоятельным.
- (decision) Послабление firestore.rules (снять требование contentsVersion == 0 при create) и удаление колонки contentsVersion с клиента переезжают в эпик очереди — туда, где происходит единственное пересоздание схемы по AD-16. На сервере поле перестаёт писаться в эпике версий. Порядок AD-13 соблюдён: правила первыми.
- (decision) Форма серверной пометки «предусловие ещё не доехало» задаётся в эпике идемпотентности, где рождается серверный модуль, а не в эпике новых действий. Эпик ошибок объявляет только ветвь и правило маппинга.
- (decision) AD-28 уточнён владельцем: последствие карантина одно для всех мутаций — откат локального изменения, видимый игроку. Вариант «оставить и пометить» отвергнут.
- (assumption) РИСК ПРИНЯТ ВЛАДЕЛЬЦЕМ ЯВНО: игрок, прошедший урок в самолёте, теряет прохождение при устойчивом отказе сервера. Смягчение — система резервных копий, которой сегодня не существует; до её появления откат необратим.
- (decision) Числа решены владельцем 2026-09-01: порог карантина 5 отказов; backoff 1с→2с→4с с потолком 1 час; предельный возраст записи очереди 7 дней; срок хранения выполненных mutationId 30 дней; таймаут callable 20 секунд; размер страницы выборки 200; минимальный интервал между прогонами 30 секунд.
- (question) Отложено до появления системы резервных копий: можно ли смягчить необратимость отката карантинной записи.
- (decision) AD-16 (одно пересоздание локальной схемы) отменён реальностью: база пошла настоящими миграциями до версии 5 с экспортированными схемами 1..5.json. Это строже и лучше — история миграций перестала быть фиктивной без потери данных. Истории 2.5 и 2.6 отменены, их живой остаток (снятие колонок contentsVersion и сведение трёх очередей к одной) требует собственных миграций. NFR11 подлежит пересмотру вместе с AD-16.
