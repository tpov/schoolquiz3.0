# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed

# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created, ready for development
#   - in-progress: Developer actively working on implementation
#   - review: Implementation complete, ready for review
#   - done: Story completed

# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed

# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed

# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when its first story starts (via build's sprint sync)
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates the next story after the previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; the status view surfaces open ones
generated: 09-01-2026 21:40
last_updated: 09-01-2026 21:40
project: schoolquiz3.0
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/specs/spec-charges/stories.yaml
development_status:
  # Одна «эпика» на спеку: истории в stories.yaml не сгруппированы, а порядок между ними
  # задаётся зависимостями по смыслу, не нумерацией.
  epic-charges: in-progress
  1-переименование-сердец-в-заряды: backlog  # 46 файлов, включая persistence — ждёт чистого дерева
  2-настройки-экономики-переезжают-в-серверный-документ: in-progress
  3-плазма-становится-восстановимой-и-покупаемой: done
  4-сервер-решает-какого-вида-была-попытка: done
  5-прейскурант-определяет-плату: done  # для трёх играемых видов; контрольная и экзамен ждут своих сессий
  6-резерв-платы-в-начале-возврат-если-попытка-не-доехала: in-progress  # клиент умеет назвать вид и цену; резерв и возврат — нет
  7-расчёт-заявок-на-заряды-при-отправке: in-progress
  8-офлайн-баланс-стоит-на-нуле-плазма-ждёт-связи: in-progress
  9-обнаружение-перерасхода-и-запись-с-обоснованием: in-progress
  10-коробки-копятся-офлайн-открываются-только-онлайн: in-progress
  11-турниры-становятся-серверными-сессиями: in-progress  # CAP-10 (время как разрыв ничьей) есть; серверная сессия — нет
  12-периодическая-чистка-подделываемых-балансов: in-progress  # книга движений есть; сама периодическая функция — нет
  epic-charges-retrospective: optional

# Что уже сделано по 2 и 5 (b7245eb4, f64df10b, 167122a0, fe17a6c6):
#   - чистое доменное ядро: ChargeKind, ActivityKind, EconomyConstants, ChargeRules,
#     восстановление и трата в очках (100 очков = заряд);
#   - серверный модуль functions/economy-constants.js: чтение документа с вырождением
#     по полям в начальные значения;
#   - config/economy-constants.json — один источник начальных значений для обеих сторон,
#     сверяется тестами с двух сторон;
#   - доставка: вызов getEconomyConstants (клиент шлёт свою версию, сервер отвечает
#     «то же самое» при совпадении), шаг синхронизации, хранение в настройках приложения;
#   - понижение потолка больше не конфискует — ни на клиенте, ни на сервере.
#
# Что сделано по 4 и 5 (следующий коммит): functions/activity-kind.js выводит вид попытки из
#   полок квеста, прочитанных сервером (quests/{id} или приватный путь); списание идёт по цене
#   вида из серверной таблицы, а не по LESSON_ATTEMPT_LIFE_COST. Объявление клиента остаётся в
#   событии как declaredSourceShelf; турнирная маршрутизация и грязные оценки читают выведенную
#   полку. Квест, которого нет, — неизвестный вид, и он тарифицируется по самой дорогой ставке.
#   Контрольная и экзамен этим путём не приходят (свои сессии в spec-theme-exams) — их разрешение
#   встанет в activity-kind.js, маркер там же.
#
# Хвосты после 4/5:
#   - applyQuestRatingEvents по-прежнему берёт sourceShelf клиента для маршрутизации грязных
#     оценок. Цену это не трогает, но доверие то же; снять вместе с историей 11 (турниры-сессии).
#   - lessonAttemptLifeCost в ответе профиля всё ещё отдаёт плоскую константу; на клиенте его
#     никто не читает. Убрать при переименовании (история 1).
#
# Чистые ядра историй 7, 8 и 9 (следующий коммит):
#   - маска заявок ChargeClaimMask (shared/core/scoring) + functions/charge-claims.js: проверка
#     (длина, алфавит .SP, сложность, «своя девятка под пропуском»), расчёт в порядке вопросов,
#     поднятие до 9 только оплаченных пропусков; общий набор config/charge-claim-fixtures.json;
#   - OfflineChargeLedger (economy domain): available = слово сервера − неучтённое, плазма
#     отказана с причиной WAITING_FOR_SETTLEMENT при любой неучтённой заявке;
#   - overspendVerdict + overspendRecord: потолок = min(maxOwned, было + восстановилось) + допуск
#     на часы; запись с обоснованием внутри; путь audit/charge_overspend/records.
# Ревью ядер (workflow review-charges-cores, 2026-09-02 ~00:30): из пяти линз отработали две
#   (spec, edge), проверка находок не запустилась — лимит сабагентов. Разобрано вручную.
#   Починено: учёт заявок стал по-попыточным (три пропуска за забег разрешены) и аккаунтным
#   (любая неучтённая заявка запирает плазму — CAP-8 говорит об аккаунте); потолок восстановления
#   и перерасхода — по купленным слотам, не по maxOwned; перерасход считается в очках (порознь
#   округлённые полтора и шесть десятых давали ложный перерасход); nonNegativeInt больше не
#   принимает null/""/false за ноль (нулевой потолок запирал бы аккаунт); сохранённая таблица
#   без версии получает 1 и побеждает копию; кривой questId → «квеста нет», а не падение пакета;
#   порядок ошибок маски и латинский суффикс перевода выровнены с сервером (две новые фикстуры);
#   ответ не-словарь — неудача, а не «то же самое»; toInt без заворота.
#   Осознанно оставлено: SKIP_ON_ANSWERED роняет пакет; max(held, …) в потолке; квест на
#   нескольких полках — по самой дорогой; отсутствующий квест — по самой дорогой ставке.
#   Не сделано: множитель премиального восстановления (ограничение спеки) — к истории 3;
#   getEconomyConstants отдаёт таблицу целиком, а соседняя сессия ввела goldPacks и белый список
#   clientEconomyConstants — свести, когда их правка index.js сядет.
# Что не сделано (ждёт functions/index.js и firestore.rules — заняты другой сессией):
#   - проводка в applyLessonResultEvents: маска в payload и в normalize, расчёт в той же
#     транзакции, scoreVerified против поднятой строки, запись перерасхода;
#   - правило `match /audit/{document=**} { allow read, write: if false; }` — admin/ читают
#     рецензенты, а запись о перерасходе клиентам не показывается;
#   - клиент: маска собирается в раннере (сегодня подсказка лишь уменьшает livesRemainingHearts и
#     на сервер не уезжает), едет в payload SUBMIT_ATTEMPT, OfflineChargeLedger ведёт счётчик
#     между синхронизациями и заменяет readLivesFromProfile.
#
# Витрина (история 2, клиентская половина «одного источника», следующий коммит): цены слотов и
#   потолки берутся из EconomyConstantsRepository.current(), зашитая лестница и GOLD_HEART_COST
#   сняты; маппер economy/data больше не зажимает сердца потолком сборки (понижение потолка не
#   конфискует и на клиенте). MaxStandardHearts/MaxGoldHearts остались как стартовый бак.
#
# Открытый вопрос 4 спеки (история 12, сверка подделываемых балансов) — ОТВЕТ: истории для
#   пересчёта нет. writeUserProgressDelta пишет только инкременты в users/{uid}; событие попытки
#   хранит вход награды (codeAnswer, percent, difficulty), но не сумму; награда коробки — случайный
#   бросок (500..2000 ноликов, золото, логотипы), нигде не записанный; покупки слотов не
#   журналируются. Пересчитать нолики из истории нельзя — сравнение было бы выдумкой.
#   Следствие: история 12 начинается с журнала начислений (append-only ledger/{uid}/entries,
#   пишется в той же транзакции каждым путём, что трогает нолики/skill/заряды), и только потом
#   sweep. Это server-side (index.js) — ждёт освобождения файла.
#
# Коробки (история 10, чистое ядро, следующий коммит): functions/gift-boxes.js — серия по
#   прошедшим дням (пропущенный день обрывает серию: «подряд» значит подряд; прежний код серию не
#   рвал никогда и за визит после пяти дней отсутствия давал один день), коробка с десятого дня по
#   одной в день, и проверка заявки устройства при синхронизации: серия не длиннее «было + прошло
#   суток», коробок не больше дней серии от десятого и не больше одной в день; излишек не
#   начисляется и пишется с обоснованием. Без единого слова сервера принимается один день.
#   Клиентское зеркало серии — GiftBoxStreak в economy domain (не в profile: коробки — экономика),
#   пиннится общим набором config/gift-box-fixtures.json.
#   Не сделано: проводка в openGiftBox (index.js занят), мутация заявки при синхронизации
#   (economy.CLAIM_BOXES через очередь — тоже index.js), клиентское накопление серии офлайн
#   (profile domain/data заняты другой сессией).
#
# Ревью ядер, волна 2 (линзы fraud/parity/seam; проверка отработала частично — лимит):
#   Подтверждено и починено: цена и награда шли от двух не связанных полей payload — теперь квест
#   выводится ИЗ УРОКА (lessons → themes → sections → quests; приватный урок — по своему пути;
#   объявить private больше не значит купить самую дешёвую цену); покупка слотов на сервере — по
#   лестницам и потолкам из таблицы (витрина показывала 1 золота, сервер списывал 10 — закрыто);
#   восстановление в долге — темп и потолок из таблицы; повтор попытки не переписывает lifeCharged;
#   ступень лестницы null/"" — не бесплатный слот; документ без версии получает время записи;
#   клиентский разбор: испорченная ступень отменяет лестницу целиком, дробь округляется вниз.
#   Не починено (в реестре): всё-девятки без ключей ответов — закрывает attempt-scoring соседней
#   сессии; lessonAttemptLifeCost в профиле; витрина не перечитывает таблицу, пока открыта.
#
# История 3, частично (следующий коммит): покупка плазмы идёт по лестнице 1/2/3 и потолку 3 из
#   таблицы (сервер списывал 10 и запирал на одном); ответ на покупку больше не зажимается
#   потолками сборки; период восстановления принимает ручку премиума `premiumRegenDivisor` — она
#   есть у обоих видов, стоит на единице и живёт в таблице, потому что спека запрещает периоду
#   быть голой константой. Плазма получила свой счётчик очков и свою отметку времени: слоты
#   (`goldHearts`) — вместимость, `plasmaPoints` — заряд в них, как у обычного заряда; купленный
#   слот приходит заряженным, потому что продавать сутки ожидания за золото нельзя.
#
# «Один источник» закрыт: `lessonAttemptLifeCost` больше не отдаётся профилем (плоской цены
#   попытки нет), `UserProfile.canAffordLessonAttempt` заменён на `canAfford(pricePoints)`,
#   зашитые STANDARD_HEART_SLOT_COSTS и GOLD_HEART_COST удалены с сервера.
#
# История 6, первый шаг (следующий коммит): клиент называет вид активности теми же правилами,
#   что сервер (`ActivityKindRule` — зеркало `activity-kind.js`, общий набор
#   `config/activity-kind-fixtures.json`). Это то, из чего берётся цена до запуска: показать
#   «экзамен стоит 3 заряда» и не дать начать то, на что не хватает. Сам резерв и возврат — нет.
#
# История 7, клиентская половина (следующий коммит): заявка живёт в маске на `RunnerState.Ready`,
#   ставится `claimCharge` по сложности попытки (обычный заряд — подсказка на лёгком, плазменный —
#   пропуск сложного) и едет в payload строки очереди ключом `chargeClaims`. Колонка в базе не
#   понадобилась: строка очереди пишется в одной транзакции с попыткой, значит заявка живёт ровно
#   столько же. `persistence` трогать не пришлось.
#   Сложная попытка пока заявок не ставит: сегодняшнее управление показывает ответ, а плазменная
#   заявка означает «пропущено и засчитано верным» — заявить её значило бы соврать серверу.
# История 7 и 9, серверная половина (следующий коммит): `normalizeLessonResultAttemptEvent` читает
#   маску и отвергает испорченную целиком (это не перерасход, а искажённый payload);
#   `applyLessonResultEvents` считает заявки в той же транзакции, что и плату за попытку, платит в
#   порядке, в котором вопросы задавались, поднимает до `9` только оплаченные пропуски и сохраняет
#   послерасчётные `codeAnswer`/`percentScore`; плазма списывается из своего бака. Перерасход
#   считается по окну пакета и пишется в `audit/charge_overspend/records` с обоснованием внутри.
#   `scoreVerified` остался сверкой присланного с присланным: сравнивать с поднятой строкой
#   значило бы лишить награды за всю попытку того, кому не хватило заряда, а излишек и так не
#   оплачен — наказывать дважды спека запрещает.
#   Правило `match /audit/{document=**}` закрыто для всех, включая разработчика и самого
#   обвинённого, и это проверено в scripts/rules-emulator-test.js.
#   Осталось по 7: управление пропуском в UI — сегодняшнее показывает ответ, а плазменная заявка
#   означает «пропущено и засчитано верным».
#
# История 11, половина CAP-10 (следующий коммит): время отвечания складывается из длительностей
#   самих ответов и едет в результат турнира; в сравнении таблицы стоит ниже всего содержательного
#   — ниже процента, числа групп и числа соперников, — то есть решает ровно ничью и ничего больше.
#   Прогон без замера не объявляется мгновенным: у него время неизвестно (бесконечность), и он
#   стоит позади измеренных.
#
# ОТЛОЖЕНО по 11 — серверная сессия турнира. `spec-theme-exams/exam-protocol.md` уже описывает всю
#   машинерию (жизненный цикл, дедлайны на сервере, ответы принимаются но не оцениваются,
#   античит-запись), но в коде её нет: это спека, а не реализация. Строить её ради турниров значит
#   построить общую машинерию сессий целиком и вторым заходом переиспользовать для экзаменов —
#   правильный порядок обратный. Ждёт того, кто возьмёт spec-theme-exams.
#
# Ревью волны 3 (5 линз, 146 агентов, 23 подтверждено из 47). Починено этим коммитом:
#   - время в турнирной таблице было мёртвым: `normalizeTournamentResult` роняло `elapsedMs` до
#     таблицы, и все прогоны считались неизмеренными. Мой же тест зеленел по алфавиту — имена
#     теперь подобраны против него;
#   - `regenMs: 0` на сервере значило «раз в 36 секунд» вместо «не восстанавливается» (`|| ` съедал
#     ноль), а клиент в том же случае не начислял ничего;
#   - потолок окна перерасхода считался по `maxOwned`, а не по купленным слотам, и по базовому
#     периоду — премиум записывался в перерасход за заряды, которые сервер сам ему начислил;
#   - расчёт заявок поднимал только сохранённую строку: награда, лучший результат, рейтинги и
#     турнирная запись считались по неподнятому проценту;
#   - один `attemptId` дважды в одном пакете оплачивался, рассчитывался и награждался дважды —
#     снимки читаются до первой записи, поэтому обе копии выглядели новыми;
#   - полка с BOM была турниром для сервера и обычным уроком для клиента (JS `trim` режет U+FEFF,
#     `Char.isWhitespace` — нет): 500 очков против 33.
#
# Блокер закрыт следующим коммитом: приёмник переключён на `readSubmittedAttempt` соседней сессии
#   (он сверяет список показанного с цифрами), плата идёт по `isPayable`, и сверх того сервер
#   проверяет ЧИСЛО показанных против набора урока — без этого строка из одной цифры со списком на
#   один вопрос согласована сама с собой, а награда считается по уроку целиком. Попытка без списка
#   показанного сохраняется, но не оплачивается: живых установок нет (AD-16), а нынешний клиент
#   список шлёт всегда.
#
# Подтверждено, но НЕ моё — передано в реестр отложенного: длина `codeAnswer` не сверяется с
#   уроком (одна цифра покупает стопроцентный прогон; противоядие `attempt-intake.js` соседней
#   сессии не подключено); таблица лидеров сортирует рейтинги из несвязанных компонент вместе;
#   `completedAtMs` без ограничения выбирает турнирную группу; `hasPremium` пишется липкой
#   правдой; клиент перечитывает заряды из профиля на каждой попытке (это и есть заблокированная
#   история 8).
#
# ЗАБЛОКИРОВАНО по 8: показать `слово сервера − неучтённое` негде — счётчик неучтённых заявок
#   живёт в очереди, а `shared/core/outbox`, `shared/core/persistence`, обе презентации раннера и
#   магазина держит соседняя сессия (пометка entity_ref, AD-14). Ядро `OfflineChargeLedger` готово
#   и ждёт только источника числа.
#
# Что осталось по 2 — «один источник»: functions/index.js и UserProfile.kt всё ещё держат
#   свои копии потолков и цен (MAX_STANDARD_HEARTS, GOLD_HEART_COST, MaxStandardHearts).
#   Снимать их в одиночку нельзя: сервер начнёт считать по 10 и 3, а клиент показывать 5 и 1.
#   Делается вместе с историей 1 (переименование) и 3 (плазма восстановимая).
