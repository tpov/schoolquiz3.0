---
topic: Theme exams in home quests — online, server-authoritative, theme stars and certificates
updated: 2026-08-31T11:54
---

- (direction) Source: Tpov brain dump 2026-08-29 (voice-transcribed RU). Home quests get exams; distilled in express mode — session non-interactive, so gaps became open questions rather than elicitation.
- (decision) Exam unit is the THEME, not the lesson and not the whole quest: 'random question from all lessons of that theme'.
- (decision) Exam is ONLINE. User weighed offline vs online in the dump ('Или нет, подойди... Да, давай пусть будет онлайн') and settled on online: client asks server, server issues questions, server scores.
- (decision) Server never returns the correct answer — during the exam or after it. Only the final result comes back.
- (decision) Server holds the per-question deadline ('и ждёт время'), not the client.
- (decision) Theme stars come from the exam alone, never from averaging lesson stars.
- (decision) Theme keeps best exam result and average exam result ('максимальный балл, средний балл за экзамен').
- (decision) Exam inherits the lesson difficulty ladder: an EASY exam draws only EASY questions, a HARD exam only HARD ones — 'логика такая как обычного урока'.
- (decision) Passing the exam writes a certificate record owned by the user ('записывает в папочку отдельные сертификаты, ну или у юзера').
- (constraint) Exam entry is gated on completing every lesson in the theme ('когда проходим все уроки, мы проходим экзамен').
- (note) GROUNDING SessionMode.EXAM already exists and is wired: shared/feature/lesson-runner/domain/.../model/SessionMode.kt — revealsCorrectAnswer() is false for both difficulties in EXAM, and computeTimer applies TimerCoefficients.examFactor=0.75.
- (note) GROUNDING Difficulty (EASY/HARD) lives per question in QuestionContent; StartLessonAttemptUseCase filters the pool by it and takes a random subset of POOL_SIZE=20 via selectSubset(seed).
- (note) GROUNDING computeStars maps percentScore to tenths by difficulty: EASY 0..20, HARD 20..30. Three stars = 30 tenths. Reusable unchanged for exam stars.
- (note) GROUNDING computeHardUnlocked: HARD unlocks only after an EASY attempt whose shown digits are all 9. Precedent for an exam ladder.
- (note) GROUNDING Lesson attempts are offline-first: Room + LessonResultOutboxWriter, uploaded later via functions/index.js submitLessonResultEvents, which re-derives percentScore from codeAnswer (functions/result-verification.js). The server never sees the questions.
- (note) GROUNDING Theme.kt carries no rating or star fields, and ThemeListScreen renders HierarchyItemCard with no stars. Both need extending.
- (note) GROUNDING ADR-0007 defines Certificate (server-signed, public verification URL, lives in profile domain) but no Certificate code exists in the repo yet.
- (note) GROUNDING ADR-0005 already reserves EXAM mode for courses and CompletionEffect.IssueCourseCertificate; its 2026-04-21 amendment puts COURSE quests on visibleOn subset of home/archive — so 'home quests' is where courses already live.
- (capability) CAP-1 Exam unlocks on a theme once every lesson in it is completed; locked state says what is still missing.
- (capability) CAP-2 Starting an exam opens a server-held session; the client holds no question bank and no scoring.
- (capability) CAP-3 Server serves one redacted question at a time, drawn at random across all lessons of the theme, filtered to the exam difficulty.
- (capability) CAP-4 Server enforces the per-question deadline; a lapsed deadline is scored as a miss, same as the runner's timeout answer.
- (capability) CAP-5 Answers are accepted without feedback — no correct/incorrect signal until the exam ends.
- (capability) CAP-6 Server scores the finished exam and returns the result; the client only renders it.
- (capability) CAP-7 Theme stars are derived from exam results only, keeping the best and the average.
- (capability) CAP-8 A passed exam issues a certificate the user owns and can see.
- (capability) CAP-9 Exam difficulty ladder mirrors the lesson ladder: HARD exam unlocks only after the EASY exam is passed.
- (capability) CAP-10 Losing connectivity mid-exam ends it as a server-side outcome, not a silent client failure.
- (constraint) Exam requires connectivity; there is no offline exam path — this deliberately breaks the app's offline-first default and is the reason the exam is a new server-owned flow rather than a SessionMode flag on the existing runner.
- (constraint) Client is never the scorer. It cannot see correct answers in the exam payload, cannot compute the exam percentScore, and cannot write its own certificate (ADR-0005 rule 4, ADR-0007 rule 1).
- (constraint) Certificates are server-signed and server-issued only; the client never creates one.
- (constraint) Existing per-lesson stars, lesson averageRating and the LEARNING runner keep working unchanged; the exam adds a parallel axis, it does not replace them.
- (constraint) Star arithmetic reuses computeStars: an EASY exam can reach 2 stars, only a HARD exam reaches 3. No second star formula.
- (assumption) Assumed 'домашние кости' in the transcript is 'домашние квесты' — the home shelf, where COURSE quests already live per ADR-0005.
- (assumption) Assumed 'lesson completed' means the lesson has at least one attempt whose stars are above zero, since that is the only completion signal the codebase has today.
- (assumption) Assumed exam length mirrors the lesson pool (about 20 questions) rather than the whole theme, since a theme can hold hundreds of questions.
- (assumption) Assumed a certificate is issued per theme exam, extending ADR-0007's per-course model rather than replacing it.
- (question) Certificate granularity contradicts ADR-0007: the ADR issues one certificate per COURSE quest, the dump asks for one per theme exam. Amend the ADR to theme-level, keep course-level and treat theme exams as prerequisites, or issue both?
- (question) The client already caches every question payload locally, including correct answers, so serving redacted questions online does not by itself stop lookup during an exam. Do exam themes ship as on-demand content (Quest.archived path), is local cheating accepted for now, or does something else close it?
- (question) What is the pass threshold that issues a certificate, and is a failed exam still recorded in the theme average?
- (question) Are retakes allowed, and if so is there a cooldown, an attempt cap, or a cost in life points (lesson attempts already cost LESSON_ATTEMPT_LIFE_COST)?
- (question) Does the theme average count every exam attempt or only the best per difficulty?
- (question) Do exam results feed the existing reward and activity-rating pipeline in submitLessonResultEvents, or do they stay outside skill points and nolics?
- (event) Self-validate pass 1 (coherence): PASS. 10 capabilities each carry intent+success; 6 constraints each rule something out; 6 non-goals; success signal is demonstrable. CAP-2/CAP-3 name server ownership, which reads as HOW but is the load-bearing product decision the user made explicitly — mechanism detail pushed to exam-protocol.md.
- (event) Self-validate pass 2 (preservation): PASS. Every load-bearing claim in the dump landed — theme-scope exam, all-lessons gate, online/server draw, server-held timer, no reveal, server-computed result, certificate per exam, theme stars from exams with best and average, easy-then-hard ladder. Wrapper-only content dropped: the thinking-aloud about offline vs online, kept as a direction entry.
- (event) Spec rendered: SPEC.md + exam-protocol.md + brownfield.md, adopting ADR-0003/0005/0007 as companions. 4 assumptions, 6 open questions outstanding.
- (note) Story Breakdown not offered: session is non-interactive, and the schema requires per-story checkpoints from the user.
- (direction) Tpov round 2, 2026-08-29: answered open questions 1,3,4,5,6 and added two new requirements (exam anti-cheat, sequential lesson unlock in courses).
- (decision) SUPERSEDES the per-theme certificate assumption: there is exactly ONE certificate, issued per course, only after the last section of the course is completed. ADR-0007's per-course model stands unamended. Theme exams issue no certificate.
- (decision) Pass threshold: 100% of the EASY exam plus a share of the HARD exam. Share set to 50% — 20% was the alternative but sits below the ~25% a random guess scores on four-option SingleChoice, so it would pass players who know nothing.
- (note) GROUNDING star arithmetic for the threshold, computed from computeStars: 100% easy alone = 2.0 stars (66.7% of the 3-star scale); +20% hard = 2.2 (73.3%); +40% hard = 2.4 (80.0%); +50% hard = 2.5 (83.3%); +100% hard = 3.0. Tpov's remembered '73 or 80 percent' are exactly the 20% and 40% hard shares.
- (decision) Retakes: one exam attempt per theme per calendar day. Date-based, not a rolling 24h window — fail today, retry tomorrow.
- (decision) A session the server expires by timeout still consumes that day's attempt; otherwise abandoning a losing exam would be free.
- (decision) SUPERSEDES the theme-average decision: the theme shows the MAX exam result only, mirroring how lessons show best stars. No average is stored or displayed.
- (decision) The economy — nolics, experience and charge (заряд, formerly life points) — stays offline-first and unchanged in kind. An exam additionally costs whole charges.
- (decision) Exam charge cost set to 1 whole charge (100 points, vs 33 for a lesson attempt). 2 was the alternative; the once-a-day limit already does the real gating, so the smaller cost is enough.
- (decision) NEW REQUIREMENT: lessons in a COURSE catalog unlock sequentially — the next lesson is locked until the previous one is passed, or until it is bought with nolics.
- (decision) NEW REQUIREMENT: a lesson is passed when every EASY question in it is answered correctly. This is the same all-9 condition computeHardUnlocked already uses, and it resolves the earlier open question about what 'lesson completed' means.
- (decision) NEW REQUIREMENT: a server-side exam anti-cheat records per-question timing and the player's answer pattern. No blocking rule yet — it marks statistics now, and heuristics filter later.
- (constraint) The anti-cheat never blocks or voids an exam in this iteration; it only records. A rule that rejects results needs its own decision once there is data to set it from.
- (constraint) Charge cost and the daily limit are enforced server-side for exams, unlike lesson attempts which are charged on sync — the exam is online, so there is no reason to defer it.
- (assumption) Assumed course completion chains upward: a theme is done when its exam is passed, a section when all its themes are done, the course when all its sections are done — and that last event is what issues the certificate.
- (assumption) Assumed there is no separate section-level or course-level exam; the certificate falls out of the last theme exam of the last section.
- (assumption) Assumed the daily retake limit is per theme and per difficulty ladder position, not one exam per day across the whole app.
- (question) Does a lesson unlocked by paying nolics count as passed for the exam gate? Buying the next lesson is not the same as having answered the previous one, and the exam gate is defined on passing.
- (question) Which calendar day — the device's, or UTC on the server? A device-local day is exploitable by changing the timezone; a UTC day means the reset lands mid-evening for some players.
- (question) Renaming life points to 'заряд'/energy touches the shop, the profile, the server economy and the legacy price list. Does it ride along with exams, or ship as its own change?
- (question) Does the EASY exam have its own pass mark, or is it only meaningful as the 100% prerequisite inside the theme pass rule?
- (event) Spec re-derived after round 2: 14 capabilities (CAP-11..14 added), 9 constraints, 10 non-goals, 5 assumptions, 5 open questions. Companions updated with the pass-rule table, cost/rate, anti-cheat record, sequential-unlock gap and the charge economy constants.
- (event) Self-validate pass 1 (coherence): PASS. All 14 capabilities carry intent+success; every constraint rules something out; CAP-7 and CAP-8 amended in place rather than renumbered, IDs stable.
- (event) Self-validate pass 2 (preservation): PASS. Round-2 claims all landed — single per-course certificate after the last section, 100%-easy-plus-half-hard pass rule, daily retake by date, timeout consumes the day, theme shows max only, charge cost, offline economy unchanged, sequential lesson unlock with nolic purchase, recording-only anti-cheat. Wrapper-only content dropped: the 50-or-20 and 1-or-2 deliberations, resolved into decisions with reasons.
- (direction) Tpov round 3, 2026-08-29: resolved the calendar-day and bought-lesson questions, and replaced the exam gate predicate.
- (decision) SUPERSEDES the calendar-day retake: the cooldown is a rolling 23 hours, not a date. Sidesteps the timezone question entirely, and 23 rather than 24 lets a player keep sitting exams at the same hour without the slot drifting later each day.
- (decision) Cooldown runs from the START of the previous sitting, so how the session ended cannot move it.
- (decision) Answer to the nolics question: NO. Buying a lesson only opens access to it; it is not a pass and earns no stars.
- (decision) SUPERSEDES the exam gate predicate: the exam unlocks when every lesson in the theme stands at 2 stars or better, not when every lesson is 'passed'. A bought lesson has zero stars until it is actually played, which is exactly what keeps the exam from being bought open.
- (note) GROUNDING 2 stars = 20 tenths. computeStars EASY reaches 20 tenths at 98% and above, so the star gate is a hair looser than the all-9 unlock rule but effectively the same bar.
- (note) GROUNDING EDGE CASE: computeStars HARD starts at 20 tenths even for 0% — any HARD attempt satisfies a bare 'bestStars >= 2' test. Harmless while HARD is reachable only through an all-9 EASY attempt, but the implementation must not read the gate as 'any attempt scoring 2 stars' without that context.
- (assumption) Read 'суммарно пройти все уроки на две звезды' as every lesson individually at 2 stars, not an average of 2 across the theme.
- (question) Two near-identical thresholds now coexist: all-easy-correct opens the next lesson, 2 stars opens the exam. They differ only between 98% and 100%. Collapse them into one predicate, or keep both deliberately?
- (event) Spec re-derived after round 3: CAP-1 gate restated on two stars per lesson, CAP-12 moved to a rolling 23h cooldown, CAP-11 clarified that buying earns no stars. Two constraints added, open questions down to four. Self-validate both passes: PASS.
- (direction) Tpov round 4, 2026-08-29: purchase scope widened, exam confirmed unbuyable, and the two thresholds collapsed into one.
- (decision) SUPERSEDES the next-lesson-only purchase: nolics buy ANY locked lesson in a course, not just the following one. Sequential order governs free progression only — to reach a lesson without paying, the previous one must be passed.
- (decision) The exam itself can never be bought. Nolics buy lessons; the exam has one key and it is earned.
- (decision) RESOLVES the two-threshold question: there is ONE predicate — every easy question in the lesson answered correctly, the all-9 test computeHardUnlocked already implements. 'Two stars' is how that predicate is shown to the player, not a second rule. The 98-vs-100% gap was an artifact of integer rounding in computeStars and disappears with the predicate stated on the answers rather than on the star value.
- (note) Collateral benefit: stating the gate on an all-9 EASY attempt also removes the HARD floor edge case, since a 0% HARD attempt cannot satisfy it. The brownfield warning about bare bestStars >= 20 is now moot and rewritten as the reason the predicate is phrased this way.
- (direction) Tpov: once the spec is settled, convene all the personas to assess the project — bmad-party-mode.
- (event) Spec re-derived after round 4: single pass predicate, purchase widened to any locked lesson, exam declared unbuyable. Open questions down to three. Self-validate both passes: PASS.
- (direction) Tpov round 5, 2026-08-29: answered the party's three questions — purchase UI, what a theme awards, and who owns charge.
- (decision) Purchase UI stays minimal: the locked lesson card shows the price and a padlock, nothing else. No copy explaining that buying grants access but not stars.
- (decision) A theme exam awards a result, rewards, and possibly a placement/rank; it never awards a certificate. The course still issues exactly one certificate at its end.
- (decision) SUPERSEDES server-enforced charge: charge is an OFFLINE, device-side cost, deducted by the client as soft motivation. Clearing app data resets it, and that is accepted — lessons are offline and the charge is not a security boundary.
- (constraint) The 23-hour cooldown stays server-side and is the real gate on exam frequency; charge is motivation only and gates nothing the server relies on.
- (note) GROUNDING existing leaderboard precedent for the placement idea: Lesson.top3 (List<TopParticipant>) already exists on the lesson model, server/workers/leaderboard and functions/tournament-ranking.js already compute rankings.
- (note) CONSEQUENCE flagged by the room: functions/index.js submitLessonResultEvents pays skill points and nolics only when lifeCharged is true — the successful server-side life-point charge is currently what gates every reward payout. Moving charge to the device removes that anchor.
- (question) If charge moves to the device, what gates the server's reward payout? Today lifeCharged does it; without it, submitLessonResultEvents pays for every accepted attempt, including replayed offline queues.
- (question) Is the theme placement a real leaderboard (who ranks against whom — theme, course, global?) or just the player's own best shown back to them?
- (risk) Sally's objection stands unresolved by decision, not by oversight: a player can buy every lesson in a theme and still find the exam locked, and the card that took their nolics said only a price and a padlock.
- (event) Spec re-derived after round 5: CAP-13 moved to device-side charge, CAP-15 added for theme payout, known-risks section added for the purchase trap. Five open questions. Self-validate both passes: PASS.
- (event) Round-table with five independent agents (analyst, PM, UX, architect, dev) reading the spec and the code. Findings below are theirs, verified against source.
- (note) CORRECTION to an earlier claim in this log: lifeCharged is NOT the replay anchor. Replay is stopped by isNew = !existingSnapshots[index].exists (functions/index.js:324) plus scoreVerified. lifeCharged is an economic throttle, and the two were conflated.
- (note) FINDING (dev): lifeCharged gates four things, not one — skillPoints/nolics, the radar activity ratings, and writeTournamentAttemptToTransaction (functions/index.js:346-356). Removing it silently changes tournament standings, not just currency.
- (note) FINDING (dev): lifePoints is server-owned in users/{uid}; FirebaseProfileRemoteDataSource.kt:75 reads it and ProfileRepositoryImpl.kt:64 does local.upsert(remoteProfile) wholesale. A device-side charge deduction is therefore overwritten by any profile refresh — the charge does not merely reset on data wipe, it evaporates on the next sync.
- (note) FINDING (dev): QuestionContent.Ordering has no separate answer field — the order of items IS the answer. Field-stripping cannot redact it; the server must shuffle items and hold the permutation itself, or CAP-5 breaks on the first ordering question.
- (note) FINDING (dev): Survey scores 9 for any valid click in evaluateAnswer. A theme containing surveys yields free nines, hence a free 100% on the easy exam. Surveys must be excluded from the exam draw, not merely filtered by difficulty.
- (note) FINDING (dev): correctOptionId cannot be made nullable for redaction — QuestionContent's init require() would throw and evaluateAnswer would fall through to Score(1), corrupting ordinary lessons. Redaction needs a separate wire type inside the exam feature.
- (note) FINDING (dev): the single-predicate claim is not yet true. computeHardUnlocked tests all-9 over the POOL_SIZE=20 subset (StartLessonAttemptUseCase.kt:119), while CAP-1 says every easy question of the lesson. For a lesson with more than 20 questions these differ, and the exam gate would sit on the weaker one.
- (note) FINDING (architect): replace the lifeCharged anchor with attemptId idempotency plus a daily cap on paid attempts per lesson — server-side, cheap, and it lets charge honestly become motivation rather than a gate.
- (note) FINDING (analyst): if nolics are bought with real money, the purchase trap stops being a feelings problem and becomes a refunds and store-policy problem.
- (note) FINDING (analyst): with proctoring in non-goals, the certificate link honestly attests that an account passed, not that a person did. That is the most expensive assumption stated as fact.
- (note) FINDING (UX): any pixel that changes after submit will be read as a verdict, so the acknowledgement must be deliberately uniform — same mark, same duration, no colour.
- (note) FINDING (UX): the exam lock screen must distinguish 'lesson not unlocked' from 'lesson unlocked but not passed', so a player who bought a theme sees work outstanding rather than an abstract refusal.
- (note) FINDING (UX): a failed result must route back into the theme's lessons rather than dead-end on a number, since the next attempt is 23 hours away with no explanation of what was missed.
- (note) FINDING (PM): the feature is built around the mechanism, not the job. The job is 'prove I learned this to someone outside', which the certificate serves; the exam is only its instrument, yet it holds the entire budget.
- (question) Are nolics purchasable with real money? The answer decides whether the purchase trap is a UX risk or a refunds and store-policy risk.
- (question) Ship order: theme exam first and certificate later (the seam the spec already draws), or John's inversion — certificate and public verification first, cheapest honest exam second?
- (risk) The device-side charge decision (round 5) is not implementable as stated: lifePoints is server-owned and overwritten on profile sync. Either charge stays server-side, or the profile sync stops clobbering it.
- (event) Spec re-derived after the round table: redaction constraints per question type added, single-predicate claim corrected, three known risks added, seven open questions. Self-validate both passes: PASS.
- (direction) Tpov round 6, 2026-08-29: answered the money question and revealed a course-level main exam that was not in the spec.
- (decision) RESOLVES the real-money question: nolics are an offline, largely motivational currency. Gold is the real-money one. The purchase trap is therefore a disappointment risk, not a refunds or store-policy risk.
- (decision) Purchasing a lesson exists to let a player take any lesson without first clearing the previous lesson's easy questions — it buys past the sequential gate, nothing else.
- (decision) NEW AND LOAD-BEARING: the certificate is issued after a MAIN EXAM, a separate course-level sitting distinct from the last lesson and from any theme exam. Completing the last section is not itself the issuing event.
- (note) This materially answers the PM's objection from the round table: the course now ends on a culminating event rather than on a silent completion, and the certificate has a moment attached to it.
- (question) What does the main exam draw from — every question of the course, or the theme exams' pools, or something narrower?
- (question) Does the main exam have the same easy-then-hard ladder, the same 23-hour cooldown, and the same charge cost as a theme exam, or its own?
- (question) What unlocks the main exam — every theme exam of every section passed, or a looser bar?
- (question) Can the main exam be re-sat after it is passed, and does a better result change anything once the certificate exists?
- (question) Does the course carry stars of its own from the main exam, the way a theme does from its exam?
- (event) Spec re-derived after round 6: CAP-16 main exam added, CAP-8 retargeted from section completion to passing the main exam, real-money risk retired. 16 capabilities, 8 open questions. Self-validate both passes: PASS.
- (note) DEFECT in the round-6 derive, caught by the PM and the analyst: CAP-16 was added while the non-goal 'No separate section-level or course-level exam sitting' was left standing, so the spec both mandated and forbade the main exam. Non-goal narrowed to section-level only.
- (question) Can gold — the real-money currency — touch the path to the certificate at any point: an attempt, a retake, an unlock, a cooldown skip? If yes the product sells the document; if no it belongs in constraints, first line.
- (question) Can the main exam be failed, and can it be retaken indefinitely? Unlimited retakes make the certificate a measure of persistence rather than knowledge; a hard limit invents a way to permanently refuse someone who completed the whole course.
- (note) FINDING (analyst): the certificate now hangs on one server event — main exam passed — rather than on the section/course completion aggregates that do not exist in code. Cheaper and more verifiable than the CompletionEffect cascade, and it moves the theme exam off the critical path to the document.
- (note) FINDING (analyst+PM): the Survey free-nines and the Ordering redaction problem escalate with the main exam — they now sit on the path to a document an employer opens, and the Room answer cache gets worse at course scale, not better.
- (note) FINDING (UX): a new course-screen state now exists — last section closed, every theme green, progress at 100%, and no certificate. It must read as a door marked 'main exam', not as a bug or a failed load.
- (note) FINDING (UX): a 23-hour cooldown is the right daily rhythm for a theme exam and reads as mockery on the main one after months of work. Either the main exam gets its own cooldown, announced in advance, or a failed main exam shows a route back — which themes sagged — rather than a silent timer.
- (note) FINDING (UX): the theme exam is now justified as a rehearsal — the player first lives through the silence, the timer and the not-knowing at cheap stakes, and arrives at the main exam already knowing the format. This is the answer to the PM's 'mechanism for its own sake' charge.
- (note) FINDING (UX): device-side charge is a trust bug, not only a server one — a price that flickers (deducted locally, restored by profile sync, or refused after deduction) destroys trust faster than an unfair price. Deduct in the same call that opens the session and show only what the server confirmed.
- (note) FINDING (UX+analyst tension): the theme exam moving off the critical path makes the certificate hang on a single sitting. One dropped connection and there is nothing to attest; one friend prompting alongside and the wrong person is attested.
- (note) FINDING (architect), significant: the main exam collapses the missing section/course aggregate from an event-driven emitter into a one-shot admission predicate the server computes once at startCourseExam and forgets. No versions, no cursor, no sync contract, nothing to go stale between sessions. CompletionEffect.IssueCourseCertificate then hangs on the main exam's terminal Scored state, which the protocol already describes.
- (note) FINDING (architect): main-exam questions should not be cached in Room at all — one on-demand content pool, uncached. Theme exams can keep the local cache and accept that answers are readable, since the cost of cheating there is theme stars. This closes the expensive hole surgically instead of reworking sync.
- (note) FINDING (architect), rebutting the PM's inversion: the certificate can no longer ship first, because it became the output of the main exam session rather than a standalone document — building the signature and public verification early yields a verifier for an empty set. The risk in this feature is the server session (redaction, deadlines, reconnect races, cooldown, retries), and that is debugged at low stakes on theme exams. What does decouple: the Certificate model and the public verification endpoint now depend on nothing and can run in parallel from day one.
- (note) FINDING (architect): charge for an exam should be deducted server-side in startThemeExam, since the exam is the one online flow; client-side charge stays a lessons problem and is not this feature's to solve.
- (question) What exactly admits a player to the main exam — every theme of the course passed, every section, or hard rungs too? The answer decides what the admission predicate reads.
- (note) FINDING (dev), verified: questionToDocument (functions/index.js:3235) writes only lessonId onto a question document — no themeId, sectionId or questId. collectionGroup('questions') therefore cannot be narrowed to a theme or a course, so a theme draw walks every lesson and a course draw walks the whole tree on every exam start, per player, inside the capped FUNCTION_OPTIONS. There is no firestore.indexes.json in the repo at all.
- (decision) The exam draw index is built at publication, not at exam start: buildPublicationDocuments (functions/index.js:2043-2070) already walks sections, themes, lessons and questions in one pass and holds theme.sectionId at the question loop, so the ancestry costs nothing there.
- (note) FINDING (dev), verified: attemptId idempotency is already in production — isNew at functions/index.js:324, and eventBucketId hashes the id alone precisely because including the client date once caused replayed attempts to be paid twice (comment at index.js:3891-3896). Only the daily cap on paid attempts remains to be built.
- (note) CORRECTION, verified: nolics are server-owned, not offline. pointsNolics is incremented by writeUserProgressDelta (index.js:2941) and spent server-side ('Not enough nolics', index.js:3707). Tpov's product point stands — nolics are the motivational currency and are not bought with real money — but the technical implication does not: a device-side lesson purchase would be erased by local.upsert(remoteProfile) exactly as the charge is. Lesson purchase must be a server call.
- (constraint) Lesson purchase with nolics is a server call, for the same reason the exam charge is: the balance lives in users/{uid} and profile sync overwrites the local copy wholesale.
- (note) FINDING (dev): questionToDocument syncs payload whole, correct answer included — confirming the Room-cache exposure at the document level, not just at the sync level.
- (event) Spec re-derived after Amelia's verified pass: draw-index constraint, server-owned nolics, corrected lifeCharged question, provisional gold constraint. Self-validate both passes: PASS.
- (direction) Tpov round 7, 2026-08-29: gold ruled out of the certificate path, main exam parameters set, admission predicate defined, retakes made unlimited.
- (decision) RESOLVES the gold question: gold buys premium boxes and NFTs, and drops from boxes. It does not interact with progression and never touches the path to the certificate. The provisional constraint becomes real.
- (decision) Main exam admission: every theme exam of every section passed at the theme pass rule — 100% of the easy exam plus 50% of the hard one. No separate hard-rung condition; the theme rule already carries it.
- (decision) Main exam retakes are unlimited.
- (note) CONSEQUENCE, accepted: with unlimited retakes the certificate attests that the holder passed once and persisted, not that they passed reliably. This is the analyst's dilemma resolved toward persistence, and it should be stated rather than discovered by a verifier.
- (note) UPSIDE of unlimited retakes: it defuses the UX objection that one dropped connection destroys months of work — a lost session costs a cooldown, not the certificate.
- (question) Main exam question mix: hard only, or easy and hard mixed? Tpov leaned toward asking rather than deciding. Recommendation on file: hard only, because computeStars maps HARD to the 2-3 star band that a certificate-worthy result lives in, mixing would need a second star formula the spec forbids, and every easy question was already proven to 100% at every theme exam just to be admitted.
- (note) AMBIGUITY to resolve — sync direction. Tpov describes sync as local-to-remote: results are written up to the server so an anti-cheat script can check them. That is true of attempts (the Room outbox feeding submitLessonResultEvents). It is not true of the profile: ProfileRepositoryImpl.kt:64 does local.upsert(remoteProfile), so the balance flows remote-to-local and overwrites. Both directions exist, for different data, and the device-side spending question turns entirely on the second one.
- (decision) A periodic server-side anti-cheat script is part of the design, consistent with CAP-14 recording rather than blocking.
- (event) Spec re-derived after round 7: gold constraint made real, CAP-16 given its admission rule and unlimited retakes, persistence risk recorded, sync-direction question raised. Self-validate both passes: PASS.
- (note) FINDING (PM), sharp: unlimited retakes with no per-question feedback ARE feedback, only slow. Answer blind, read the score, change one guess, sit again. The spec forbids showing a verdict per question and then hands out unlimited attempts — the same information, spread over iterations.
- (note) FINDING (PM): twenty hard questions from a course-wide pool, a 50% bar, blind, repeatable without limit — that is a loot box, not an exam. It gets rolled rather than passed.
- (note) FINDING (PM): three individually tolerable assumptions compound into a document that attests nothing — hard questions, unlimited attempts, and the answers sitting in the player's own Room cache.
- (note) FINDING (PM): he endorses hard-only but rejects the stated reason. The star formula is a consequence, not a basis; the basis is that easy questions are already proven to 100% in every theme, so repeating them dilutes the main exam and inflates the pass mark with guaranteed marks.
- (question) What is the price of the main exam, in one line? With retakes unlimited, charge evaporating at the next profile sync, and no attempt cap, it currently costs nothing but a cooldown — free, endless and blind, for the most valuable thing in the product.
- (question) Why unlimited rather than a small number with a real cooldown? If the intent is to protect a player who cracked under nerves, three attempts and a clear cooldown do that; unlimited protects something else.
- (note) DEFECT, caught by the analyst: CAP-13's success criterion said 'clearing app data resets the charge'. False — a local deduction is erased by the next profile sync, silently and with no wipe involved. CAP-13 rewritten to a server-side deduction taken in the same call that opens the session.
- (note) FINDING (analyst), reframing: with unlimited retakes the main exam stops being a gate and becomes a ceremony — by definition everyone who does not quit eventually passes. The real gate is admission: every theme exam of every section at 100% easy plus 50% hard. The anti-cheat and unforgeability budget is aimed at the final, while all the proof lives in the admission.
- (note) FINDING (analyst): the honest claim to a verifier is therefore 'this person closed every theme of the course and returned as many times as needed' — a certificate of completion, not of grade, in the Coursera sense. Not a shameful product, but the verification page must say that and the Why section currently promises something else.
- (note) FINDING (analyst): the only scarce resource on the path to the certificate is time — 23 hours per theme per rung. A twenty-theme course is twenty days at best, plus a day per miss. That is the certificate's real price and it should be named before release, and there is still no metric that could measure it.
- (note) FINDING (analyst), escalating the Survey hole: it does not sit in the final, it sits in the admission rule '100% of the easy exam'. While Survey hands out nines for free, the only real filter in the whole design is the soft one.
- (decision) Question mix for the main exam: hard only. Endorsed independently by the PM and the analyst on the same basis — easy questions are already proven to 100% per theme at admission, so including them re-asks what the design already counted and inflates the pass mark with guaranteed marks.
- (note) FINDING (architect), significant: the admission predicate collapses to one small record per (user, theme) — best easy percent, best hard percent, passed flag — written when a session reaches Scored. It is monotonic, because best never falls (that is CAP-7), and a monotonic update is idempotent, so it survives any replay and any delivery order. The theme screen already draws this record as stars; the aggregate is not new, it is finally named.
- (note) FINDING (architect): admission must compare a SET of passed theme ids against the course structure plus a structure version, never a count. An author adding a theme moves the denominator, and a counter would admit whoever slipped through before it appeared.
- (note) FINDING (architect): because computeStars floors HARD at two stars even at 0%, the certificate must read passed and percentScore only, never stars.
- (note) FINDING (architect), answering the PM's price question: unlimited retakes are ALREADY throttled by the server-side charge. 100 points a sitting, one heart an hour of regeneration, a 500-point ceiling — five sittings back to back, then one an hour. A real server throttle that life-points.js already implements; no new mechanism needed.
- (decision) Do not forbid the grind, record it: the certificate carries the session id, the percent and the date of the sitting that earned it, and the public verification page shows them. A persistent player gets a document that tells the truth, and no enforcement rule is written before there is data to set it from.
- (question) The main exam's own pass mark is still unnamed, and with retakes unlimited it is the only thing between a persistent player and the document.
- (note) FINDING (UX): unlimited retakes retire the pre-start warning's severity. A door that reads like judgement day over a cost of one day trains players to skip every warning in the product; one calm honest door for both theme and main exam.
- (note) FINDING (UX): what unlimited does NOT fix — fail at 44 percent, timer, sit again blind, 46 percent, timer. A slot machine with a daily lever. The fix that reveals no answers: a per-theme breakdown on the result screen, 'fractions weak, percentages solid', turning the wait into preparation.
- (note) FINDING (UX): with hard-only and total silence, a 'question 7 of 30' counter becomes load-bearing — the only signal that the player is moving rather than falling into a void.
- (note) FINDING (UX): the 'hundred percent and no certificate' state changes tone with unlimited retakes — it becomes an invitation rather than a last stand. Its own course screen, reading as a threshold walked up to, not as a certificate that failed to load.
- (note) FINDING (dev), verified and severe: there is no floor on hard-question count anywhere. QuestAuthoringValidation.kt:15 is hasHard = saved.any { it.difficulty == HARD } — one hard question satisfies publication — and processArenaRequest (index.js:1577) only checks the list is non-empty. A twelve-lesson course can be legal with twelve hard questions in total, against a draw of twenty. No sampling then occurs: the exam IS the whole pool, every time.
- (note) CONSEQUENCE: unlimited retakes are safe only while the pool is much larger than the draw. At a pool of twelve the second attempt is recall, not examination, and the certificate attests memory of twelve questions.
- (decision) Hard-only for the main exam is adopted, but conditional on a floor for hard-question count, checked AT PUBLICATION rather than at exam start — at start the course is already live and the main exam is a dead end the author cannot be pulled out of. The publication index gives the check for free.
- (decision) Split the publication draw index by difficulty, so a HARD draw does not read the easy half of the course.
- (note) FINDING (dev): sync has three directions, not one. Bottom-up: submitLessonResultEvents, results, the anti-cheat feed — exactly as Tpov described. Top-down delta: content, LessonRemoteDataSource.fetchChangedByParents(themeIds, cursor) by lastModifiedAt. Top-down wholesale: profile, ProfileRepositoryImpl.kt:64 local.upsert(remoteProfile), on a schedule via ProfileSyncWorker (profile_periodic_sync). Charge and nolics live in the third.
- (note) DISAGREEMENT in the room on the calendar: the analyst reads the cooldown as per (theme, rung), so both rungs can be sat the same day and a theme costs one day; the dev reads it sequentially at two days per theme. Ten themes is then either ten or twenty days before the main exam is visible at all. The spec assumption is ambiguous and must say which.
- (question) Is the 23-hour cooldown per (theme, rung) — allowing the easy and hard exams of one theme on the same day — or one per theme? It decides whether a twenty-theme course costs twenty days or forty before the main exam appears.
- (event) Spec re-derived after round 8: CAP-17 monotonic admission record and CAP-18 certificate contents added, hard-question floor and difficulty-split index made constraints, charge named as the existing throttle. 18 capabilities, 10 open questions. Self-validate both passes: PASS.
- (direction) Tpov round 8, 2026-08-29: pass mark, cooldown scope, certification eligibility and draw size all settled.
- (decision) Main exam pass mark: 60 percent, as a starting value.
- (decision) RESOLVES the cooldown ambiguity: the 23-hour cooldown belongs to one exam, not to the player. Every exam in the app can be sat on the same day, each once. This collapses the calendar objection entirely — the time cost of a certificate is finishing the lessons, not waiting out exam cooldowns.
- (decision) NEW: a course is eligible for certification only if it holds at least 200 questions, as a starting value. Checked when the course is created, with a certification notice shown to the author — not at exam start.
- (decision) Draw size is 20 questions for every exam, theme and main alike.
- (note) GAP left by the 200-question rule: it counts ALL questions, while the main exam draws only hard ones, and QuestAuthoringValidation.kt:15 still accepts a single hard question in the whole submission. A 200-question course can therefore satisfy certification and still hold a hard pool smaller than one 20-question draw.
- (question) How many of the 200 must be hard? Recommendation on file: at least 100 (half), so a 20-question hard draw samples from roughly five times its own size and repeated sittings do not converge on the whole pool. Below about 60 hard, the second sitting is largely recall.
- (note) VOCABULARY: Tpov did not recognise 'пол' as a translation of 'floor'. Use 'минимум' in Russian.
- (event) Spec re-derived after round 8: CAP-12 rewritten to a per-exam cooldown, 60% pass mark, 20-question draw and the 200-question certification rule added as constraints, calendar question retired. Nine open questions. Self-validate both passes: PASS.
- (note) FINDING (PM): the loot-box charge is WITHDRAWN at a hard pool of 100, a draw of 20 and a 60% bar — twelve of twenty from a hundred cannot be rolled, it has to be known.
- (note) FINDING (PM): the persistence charge is not withdrawn, it moves from luck to memory. A hundred questions, twenty a sitting, sittings without limit — a player memorises a hundred flashcards in a week. That may be a legitimate thing to certify; if it is, the certificate should say so.
- (note) FINDING (PM), a real contradiction the room now holds both sides of: with the cooldown owned by the exam, five charges buy five DIFFERENT exams, not five attempts at one. The 23-hour cooldown therefore restrains nothing for a player going wide across themes — and if the main exam obeys the same rule, 'unlimited retakes' means in practice 'once a day, forever'.
- (note) CORRECTION to the architect's answer on price: the charge is not the throttle on the MAIN exam. Per-exam cooldown caps the main exam at one sitting per 23 hours regardless of charge, so charge throttles breadth across exams, not depth on one. The main exam's price is time, not charge.
- (note) FINDING (PM): the 200-question rule should count hard questions, not all. Endorses 100 hard. And the hole stands — 199 easy plus 1 hard passes a 200-total gate, earns certification rights, and the main exam physically cannot be assembled.
- (question) Does the main exam obey the same 23-hour per-exam cooldown as a theme exam? The spec currently implies yes via CAP-12 and says 'unlimited retakes' in CAP-16; both readings are live in the room and they describe very different products.
- (question) Where does 60% come from, and what will move it? The PM asks for one line in the spec naming what the server records on launch day so that in a month it is known whether 60 is a wall or a formality. There is no completion metric today and no exam metric either.
- (note) The analyst withdraws her twenty-day calendar claim as her own error once the cooldown was clarified as per-exam.
- (note) FINDING (analyst), framing: the bank-to-draw ratio is now the ENTIRE protection model. Money does not touch the path, time is gone, attempts are unlimited, easy lessons reveal answers and Room holds every answer on the device. The only scarce resource left is how many questions the bank holds against the twenty a sitting draws. The 100-hard rule is therefore not a content-quality rule, it is the last remaining lock, and what governs is the ratio, not the absolute number.
- (note) FINDING (analyst), a hole nobody had named: the 200-question threshold sits at COURSE level while CAP-3 draws twenty from a THEME. A course reaches a hundred hard easily while one theme holds three — and that theme's hard exam shows the whole theme every sitting. Since theme exams are the real gate, the lock is soft exactly where it is checked. The minimum is needed on both levels.
- (note) FINDING (analyst): a check at course creation is a warning, not a gate, and it looks at the course once in its life. Authors add and archive questions for years, so certification eligibility is a computed property recalculated continuously, not a stamp at birth.
- (decision) The certificate must snapshot what it attested — bank size and pass mark at the moment of issue. The course is mutable; the verification page is public and permanent. Without the snapshot the page will one day show a verifier a claim that was true and stopped being true.
- (note) FINDING (analyst) on 60%: with unlimited sittings it is a speed bump, not a threshold. Twelve of twenty cannot be guessed — but with a small bank it need not be guessed, only recognised. What a verifier is being sold is recognition of questions from a set the holder had unlimited access to and unlimited attempts at. Sellable honestly, but not under the word 'exam'.
- (question) Does the per-theme minimum of hard questions need its own number, separate from the course-level 200? CAP-3 draws twenty from a theme, so a theme with three hard questions defeats the lock regardless of the course total.
- (note) CORRECTION (dev), verified by direct computation: the coverage table on file answered the wrong question. Pool size does not enter the pass probability at all. Passing is 12 correct of 20; for a player who knows fraction p of the material it is Binomial(20,p) >= 12 — p=0.4 gives 5.7% a sitting, p=0.5 gives 25.2%, p=0.6 gives 59.6%. A hundred hard questions or a thousand, the same numbers.
- (note) CONSEQUENCE, verified: unlimited retakes multiply that. Knowing 40% of the material passes with 82.5% probability over 30 sittings and 90.2% over 40; knowing 50% passes with 94.5% within 10. The ONLY thing between such a player and the certificate is how many sittings a day are allowed — not the pool size.
- (note) The 100-hard minimum is still worth having, but for the other reason: below it the draw stops being a draw and the sitting becomes recall of a fixed set. It protects the meaning of the exam, not its difficulty.
- (note) CORRECTION (architect): the charge-as-throttle argument is withdrawn. With a per-exam cooldown, five charges spread across five different exams, so charge limits only the sixth exam in a day. It is the motivational cost Tpov called it originally. The real limiter is 'unlimited, but once a day'.
- (note) FINDING (architect): the pass mark holds only because the player never learns which questions they failed. Relax CAP-5 for UX and retakes become search with feedback and the threshold collapses. Any changing pixel after submit now costs a certificate, not just a feeling.
- (decision) 60% lives in server config, not a code constant — 'for now' means moving it must not cost a release.
- (note) FINDING (architect): a twelve-theme course is twenty-four exams, so with charge in hand admission closes in a couple of days, not a month. Keep the rollup per player, not per course, or a popular course produces contention on one document.
- (note) CORRECTION (dev), self-correcting her earlier claim: the publication pass does NOT see the whole course. privateDocuments (index.js:2034-2070) walks one submission's sections/themes/lessons/questions and writes with merge:true (index.js:1610). Nothing ever holds the full course.
- (decision) Certification eligibility is a cumulative counter maintained in the publication pass — total and hard separately — with the certification flag beside it. Course creation is where the author SEES the item, not where it is computed.
- (decision) Count by set of question ids, never FieldValue.increment: questions are re-published on edits through the same merge, so an increment would credit a sixty-question course with two hundred after four edits. Same bug class as the scar documented at index.js:3891-3896.
- (decision) Do not put the course threshold into QuestAuthoringValidation.kt — hasEasy && hasHard validates a SUBMISSION, and a hundred-hard rule there would reject every course's first submission. Submission property and course property are different checks.
- (decision) The minimum applies at every level the draw happens — theme and course both — and must sit well above the draw of 20, or 'random selection' silently becomes 'the whole pool' and CAP-3 dies without a symptom.
- (note) FINDING (UX): ten exams an evening breaks what was built for one a day. The same warning shown ten times becomes an OK button pressed blind — show the door once per evening, then keep it as a line in the start button, no modal. And no 'next exam' as the primary action on the result screen, or the design becomes a conveyor belt.
- (note) FINDING (UX), an upside: an evening of exams leaves the player a map — ten themes, ten results, visible weak spots. The per-theme breakdown assembles itself, which answers what a player does between attempts.
- (note) FINDING (UX): a player now has ten cooldowns ticking at once and needs one place that shows them all, or they spend the next day poking at locks to find the open one.
- (decision) The author sees a 'Сертификация' line from the course's first minute showing distance — '147 of 200' — not a verdict at publish time. Six months of work answered with 'no certification, you have 183 questions' is a slap, not a notification. One sentence explains why the number exists: every exam draws twenty blind, and on a smaller bank players meet the same questions every retake.
- (decision) A course below the threshold publishes normally as a full course without a certificate, never as defective.
- (question) TENSION to resolve: the analyst wants certification recomputed continuously as a live property; the UX lead wants published certification never revoked retroactively, so an author deleting thirty questions cannot strand a player who already passed every theme exam. Both are right — decide whether eligibility is frozen per enrolled player, per issued certificate, or per published version.
- (question) Does the main exam obey the same 23-hour per-exam cooldown? Given the binomial numbers this is now the single most consequential unpinned decision in the spec: once a day makes a 40%-knower wait a month, no cooldown makes it half an hour.
- (question) What does the server record on launch day so that in a month it is known whether 60% is a wall or a formality? There is no completion metric and no exam metric today.
- (event) Spec re-derived after round 9: CAP-19 author certification line and CAP-20 cooldown overview added, CAP-18 given the attestation snapshot, charge-as-throttle constraint replaced with the cooldown finding, certification made a computed cumulative property. 20 capabilities, 11 open questions. Self-validate both passes: PASS.
- (direction) Tpov round 9, 2026-08-29: main-exam cooldown confirmed, bank and exam-count minimums proposed, instrumentation answered, and a validity period floated.
- (decision) RESOLVES the most consequential open question: the main exam DOES obey the 23-hour per-exam cooldown. Unlimited retakes therefore means unlimited but once a day — a player who knows 40 percent of the material needs roughly a month of daily sittings, one who knows half needs about ten days.
- (decision) Every theme must have an exam. It is no longer optional per theme.
- (decision) PROPOSED minimums, superseding the 200-per-course rule: 200 questions behind each exam, and at least 10 exams in a course for it to certify.
- (question) AMBIGUITY, material: '200 questions per exam' with at least 10 exams reads as at least 2000 questions in a certifiable course — an order of magnitude above the earlier 200-per-course rule. Confirm the reading: is 200 the bank behind each single exam, or still the course total?
- (decision) Instrumentation: every stage is persisted — all lesson attempts as now, and exam sittings stored separately from them. Statistics fall out of that, and Tpov will play it himself and adjust.
- (note) This answers the PM's launch-day question: the record to keep is the exam sitting itself, separately from lesson attempts, so pass rates against the 60 percent bar are measurable from day one.
- (question) Tpov did not recognise the frozen-vs-live eligibility question and read it as being about expiry. It still needs answering, in plainer terms: an author publishes a certifiable course, a player passes every theme exam, the author then deletes half the questions and the course no longer qualifies. Is eligibility fixed when the player starts the course, when the certificate is issued, or per published version?
- (decision) PROPOSED: a validity period of one year. Floated while answering a different question, most likely meant for the certificate rather than for hearts; needs confirming before it becomes a constraint.
- (note) A certificate that expires changes the analyst-versus-UX tension materially: if nothing is permanent, freezing eligibility forever matters less, and the verification page gains an honest reason to state a date.
- (event) Spec re-derived after round 9: main-exam cooldown pinned into CAP-16, CAP-21 added for sitting persistence, per-exam bank minimum and mandatory theme exams added, open questions reshuffled to 11. Self-validate both passes: PASS.
- (direction) Tpov round 10, 2026-08-29: terminology split, minimums named, and the eligibility question answered from the other end.
- (decision) TERMINOLOGY, adopted from Tpov's suggestion: the theme-level assessment is a контрольная — rendered 'theme test' in this English spec — and 'экзамен' / 'final exam' is reserved for the course-level one. Russian school vocabulary, and it removes the confusion of two things both called exams.
- (decision) RESOLVES frozen-versus-live eligibility from the other end: the server forbids an author from publishing an update that would drop a certifiable course below the bar. The problem is removed at the source rather than adjudicated afterwards.
- (decision) Course version governs the rest: if the course updates while a player is working through it, the current certificate is earned against the updated course.
- (decision) Minimums named: 200 questions behind each theme test, and 20 to 30 theme tests in a course for it to certify.
- (note) ARITHMETIC of that: 20 tests times 200 questions is 4000 questions in a certifiable course, and 30 times 200 is 6000. Ten times the draw at every level, so the lock is genuinely solid — and it puts certification out of reach of a casual author. That may be the intent; it should be a knowing choice.
- (question) Is the 200 per theme test the total for the theme, or 200 per rung — the test has an easy sitting and a hard sitting, each drawing 20, so a 200-question theme could still hold only a handful of hard ones.
- (event) Spec re-derived after round 10: terminology split to theme test / final exam throughout SPEC and companions, certification minimums and the no-de-certification rule added, open questions down to eleven. Self-validate both passes: PASS.
- (note) Title and Why updated to carry the two-assessment vocabulary explicitly.
- (direction) Tpov round 11, 2026-08-29: final three numbers settled.
- (decision) Each theme test needs 100 easy and 100 hard questions — 200 per theme, split explicitly, so both the easy and the hard sitting draw 20 from a bank of 100.
- (decision) A course certifies with 12 theme tests. That is 2400 questions — 1200 easy, 1200 hard — reachable by a determined single author rather than only by a team.
- (decision) The certificate is valid for one year. Re-earning it means sitting the final exam again.
- (note) The validity period gives the verification page an honest claim it can support: passed this course within the last year, at this percent, against a bank of this size.
- (event) Spec re-derived after round 11: 12 theme tests at 100 easy plus 100 hard each, one-year certificate validity. Every load-bearing question is now closed. Self-validate both passes: PASS.
- (event) Open questions renumbered to 8 after the round-11 closures; none of the remaining eight is load-bearing for the core design.
- (direction) Tpov round 12, 2026-08-30: the difficulty ladder collapses from two sittings into one, and the pass thresholds move.
- (decision) SUPERSEDES the two-rung structure: a theme test is ONE online sitting with two phases — random easy questions first, then hard — not an easy sitting and a hard sitting with separate cooldowns. Both phases are online and server-scored.
- (decision) SUPERSEDES the theme pass rule of 50 percent hard: passing a theme test requires 30 percent on the hard questions.
- (decision) The draw scope is defined by the hierarchy node, generically: a theme test draws from everything under its theme, the final exam from everything under its course. One rule, two scopes.
- (decision) Lesson unlock is immediate and local: two stars on a lesson opens the next one, and the player does not wait for hard-question results to come back from the server. Easy is scored on the device, so the unlock decision needs no round trip.
- (decision) Purchasing a lesson does not trigger a download — the content is already there. Loading is a separate feature that brings sections down in portions as the player advances.
- (question) Does passing a theme test still require 100 percent on its easy phase, or is 30 percent on hard now the whole rule? Only the hard threshold was stated.
- (question) Is the final exam now easy-then-hard like a theme test, or still hard only? 'По сути так же, только 60 процентов сложные' reads as the same two-phase shape, which reverses the earlier hard-only decision.
- (question) Draw size per phase: still 20 questions in total across both phases, or 20 easy plus 20 hard?
- (decision) RESOLVES the easy-phase question: easy is gating and must be 100 percent correct. Only then does the hard phase open. This is the long-standing ladder the codebase already implements as computeHardUnlocked's all-nines test, now applied inside a sitting rather than across sittings.
- (decision) Theme test pass rule is therefore 100 percent of the easy phase, which unlocks the hard phase, plus 30 percent of the hard phase.
- (decision) RESOLVES the final exam's shape, confirming rather than reversing the earlier call: hard questions only. The theme tests already carry the easy phase, and the final exam is limited to one sitting a day, so re-asking easy material there buys nothing.
- (note) CONSEQUENCE of easy being gating inside one sitting: a player who scores 95 percent on the easy phase never sees a hard question, and the sitting ends there — having spent a charge and consumed the 23-hour cooldown. That is a harsher failure than the old two-sitting shape, where the easy rung was its own attempt.
- (decision) The easy phase being lookupable from the device is accepted rather than closed. Admission to a theme test already required 100 percent easy on every lesson of that theme, so the easy phase re-confirms what was proven rather than gating anything new; the real gate is the 30 percent hard, whose answers never reach the device. No carve-out to AD-5's publication rule is needed.
- (question) Draw size per phase is still open — the earlier 'always 20 questions' predates the two-phase sitting.
- (direction) Tpov round 14, 2026-08-30: the theme test becomes a single mixed pass.
- (decision) SUPERSEDES the two-phase sitting decided one round earlier: a theme test is ONE pass with easy and hard questions MIXED, 10 of each, 20 in total. There is no easy gate inside the sitting.
- (note) This retires the harsh consequence flagged last round — a player who missed one easy question no longer loses the whole sitting — because there is no longer a phase to fail before the hard questions appear.
- (decision) The theme test's pass mark rises from 30 percent to 60 percent, matching the final exam.
- (decision) The pass mark is configurable and intended to be set per course by the author or by an algorithm in future, not a global constant. It joins the server-config set alongside the cooldown and the draw size.
- (note) The admission record simplifies with the mixed sitting: one best percent per theme rather than a best easy and a best hard, since one sitting now produces one number.
- (question) ARITHMETIC PROBLEM created by mixing, with numbers: easy answers are on the device because offline lessons need them. Ten easy of twenty is half the scored set, so a player who reads them off their own Room database starts at 50 percent. At a 60 percent bar they need only 2 of 10 hard, which random guessing supplies 75.6 percent of the time on a single sitting — and retakes are daily and unlimited. At 80 percent they would need 6 of 10, which guessing supplies 2 percent of the time.
- (question) Options on file: raise the bar to roughly 80 percent; or score only the hard questions and let the easy ones ride as unscored warm-up; or weight hard more heavily; or shift the mix toward hard. Doing nothing makes the theme test passable by lookup plus guessing.
- (decision) RESOLVES the mixing hole by composition: a theme test is 5 easy and 15 hard, mixed, 20 in total. Looking up every easy answer now yields 25 percent, so a 60 percent bar still needs 7 of 15 hard — which guessing supplies 6 percent of the time, against 76 percent at the ten-ten split.
- (note) The honest side of the same table: a player who knows 60 percent of the hard material passes 90 percent of the time at 5-15, and one who knows 80 percent passes essentially always. The composition costs almost nothing to someone who studied and almost everything to someone who did not.
- (decision) The final exam stays hard only, 20 questions, 60 percent.
- (direction) Tpov round 15, 2026-08-30: prices named.
- (decision) Lesson unlock costs 10 nolics; unlocking a lesson's hard mode costs 20. Tpov reached this by noticing that one unlock would otherwise hand over both difficulties at once, so they are priced separately.
- (decision) Price is flat, never escalating with progress. Reason given: a player interested in one particular theme should not pay more for reaching it, and nolics are a motivational currency rather than a competitive one. Escalation would matter only if nolics were traded between players.
- (decision) Charge costs restated: a lesson attempt stays 33 units, a theme test costs one whole charge (100), and the final exam costs two (200). This supersedes the earlier single-charge exam.
- (note) HONEST FLAG on the 10-nolic price: one lesson replayed at 100 percent easy pays exactly 10 nolics, and replays are not blocked — a new attempt carries a new attemptId and pays again. So the sequential gate costs one replay per lesson skipped, which is close to free. Tpov said explicitly these are rough numbers he will play with and adjust, so it is recorded rather than argued.
- (question) Reading to confirm: 10 buys past the sequential gate for one lesson, and 20 separately buys that lesson's hard mode, which is otherwise earned by answering all its easy questions correctly. Two distinct purchases rather than one tiered price.
- (direction) Tpov round 16, 2026-08-30: the reward and price formulas are redesigned around allocated time.
- (decision) SUPERSEDES the flat 10/20 unlock price: the price of unlocking a lesson is computed from its ALLOCATED TIME, the same quantity the timer is built from — characters times the difficulty coefficient. A longer lesson costs more to skip.
- (decision) Rewards are computed from the same allocated time: XP and nolics scale with how much time the attempt was worth, not with a flat per-attempt figure.
- (decision) The hard tariff rises from 2x to 3x.
- (decision) The percent coefficient is super-linear — the higher the score, the higher the rate per unit. Legacy had this; the shape here will differ.
- (decision) SIGNIFICANT: payment is on IMPROVEMENT over the player's previous best, not on the attempt's absolute score, and that improvement is paid at a doubled coefficient. A first attempt is entirely improvement, so it is doubled in full; going from 50 to 60 percent pays for ten points, doubled.
- (note) CONSEQUENCE, and it closes the hole flagged one round earlier: paying only for improvement kills the grinding faucet outright. Replaying a lesson already at 100 percent yields nothing, so a player cannot farm nolics by repeating one lesson to buy their way through a course.
- (decision) Unlock prices apply in exactly two places: on the arena when a player wants one particular lesson, and inside a course when the previous lesson is unpassed. Nowhere else.
- (decision) Economy targets: the minimum unit is one nolic, and inflation is to be avoided. Calibration anchor given by Tpov — one lesson passed perfectly first try, on both easy and hard, funds exactly one unlock of another lesson.
- (note) IDEA parked, not decided: bonus multipliers such as x2 for a first-try clear. Tpov flagged it as a plan rather than a decision.
- (question) The shape of the percent coefficient is unnamed. Super-linear is stated; whether it is quadratic, stepped, or something else decides how sharply the top of the range pays.
- (decision) Repeat coefficient set to 0.1: percent already earned pays a tenth on a replay. New percent keeps its doubling untouched. A perfect small lesson pays 212 nolics first time and 11 on every replay after, so grinding a known lesson for one unlock costs about seven hours against forty minutes at full rate.
- (decision) Tariff bands kept as bands rather than fitted to a curve. The closest simple continuous fit, p + 1.65p squared over 100, is accurate at the top and about 40 percent wrong around 25 percent — exactly where payments are small and a single nolic is visible.
- (note) IMPLEMENTED: functions/lesson-reward.js carries the whole economy — marginal tariff bands, the improvement rule, the hard tariff, allocated-time scaling and the unlock price — with 11 tests. functions/lesson-unlocks.js now derives prices from it instead of holding flat numbers, and unlockLesson reads the lesson's own questions to price it rather than trusting the client.
- (note) The JS timer mirror (computeTimer from RunnerLogic.kt) now lives in lesson-reward.js, because allocated time is what both the reward and the price are built from. It is a second Kotlin/JS pair alongside the scoring one and must change in the same commit.
- (question) Still unwired: the reward formula is not yet called by submitLessonResultEvents, because paying on improvement needs the server to hold a per-lesson best per player, which does not exist. That is the lesson-progress aggregate the spine names, and it is the next real piece.
- (direction) Tpov round 17, 2026-08-30: the charge economy, split into two currencies. NOTE: the message was interrupted mid-thought in its final section about plasma synchronisation, so the last rule is captured but incomplete.
- (decision) Two kinds of charge, renaming the old hearts: the ordinary charge (yellow, formerly the standard heart) and the plasma charge (formerly the gold heart). Ordinary charges act on easy questions, plasma on hard ones — including in tournaments.
- (decision) PREFERRED mechanic: a charge does not reveal the answer, it SKIPS the question and it is counted as correctly answered. Tpov's reason is that a player who never sees the answer cannot farm answers across several accounts.
- (decision) Because the server scores the result, it knows how many charges were spent. Spending beyond the allowed limit is not blocked but recorded — flagged with its justification, in keeping with the recording-only anti-cheat already decided.
- (decision) Ordinary charges are bought with nolics; plasma charges with gold, at an escalating price — one charge one gold, two charges two gold, three charges three gold.
- (decision) Maximum counts are server constants, not code: about 10 ordinary and 3 plasma to start, and Tpov intends to change them.
- (decision) ALL economy constants move to a server-side table — maximum charges of each kind, regeneration periods, prices. The client pulls them on sync, so raising a price or a cap is a server edit rather than a release.
- (decision) SECURITY MODEL, stated as a general split: motivational material versus monetary material. Motivational — XP and nolics — is cheatable by design, reconciled by a monthly server-side pass, and treated lightly. Monetary — gold, NFTs, icons — is bought with real money and potentially sellable for it, so every operation happens online and is strictly controlled. The plasma charge counts as monetary.
- (decision) Plasma therefore regenerates on the SERVER only. It does not regenerate on the device, and it is settled when results are uploaded — spent as the server reads the answers. Locally it sits at zero until that settlement, which is what makes the offline double-spend detectable.
- (note) Ordinary charge regeneration stays at one hour. Plasma is undecided between 24, 12 and 8 hours; Tpov reasoned aloud about tournament cadence and did not settle it.
- (decision) Ranking tie-break at 100 percent goes to time: whoever finished faster ranks higher. Floated but not decided: converting speed into a percentage bonus instead.
- (question) Does one ordinary charge give a hint on ONE easy question, or on ALL easy questions of a sitting? The wording reads as all, which would make a single charge decisive.
- (question) Plasma regeneration period: 24, 12 or 8 hours. Tpov called three skips out of twenty questions generous and did not resolve it.
- (question) The final paragraph on plasma synchronisation was cut off mid-sentence. The rule captured is that plasma never regenerates locally and is settled at upload; the half-regenerated case Tpov was working through is incomplete.
- (decision) The per-lesson best a player has scored is kept as a map field on the user document, beside the unlocks and the balance — the same document submitLessonResultEvents already reads and writes in its transaction. No new aggregate, no new query, no new sync path. Tpov's point stands: the server knows the formula and can compute each result's payment itself.
- (decision) Forgery resistance is explicitly not this formula's job. Nolics are a motivational currency; catching an inflated result belongs to the anti-cheat script, which is a separate concern with its own data.
- (note) IMPLEMENTED: submitLessonResultEvents now pays through the new formula. Allocated seconds per lesson are read once before the transaction from the lesson's own questions; the previous best per (lesson, difficulty) is read from and written back to lessonBest on the user document inside the same transaction. The old flat lessonResultReward, HARD_RESULT_REWARD_MULTIPLIER and NOLICS_PERCENT_STEP are deleted.
- (event) E3 complete and the full gate is green: sequential lesson gate with three access states, questType threaded through navigation so only courses gate, locked card, server-side unlockLesson priced from the lesson's own questions, lessonUnlocks persisted on the user document through Room migration 2 to 3, and the purchase wired from the lesson list. Reward formula live in submitLessonResultEvents.
