---
topic: The full quest lifecycle: authoring a draft, review, publication to the arena (or the archive for courses), play, and a developer's promotion of an arena quest onto the home or tournament shelf — brownfield contract for the production release
updated: 2026-09-01T17:03
---

- (direction) Owner asked to study the whole circle: quest creation -> play -> publication to arena, tournaments and the home shelf, ahead of a production release.
- (constraint) Owner's release rule: a quest can be released only to ARENA; a COURSE goes to the ARCHIVE instead. No other shelf is a publication target.
- (constraint) Owner's release rule: only from ARENA can a DEVELOPER move a quest onward, to HOME or to TOURNAMENT, and the quest then disappears from ARENA — the move is a transfer, not an addition to a set.
- (direction) Owner offers up to 10 real devices/accounts so every lifecycle scenario can be run end-to-end before release.
- (note) ADR-0005 (docs/architecture/0005-quest-lifecycle.md) is the standing design: three axes QuestType x QuestPhase x visibleOn set, with shelves arena/tournament/tournament_leader/home/archive.
- (assumption) 'Домашние' in the owner's request means the HOME shelf (домашние квесты), not school homework — consistent with ADR-0005 and the menu-refactor spec.
- (event) Launched an 8-slice adversarially-verified codebase map of the lifecycle (workflow quest-lifecycle-map) to ground the spec in what the code actually does.
- (decision) Owner approved seeding the LIVE Firebase project school-quiz-89336951 with test data for the device run; destructive/negative security probes stay on the emulator.
- (decision) Owner declined to settle the shelf model on the spot and asked for a separate study — legacy behaviour, common industry practice, and a recommendation — before choosing between transfer-semantics and set-semantics.
- (note) Anonymous sign-in (AppApplication.kt:102) means every device self-provisions a uid; roles are granted server-side via admin SDK, so no human login is needed for the device matrix. Helper: scripts/devices/uid.sh.
- (note) Device matrix sized at 9 (floor 6): author, tester, admin/logic, translator, senior translator, developer, players A/B, cold player C.
- (decision) Owner approved seeding the live Firebase project school-quiz-89336951 with test data for the device run; destructive and security-hole tests stay on the emulator.
- (decision) Owner deferred the ADR-vs-code question: instead of choosing now, he asked for a separate study of the shelf-transition mechanism — how legacy did it, how the industry does it, what is best for us.
- (note) Devices will be connected by the owner; roles are granted server-side via admin SDK, no login needed — the app signs in anonymously (AppApplication.kt:102) and the uid is readable per device via scripts/devices/uid.sh.
- (event) Launched workflow shelf-mechanism-study: 6 recon lenses (legacy archaeology x2, industry x3, our-constraints), 3 biased designs, 3 judge lenses, one recommendation ending in <=6 plain-language owner decisions.
- (note) CORRECTION, verified first-hand: developerLevel IS granted automatically. The rating aggregator writes profiles/{uid}.developerLevel from quest ratings — delta (rating-2)*10, POINTS_PER_STAR=10 (functions/index.js:88-90, :1968-1970, :2040-2053). Eleven 3-star ratings from distinct accounts cross the >100 gate (functions/index.js:1347). Self-rating is skipped for this score (:1962) but counted in the public average (:1846-1854).
- (constraint) developerLevel > 100 also satisfies isVerifier() and isReviewer() (firestore.rules:186-193, :195-204), so the farmed number opens verification_requests — real name, birthday, city, telegram — and private/{anyUser}/**.
- (constraint) firestore.rules let any signed-in user CREATE quests/{id} with visibleOn:['home'] (firestore.rules:99-109) and UPDATE visibleOn on their own quest (:112-116). The app itself never writes /quests, so both can be closed outright.
- (decision) Shelf mechanism: keep the manual transfer the code already implements (option A), plus five additions; reject automatic rating-driven promotion — it never existed in either generation and the rating substrate cannot support it. Full study: _bmad-output/specs/spec-quest-lifecycle/shelf-mechanism.md
- (note) Additive shelves are rejected on evidence, not taste: LessonResultOutboxWriter.kt:113-120 picks one shelf by priority and functions/tournament-ranking.js:288-296 forms a tournament group only from a tournament shelf, so a quest on {arena,tournament} would score in no tournament.
- (question) Six owner decisions are pending — see section 6 of shelf-mechanism.md: who may move on launch day; does Home remove from Arena; what happens when an author edits a curated quest; may a course return to the archive; is a hide-everywhere button needed at launch; what happens to existing farmed developer numbers.
- (note) VERIFIED: the review gate is attendance, not judgement. lessonPassed only asks whether a score EXISTS — hasTestingResult returns true for any non-null testingScore (functions/index.js:3468-3474, :2569-2583). Scores are 1..3 and REVIEW_PASS_LEVEL=100 is applied to translation only (:2580). A tester scoring 1/3 publishes the quest, and lessonReviewStatus can only return READY_FOR_PUBLICATION or UNDER_REVIEW (:2565-2567) — there is no reject path at all.
- (note) VERIFIED: an author can review their own quest. canSubmit (functions/index.js:2427-2455) and availableTasks never compare the submitting profile's uid to task.ownerUid, though ownerUid is carried at :2390.
- (note) VERIFIED, closing open question 5: reconcileChangedReviewLessons (functions/index.js:1767-1801) does reach publishSubmissionIfReady, so the nightly job can publish — but through the same lessonPassed gate, so it is a safety net rather than an additional hole.
- (constraint) Release blocker chain, unattended end to end: farm 11 ratings from free anonymous accounts -> developerLevel > 100 -> self-review own quest at the lowest score -> auto-publish to arena -> self-promote to the children's home shelf. Each of the four fixes breaks the chain independently.
- (decision) Device count corrected from 9 to 6: A author, B player, C tester, D admin, E developer, F second player for a real tournament group. Translators are optional because required languages come from configs/arena_review, which we seed.
- (decision) Owner chose to FIX the six release blockers before running the device scenarios.
- (decision) Self-review rule, from the owner: an author may not approve their own quest — EXCEPT a DEVELOPER, who may approve their own. So the guard is 'uid == ownerUid is refused unless developerLevel > threshold', not a blanket refusal.
- (direction) Owner asked to mine legacy/ for anything that simplifies planning the fixes, rather than designing each from scratch.
- (question) Owner does not remember how qualification points are actually awarded, and believes they come from complaints in chats rated by a senior moderator or a developer. This must be answered from the code before the points design is settled — the rating-driven grant found in functions/index.js:1968 may be only one of several sources.
- (event) SPEC.md derived from the memlog: 12 capabilities, 6 constraints, 5 non-goals, companions brownfield.md + shelf-mechanism.md + adopted ADR-0005 and ADR-0006.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6 and 8): PASS. Every capability carries intent and success; non-goals are explicit and evidence-backed; the success signal is a runnable six-device scenario plus four negative probes.
- (event) Self-validate pass 2 (preservation): PASS with one deliberate drop. Every load-bearing memlog claim landed in SPEC.md or a companion. Dropped as wrapper-only: the operational decisions about seeding the live project, the uid helper script, and the workflow launch records — process, not contract.
- (question) CAP-7's exact field split is blocked on the points investigation now running (workflow points-and-legacy-mining).
- (note) ANSWERED — how points are awarded. Exactly one mechanism raises a qualification level anywhere in the product: the daily quest-rating aggregator, hardcoded to developerLevel (functions/index.js:91). testerLevel, adminLevel, translatorLevel, moderatorLevel and sponsorLevel have NO award path at all, in either generation — they can only be typed into Firestore by hand. There is no admin-grant callable among the 38 exports.
- (note) VERIFIED: moderatorLevel is inert. Its only occurrences are mirror writes (functions/index.js:3596, :3603, :3619, :3733, :3795) and Room schema declarations; no gate in functions/ or firestore.rules consults it, and requireProfile does not even load it.
- (note) VERIFIED: reviewReputation is an orphan. Written at functions/index.js:2925 (+3/0/-3 for agreeing with the senior consensus; +5/-1 per translation segment), mirrored at :3615, and read by nothing except an instrumented test (LiveFirebaseReviewWorkflowInstrumentedTest.kt:168-169). It never becomes a level.
- (note) The owner's complaint-in-chat memory resolves to three real fragments, none of which is the chain he described. Legacy shipped a chat SCREEN that never worked (send() had no callers, the listener was TODO, new-message check hardcoded false). Legacy contained unused formulas turning chat time into SKILL, not qualification. And legacy had a working manual grant dialog where a privileged human wrote a message plus trophy/nolic/gold/skill amounts into another user's profile inbox — a senior awarding points with a written justification, but never about a complaint. No complaint object has ever existed in either codebase; the Russian and English sweeps return zero, including across git history.
- (decision) The manual grant dialog from legacy is the shape to copy for CAP-7's admin grant: a named target, an amount, and an attached written reason, delivered to the recipient. Legacy's flaw to avoid — its host screen was unreachable and the write was client-side.
- (note) Legacy prior art per fix: F1 has the target design already written (skill ladder and six independent qualification ints as separate axes, thresholds all 100 — the origin of the current > 100). F4 has a protected-field table with revert-on-violation and an audit record worth copying as a table, though its architecture failed because the client chose whether to call it and carried its own bypass flag. F5 has a server-owned hide flag the sync respects. F2, F3 and F6 have nothing at all — legacy never had a reject flow, a self-review rule, or a report/block path.
- (question) moderatorLevel is a role that grants nothing and is read by no gate. Decide: wire it to the review/moderation work, or delete it from the model so it stops implying a capability that does not exist.
- (event) SPEC.md re-derived: CAP-7 now carries the answer to the points question and names the fact that no role has an award path; open questions replaced with moderatorLevel and reviewReputation.
- (note) Story breakdown PROPOSED (not yet confirmed by the owner): 8 stories rendered to stories.yaml in dependency order — rules close, grant path, ratings/privilege split, review verdict, self-review guard, takedown+audit, report/block, device run. Checkpoints proposed by me, pending the owner's word.
- (event) stories.yaml checked against assets/stories-schema.md validity rules 1-4: PASS.
- (decision) Owner chose option B: the moderation system is designed now against a chat shaped like the legacy one, while the chat itself may remain a stub. Moderation therefore ships against the one real user-generated surface — lesson comments — and attaches to the chat unchanged when it lands.
- (decision) Moderation rules, from the owner: anyone may complain; a moderator decides; the complainer gains on an upheld complaint and loses on a rejected one; a moderator's decisions are graded by those above them, plus or minus by how far they sat from the senior verdict; and a moderator can only be banned by a moderator at least 100 levels above, or by a developer.
- (note) The owner's moderation design is the mechanism the codebase already implements for quest review — scoreAggregate (functions/index.js:2593-2610): the reviewers within ACTIVE_LEVEL_WINDOW=100 of the highest-level reviewer form the senior band, their rounded mean is the verdict, and everyone below is paid +3 for matching it, 0 for missing by one and -3 for missing by two. The owner's '100 units above' is the same constant. Moderation should reuse this function rather than grow a second consensus implementation.
- (constraint) Legacy chat shape, for the stub contract: tpovId, time, user, msg, importance, icon, rating, reaction (ChatEntity.kt:8-19, ChatRemote.kt:5-13). The per-message rating slot existed and was never written; importance was the only field ever read, colouring the row at importance == 7.
- (note) BLOCKER for moderation, verified: lessonComments cannot be moderated at all today. The document stores authorNickname and authorAvatarUrl but NO author uid (firestore.rules:142-157), so there is nobody to ban; and 'allow update, delete: if false' with no server function touching the collection means an abusive comment cannot be removed by anyone, moderator or not.
- (event) SPEC.md re-derived with the moderation contract: CAP-13..CAP-17 added, three constraints added (chat shape as stub, one consensus implementation, moderatorLevel becomes the moderation right), the complaint-system non-goal replaced by a chat-itself non-goal.
- (event) stories.yaml re-derived to 11 stories: 7 splits into ownership+removal, report+verdict, moderator grading+ban gate, and the chat stub contract. Ids renumbered freely — no stories/ spec files exist yet, so none are pinned. Validity rules 1-4 re-checked: PASS.
- (note) CORRECTION: a design of record for earning qualifications DOES exist and I had not read it. ADR-0006 (docs/architecture/0006-roles-and-qualifications.md:66-80) states: TESTER, TRANSLATOR and MODERATOR are earned automatically by passing an interview quest (CompletionEffect = OfferQualification), TRANSLATOR additionally by promotion from more experienced translators; SPONSOR accrues from economy actions; ADMIN and DEVELOPER are granted BY HAND by an admin through an admin panel gated on adminLevel >= 100. So manual granting is correct only for two of the six roles.
- (note) ADR-0006:92 also settles the moderatorLevel question the owner was asked: 'Доступ к модерации контента -> требует MODERATOR >= 100.' The role was always meant to gate moderation; it is inert only because nothing was built. The owner's instinct matches the standing design.
- (constraint) The interview-quest path cannot ship this release: CompletionEffect and OfferQualification have zero occurrences in production code, and SessionMode.EXAM — which the interview quest would run in — is never constructed outside tests. Manual granting must therefore cover all six roles for now, with the interview path deferred to its own spec.
- (decision) Points model, adopted: quest ratings keep their arithmetic and their reversibility but move to profiles/{uid}.authorReputation, a public score that grants nothing; reviewReputation stays as the reviewer-side score and finally gets a reader; privilege moves to a grantQualification callable that writes an audit document per grant carrying target, actor, role, previous and new level, and a written reason. That audit collection is the seam the complaint verdicts plug into later without a schema change.
- (event) SPEC.md re-derived again: CAP-7 now states its relation to ADR-0006 and covers six roles; CAP-18 added for the two visible-but-powerless reputations; interview quests added as an explicit non-goal.
- (event) Self-validate pass 1 (coherence): PASS — 18 capabilities, each with intent and success; 9 constraints; 6 non-goals.
- (event) Self-validate pass 2 (preservation): PASS — the ADR-0006 correction, the moderatorLevel ruling and the adopted points model all landed in SPEC.md.
- (decision) Owner: one reputation number, not two — report reputation and review reputation are the same score.
- (decision) Owner: a ban's length is set by the banning moderator's level. moderatorLevel 100-199 bans for 7 days; 200-300 bans for a month; above 300, or a developer, bans permanently.
- (assumption) Level 300 sits in both brackets the owner named ('200-300' and '300+'); read as 200-300 inclusive giving a month, and strictly above 300 giving permanent. Flagged to the owner rather than silently chosen.
- (decision) Owner: no lesson-comment data exists yet, so the author uid is required from the first write and no migration for authorless comments is needed. The comment FEATURE is shipped in code (LessonComment.kt, FirebaseLessonCommentRepository.kt, firestore.rules:142-157); it is the DATA that is empty.
- (decision) Owner: on launch day the developer qualification is held by the owner alone, set by hand once; everyone else is granted through CAP-7.
- (event) A production read to confirm the empty collections was blocked by the sandbox classifier because it used the admin service-account key; proceeding on the owner's statement instead.
- (event) Story 1 DONE and verified. firestore.rules: quests create is now 'if false' and the update allowlist keeps only title, description, picturePath — visibleOn, archived and catalogId left it. Verified first that the app never writes /quests: FirebaseQuestRemoteDataSource only reads and calls setPublicQuestShelf. scripts/rules-emulator-test.js gained 9 assertions covering the closed holes. Full suite: 105/105 pass on the Firestore emulator.
- (note) Two environment problems found and fixed while running the suite. firebase-tools now requires JDK 21+; the default java on this machine is 17, and zulu-24 at /Library/Java/JavaVirtualMachines/zulu-24.jdk is what works. And scripts/node_modules was silently half-extracted because the disk was full — the owner freed ~13 GB by deleting the huggingface and fastembed model caches and the one Android AVD, after which a clean npm install produced intact packages.
- (decision) Owner corrected the hierarchy: DEVELOPER is the top role, not ADMIN — confirmed at functions/index.js:3450-3462, where developerLevel overrides TESTING, LOGIC and TRANSLATION while adminLevel covers only the first two. Granting is therefore bounded by rank, not reserved to admins: an actor may grant only strictly below their own level, and never to themselves.
- (decision) Owner removed story 2 from this workstream: the qualification-granting callable and its screen will be built by a separate agent. Until then qualifications are set by hand in the Firebase console, which is the standing fallback and unblocks story 3.
- (note) Story 3 is therefore no longer blocked by story 2: zeroing the farmed developer levels is safe as long as the owner can still set levels by hand, which the console provides.
- (event) Story 3, code half DONE. functions/index.js: the two constants collapse into AUTHOR_RATING_SCORE_FIELD = 'authorRatingScore'; writeUserQualificationLevelDelta becomes writeAuthorRatingScoreDelta and now writes one top-level field on users/ and profiles/ instead of fanning the number into the qualifications and qualification maps; currentRatingQualificationLevel becomes currentAuthorRatingScore and reads the two top-level copies. functions lint and all 11 unit suites pass.
- (decision) isVerifier() and isReviewer() KEEP developerLevel, against the earlier recommendation. The owner established that DEVELOPER is the top role; stripping its read access would contradict that. The escalation is closed by the field split alone — after it, developerLevel can only arrive by a deliberate grant.
- (note) scripts/migrate-author-rating-score.js written, NOT run. Dry run by default; --apply to write; --keep to spare uids. It zeroes developerLevel on profiles/, the four developer mirrors on users/, and resets qualificationAppliedScore on both quest_rating_aggregates scopes so the score re-accumulates into the new field.
- (note) PRE-EXISTING failure found in scripts/review-pipeline-e2e.js:409 — 'первая отправка не начислила очки'. Confirmed unrelated by stashing all functions/index.js changes and re-running: it fails identically on the untouched file. 21 scenarios before it pass. Spawned as a separate task.
- (direction) Owner: report tersely in plain words, no code in chat, keep going through the stories without pausing for confirmation — only stop on a genuine blocking question.
- (event) Story 5 DONE and verified. canSubmit refuses uid == ownerUid below the developer exemption, availableTasks hides the author's own task so the queue never offers what submission would refuse. New e2e scenario 'автор с квалификацией не проверяет свой собственный квест' passes; the full pipeline still publishes after the guard (22 scenarios green, only the known pre-existing reward failure remains). Rules suite green after adding an admin-cannot-write-visibleOn assertion.
- (event) Story 6 DONE. Server: 'archive' joins the accepted move targets; setPublicQuestShelf now also maintains the archived flag to match placement, records previousVisibleOn/actor/reason into admin/shelf_moves per move; new retirePublicQuest callable empties visibleOn with the same audit; the author-triggered republish shelf reset writes an audit record naming the author. Client: 'Вернуть в архив' and 'Снять с публикации всюду' in the developer menu (en/uk/ru), RetirePublicQuestUseCase wired through repository, Koin and the component; local row deletion reuses the existing refresh path. All affected modules compile; presentation unit tests green; functions lint green; e2e pipeline still publishes (22 scenarios, only the known pre-existing reward failure).
- (note) Deliberately NOT added: refusing a curator their own quest's shelf move. All curators are developers, the owner's rule lets a developer act on their own content, and at launch the owner is both the only curator and the author of the seeded catalogues — the refusal would block the main use.
- (event) Story 7 DONE. LessonComment carries authorUid (nullable only for pre-field data, of which the owner says there is none); the Firebase repository stamps it from the signed-in user; firestore.rules require authorUid == request.auth.uid on create so it cannot be forged; removal exists at last — removeLessonComment callable, moderator (>=100) or developer gated, preserving the removed text in admin/moderation/comment_removals so evidence survives the takedown. requireProfile now loads moderatorLevel — the first gate ever to read it. Rules suite 114/114; affected Kotlin modules compile; functions lint green.
- (event) Story 10 DONE. chat-contract.md written as a spec-authored companion: the legacy message shape carried over with tpovId replaced by an author uid and the never-written rating/reaction columns dropped; the report target type set reserves MESSAGE so the chat's arrival is a routing case, not a schema change; four promises the future chat must keep, and legacy's screen-first cautionary tale.
- (decision) Owner: review pass mark is 2 on the 1..3 scale — 2 and 3 publish, 1 does not. A lesson scored 1 stays UNDER_REVIEW until the score is raised, and a separate reject action with a reason returns the quest to the author as an editable draft.
- (decision) Owner: a moderator has three outcomes on a report — uphold (reporter gains, content removed), reject (reporter loses), and uphold-with-ban (same gain plus a ban on the author, its length from the level ladder: 100-199 gives 7 days, 200-300 a month, above 300 or a developer permanent).
- (event) Story 4 DONE. Server: REVIEW_MIN_PASSING_SCORE = 2 — lessonPassed now tests the score against the mark instead of merely asking whether one exists, so a 1 no longer publishes; new rejectReviewSubmission callable (reason mandatory, gated by the same canSubmit that governs scoring, so the author-exclusion and developer exemption carry over) marks the task and the submission REJECTED; a rejected submission never publishes, including through the nightly reconcile. Client: the outcome loop existed in neither direction — the author learned nothing, published or refused. Added fetchOutcomes(ownerUid) on the submission remote, an applyRejection path that returns the draft to DRAFT with the reason attached, Room column rejectionReason with Migration4to5 (db version 5), and the owner uid taken from drafts awaiting review rather than from AuthRepository, to avoid a quest-authoring -> app-shell dependency the project rules forbid.
- (note) New e2e scenario runs on its own submission — the first draft of it consumed the tester's single attempt on the main quest and broke publication. It asserts three things: a pair of 1s does not publish, a reason-less rejection is refused, and a player cannot reject. 23 scenarios pass, up from 22; publication still works; only the known pre-existing reward failure remains.
- (event) Story 8 DONE. submitReport (any signed-in user, one open report per person per target, cannot report yourself); decideReport gated on moderatorLevel >= 100 or developer, refusing your own report, with three outcomes — UPHOLD, REJECT, UPHOLD_AND_BAN — paying the reporter +3 when upheld and -3 when rejected into the same reviewReputation the review pipeline uses. reports/ is client-unwritable and readable only by its reporter or a moderator.
- (event) Story 9 DONE. Ban terms come from the issuer's rank, not the case: moderatorLevel 100-199 gives 7 days, 200-300 a month, above 300 or a developer permanent. Banning a moderator requires a level at least 100 above theirs or a developer. A new firestore.rules isBanned() blocks a banned account from posting comments, treating an absent or null bannedUntilMs on a banned document as no expiry. reviewReportDecision lets a senior grade a decided report, paying or charging the original decider by the same +3/-3 — the owner's 'seniors grade juniors' rule, reusing the existing reputation rather than a second hierarchy. isModerator() added to the rules: the first gate in the product to read moderatorLevel, which ADR-0006 always specified.
- (note) Six new e2e scenarios cover filing, permission, payout symmetry, the three ban terms, the moderator-ban gap and the removal audit — all pass. Rules suite 121/121.
- (note) CORRECTION on the earlier reward failure: it WAS real and pre-existing, as first reported. I briefly retracted that after seeing it pass, but the pass came from the spawned task's fix landing in the working tree, not from a dirty emulator. Verified by stashing: the unmodified tree still fails at that assertion.
- (question) Remaining failure in review-pipeline-e2e: 'purchase-required' when collector claims a nickname, deep in the nickname section. Reproduces with the moderation scenarios disabled and its mechanism is untouched by this work; almost certainly pre-existing and newly reachable now that the earlier blocker is fixed. Filed as a separate task.
- (note) Story 11 prep: scripts/devices/prepare-run.js written (dry run by default) — it writes catalogs/{id}.questType, which nothing in the product writes despite QuestType.kt claiming the type lives on the catalog, seeds configs/arena_review with no required languages so the translation stage does not bind for the six-device run, and grants the per-device qualifications through the admin SDK, which is the only path until the granting callable lands. Developer is granted 300 rather than 100 because the developer gate is strictly greater than 100.
- (event) Device run started on one Pixel 10 Pro with the new build installed. The live project is NOT empty, contrary to the earlier assumption: five catalogs and twelve quests already exist.
- (note) CONFIRMED ON A REAL DEVICE, the grant hole: on the owner's own Google account a long-press on a quest offers only Share — no shelf actions — because profiles/{uid}.developerLevel is unset. Ten taps on the version label flip the CLIENT gate and the developer '+' appears on Home Quests, which is exactly the mismatch the map described: the UI unlocks locally while the server still refuses.
- (note) MAJOR FINDING: the ARENA IS EMPTY in every catalog checked — school, quests, games, surveys all show 'No quests' in the arena-filtered picker, while the home shelf holds all twelve. The existing content was seeded straight onto home by scripts and never travelled the publication path. Since arena is the only publication target and the only source a developer can promote FROM, the circle cannot be demonstrated on existing data: a quest has to be authored and published first.
- (question) Device run is blocked on three things: only one device is connected (six needed for the full circle); granting a qualification needs the admin SDK, which the sandbox classifier refuses; and the live project still runs the old rules and functions, so none of the new behaviour is testable there until a deploy.
