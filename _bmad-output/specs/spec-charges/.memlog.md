---
topic: Charges — hearts reworked into hint/skip charges (standard + plasma), server-owned tunable constants, server-settled spend with cheat detection
updated: 2026-09-03T09:13
---

- (note) Input: user brain dump (voice-transcribed RU) + codebase recon done before the run. Mode: express — input is rich, gaps become open questions.
- (direction) Rename, not new concept: обычное сердце -> обычный (жёлтый) заряд; золотое сердце -> плазменный заряд. Wire fields standardHearts/goldHearts are the thing being renamed.
- (decision) Charges are a hint/skip resource, not an attempt-entry toll. User describes them only as 'gives a hint / skips a question', never as the price of starting a lesson.
- (decision) Plasma mechanic = SKIP THE QUESTION, SCORED AS CORRECT — not 'reveal the answer'. User reasoned to it: 'ты не знаешь ответ, соответственно, не сможешь читерить с множеством аккаунтов'. Answer-reveal would leak content to farm accounts.
- (decision) Standard charge applies to EASY questions, plasma to HARD questions including tournaments. Plasma is the only thing that works inside a tournament.
- (decision) Spend is settled on the SERVER at result submission, not at the moment of the tap. Server debits, then scores the skipped positions as correct. The client tap is a claim, not a transaction.
- (constraint) codeAnswer stays digits-only (/^[0-9]*$/, '0'=not shown, '9'=fully correct) — Kotlin RunnerLogic and functions/result-verification.js mirror each other. Charge claims travel as a PARALLEL mask string, never as a new digit in codeAnswer.
- (decision) Regeneration: standard charge 1h (unchanged from HEART_REGEN_MS), plasma 8h -> 3/day. User weighed 24/12/8 and landed on 8, while flagging the balance doubt himself.
- (decision) Prices: standard charges bought with nolics (escalating ladder, existing STANDARD_HEART_SLOT_COSTS), plasma bought with gold at 1 gold per charge (linear: 1/2/3 gold for 1/2/3).
- (decision) Every knob is a server-owned constant in configs/* (pattern already established: configs/nickname_policy, configs/arena_review), pulled by the device on sync. Changing a max or a price on the server changes client behaviour with no app release.
- (constraint) Constants are currently hardcoded in TWO places with no single source: functions/index.js:93-96 and the UserProfile.kt companion. The config document becomes the single source; both sides read it.
- (decision) Currency classification is part of the contract: MOTIVATIONAL (nolics, skillPoints, standard charges) — forgeable, reconciled by a periodic server sweep; MONETARY (gold, plasma charges) — strict, online-only, every operation server-authorised.
- (decision) Plasma requires connectivity: a plasma charge cannot be spent while the account has unsettled offline spend. Standard charges may be spent offline and settled later.
- (decision) Offline invariant (user's own words): charges spent offline sit at ZERO locally until sync settles them. Local regen must not refill what the server has not yet debited — that gap is exactly the cheat window.
- (decision) Cheat detection: server recomputes, from its own clock and the stored balance, the maximum charges the account could have held across the window the attempts cover. Claims above that ceiling are rejected AND recorded with their reasoning.
- (decision) Tournament tie-break by elapsed time. User offered two variants (time-derived percent bonus vs pure tie-break) and settled on pure tie-break: equal percent -> faster run ranks higher.
- (constraint) Tournament tie-break today is ratingPercent -> averagePercent -> groupsPlayed -> uniqueOpponents -> userId (functions/tournament-ranking.js compareLeaderboardEntries). Elapsed time is not recorded per attempt anywhere — it has to be captured before it can be ranked on.
- (constraint) Firestore rules already reserve configs/{configId} as server-owned with 'allow read, write: if false' (firestore.rules:42). Clients cannot read it directly — the constants must reach the device through a callable, not a direct document read.
- (question) OPEN: does the life-point substrate survive? Today a heart is a SLOT worth 100 points and a lesson attempt costs 33 (LESSON_ATTEMPT_LIFE_COST, functions/index.js submitLessonResultEvents). If charges become whole-unit hint spends, the fractional substrate has no remaining job — but killing it also kills the attempt toll, which is a separate product decision the input never addresses.
- (question) OPEN: plasma balance. 3 plasma/day against a 20-question pool is, in the user's own words, 'жирно' — enough to top up to 100% and beat everyone. Levers: longer regen, a per-attempt cap on plasma skips, or excluding plasma from the score numerator entirely.
- (question) OPEN: cross-difficulty use. Standard is defined for EASY and plasma for HARD. Unstated: may plasma also be spent on an EASY question (superset), and is a standard charge simply inert inside a tournament?
- (assumption) Assumed the existing escalating nolics ladder [1000,2000,5000,10000,20000] carries over as the standard-charge price ladder, extended to whatever MAX the config names — the input said prices are tunable but only respecified the plasma ladder.
- (assumption) Assumed the cheater record is a server-only Firestore collection under the existing admin/ or a new charge_audit/ namespace, unreadable by clients — the input said 'помечаю в папочку и обоснование' without naming a location.
- (assumption) Assumed the monthly reconciliation sweep for motivational currency is an onSchedule function like the existing reconcileQuestReviewDaily / aggregateQuestRatingsDaily, not new infrastructure.
- (note) Preservation catch: user's aside about tournament cadence ('раз в неделю турнир... если турнир раз в три дня, то как раз восстанавливаются') is load-bearing tuning input for the plasma period, not filler. Folded into open question 2.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6 + 8): PASS. 11 capabilities, each with intent and success. 7 constraints, all decision-bending. 6 non-goals. Success signal is demonstrable. IDs CAP-1..CAP-11 unique, first issue.
- (event) Self-validate pass 2 (preservation): PASS. All 28 load-bearing source claims land in SPEC.md or a companion. Rejected-alternative claims preserved as such: 'server script hands back the answer' -> Non-goals; 'time-derived percent bonus' -> memlog + CAP-10 framing. Wrapper-only drops: conversational hedges and self-corrections in the voice transcript.
- (event) Spec finalized: SPEC.md + economy-constants.md + settlement-protocol.md + brownfield.md. 5 open questions, 4 assumptions. Open question 1 (life-point substrate) flagged as blocking the data model.
- (note) Artifact language: English, matching core.document_output_language and both sibling specs (spec-result-screen, spec-theme-exams). Conversation with the user stays Russian.
- (direction) User clarification round 1. Confirms the spec described the work correctly; asks for open question 1 to be restated in plain words; supplies four decisions.
- (decision) SUPERSEDES the 'charges are not an attempt toll' framing: the attempt toll SURVIVES and is paid ONLY by STANDARD charges. Plasma is NEVER spent on entering or attempting a lesson.
- (decision) Sinks are now explicit. STANDARD charge has TWO sinks: the lesson-attempt toll, and a hint on an EASY question. PLASMA has exactly ONE sink: skipping a HARD question (tournaments included). Nothing else spends either.
- (decision) SUPERSEDES plasma regen 8h: plasma regenerates in 24h (one per day), ceiling 3. Directly answers the user's own 'жирно' doubt — 1/day instead of 3/day. Standard stays 1h, ceiling 10.
- (decision) SUPERSEDES the two-class model: there are THREE classes. MOTIVATIONAL (forgeable, periodic reconciliation), INTERMEDIATE (obtainable offline, strictly anti-cheat-checked at sync, NOT consumable offline), MONETARY (online-only, per-operation authorisation).
- (decision) Gift boxes are the named INTERMEDIATE resource: can be obtained offline, cannot be OPENED offline, and the anti-cheat sweep checks them hard at sync. Partially answers old open question 5.
- (question) OPEN (narrowed from old question 1): the toll survives, but is an attempt worth ONE whole standard charge, or a THIRD of one as today (100 points per charge, 33 per attempt)? Whole units make the hint and the toll commensurable; the fractional substrate keeps today's arithmetic and the existing regen code.
- (event) Launched verification workflow charges-clarification-verify (run wf_87b1c784-a79): four parallel code readers (boxes lifecycle, attempt toll arithmetic, run duration for the tie-break, plasma regen prerequisites) plus three adversarial critiques (exploit, coherence, honest player).
- (direction) User clarification round 2, answering the four open questions.
- (decision) RESOLVES open question 1. The fractional substrate SURVIVES and is load-bearing: a charge is a tank of 100 points, points are the accounting unit, the charge is the display unit. There is no single toll — there is a PRICE LIST PER ACTIVITY KIND.
- (decision) Price list, in points: ordinary lesson 33 (0.33 charge), arena 50 (0.5), theme test 100 (1), final exam 300 (3), tournament 500 (5). Ceiling 10 charges = 1000 points, so a tournament costs half a full tank. The list is a server constant.
- (note) Price list is confirmed by the legacy code the model descends from: CoastValuesLife in legacy/shop/src/main/java/com/tpov/shop/CoastValues.kt — COAST_LIFE_HOME_QUIZ=33, COAST_LIFE_ARENA_QUIZ=33. The user's numbers extend that same per-activity scheme; arena is raised 33->50.
- (decision) RESOLVES open question 2: NO per-attempt cap on plasma. All three may be spent inside one run. The 24h regen is the only limiter the owner wants.
- (decision) RESOLVES open question 3: tournaments become ONLINE, server-held sessions, so elapsed time is trustworthy for the tie-break. Sibling spec _bmad-output/specs/spec-theme-exams/ already designs server-held sessions for exams — reuse is the first thing to check.
- (direction) WITHDRAWS open question 4. The user pushed back: the motivational/intermediate/monetary split was his framing to explain charges to me, not a thing to build. It stays as rationale for why plasma and gold are guarded strictly; it stops being a capability with success criteria. Trophies recorded as paid.
- (decision) CAP-11 is rewritten accordingly: the deliverable is the periodic reconciliation sweep the user actually described ('раз в месяц просчитывать, всё ли сходится'), not a taxonomy. The three-tier naming moves to rationale.
- (event) Launched verification workflow charges-verify-v2 (run wf_76093e2f-24d) against the corrected model: four readers (price list, online tournament, boxes, plasma regen) plus three critiques (exploit, coherence, honest player). The earlier run wf_87b1c784-a79 died with the process before reporting.
- (event) SPEC re-derived for round 2: CAP-1 generalised from lesson to activity, CAP-11 rewritten as the reconciliation sweep, CAP-13 (charge-as-tank + price list) and CAP-14 (server-held tournament session) added. Four new open questions, all consequences of the answers rather than leftovers.
- (event) Both background verification workflows (wf_87b1c784-a79, wf_76093e2f-24d) died with their process before reporting. Verification done directly instead; findings below are first-hand reads, each cited.
- (constraint) BLOCKER for the price list: sourceShelf — the only field that comes close to naming the activity — is computed ON THE CLIENT (LessonResultOutboxWriter.kt:109-120, from quest.visibleOn) and taken from client data by the server (functions/index.js:3270, stringValue(data.sourceShelf, ...)). Under per-activity pricing the client would therefore choose its own price: declare home (33) instead of tournament (500).
- (decision) The server must re-derive the activity kind itself rather than bill from the declaration. It already receives questId on the event and quest documents carry visibleOn, so the lookup is available.
- (note) Only THREE of the five priced activities are reachable today. Ordinary lesson (home shelf) and tournament (tournament/tournamentFinal shelf) exist; arena exists but is a SHELF, not a game mode — PUBLIC_QUEST_SHELVES in functions/index.js:117 is [home, arena, tournament, tournamentFinal], and an arena quest plays through the same runner.
- (note) Theme test (контрольная) has NO code at all. Final exam is reserved but unbuilt: SessionMode.EXAM exists (SessionMode.kt) but its only production use is a timer coefficient at RunnerLogic.kt:125; nothing constructs an EXAM session and QuizzesConfig.kt:66 defaults to LEARNING.
- (constraint) The price key is therefore a PAIR — (shelf, sessionMode) — not a single field. No enum of activity kinds exists to key the price list on; one has to be introduced, and the server must own its derivation.
- (note) Boxes today do NOT accrue offline — the opposite. advanceGiftBoxState (functions/index.js:3208) runs only inside the openGiftBox callable, and advances the streak by at most ONE day per call regardless of elapsed time. Five days away then one open counts as one day, not five. Nothing on the device tracks boxes at all.
- (note) Box contents include gold: generateGiftBoxReward (functions/index.js:3087) can return addGold, and giftBoxRewardUpdate (3174) increments the gold field. Premium, logos and trophies too. So 'never opened offline' is already true and well justified — the open is a server callable and grants monetary value.
- (constraint) regenerateLifePoints (functions/life-points.js) is nearly generic but derives its step from the module-level LIFE_POINT_INTERVAL_MS rather than taking it as a parameter. Reusing it for plasma's 24h period needs that one signature change.
- (constraint) readEconomyBalance (functions/index.js:3823-3824) clamps stored balances with Math.min against the ceilings, confirming that lowering a ceiling today confiscates the excess. economy-constants.md already requires this to stop.
- (note) Sync insertion point for the constants pull is clean and established: a Syncable implementation registered in apps/android-next/.../di/SyncModule.kt, following ProfileBootstrapSync. No new infrastructure shape is needed, only a new participant.
- (event) Verification folded in: new Constraint (server derives the activity kind), brownfield gained a 'Verified for the price list' section with six findings, open questions re-cut to five and ordered by what decides sequencing.
- (direction) User clarification round 3, answering the three sequencing questions.
- (decision) RESOLVES open question 2: yes, introduce a server-derived activity-kind field. The server never bills from the client's declaration.
- (decision) RESOLVES open questions 3 and 4: the toll is taken AT THE START, but it must come back if the run does not finish or the sync fails. The user's rule, in his words: the server must not zero out what was not actually spent in the end.
- (decision) Mechanism follows the existing lazy-regeneration idiom rather than a background job. Two shapes, split by whether the activity is server-held. Offline-capable activities (lesson, arena): the client reserves locally so it cannot start what it cannot afford and so the shown balance is honest; the SERVER charges only at submission, so an abandoned run leaks nothing server-side. Server-held activities (tournament, later exam): the server reserves when the session opens and releases the reservation when the session expires unsubmitted, released lazily on the next read exactly as regenerateLifePoints does.
- (note) Abandoning a run to play free is not a real exploit and needs no counter-measure: an abandoned run yields no result, no reward and no progress, and neither an easy hint nor a plasma skip reveals anything that survives the run — LEARNING mode already reveals easy answers anyway (SessionMode.revealsCorrectAnswer), and a plasma skip reveals nothing by design (CAP-2).
- (decision) RESOLVES open question 1: do not wait on spec-theme-exams. Ship pricing for the three playable activities; the theme-test and exam rows stay dormant in the config. The user asks that markers be left so the agent that later builds those modes notices the waiting rows — pointers in both directions between this spec and spec-theme-exams, plus a marker at the code site that resolves an activity kind.
- (decision) Story breakdown, 12 slices in execution order: 1 rename, 2 server-owned constants, 3 plasma as a regenerating resource, 4 server-derived activity kind, 5 price list drives the toll, 6 reserve-at-start with release, 7 settle claims at submission, 8 offline balance semantics plus plasma gate, 9 overspend detection and audit, 10 boxes, 11 tournament sessions with time tie-break, 12 reconciliation sweep.
- (decision) Checkpoints PROPOSED, not confirmed by the user yet: spec_checkpoint on 2, 4, 6, 11 (the four with real design latitude); done_checkpoint on 2, 9, 11 (the foundation everything reads, the anti-cheat, and the largest slice). Awaiting the owner's adjustment.
- (event) stories.yaml written and checked against assets/stories-schema.md: 12 entries, ids unique, all strings, prefix-free, no status field, every required field present. Schema valid.
- (event) SPEC re-derived for round 3: CAP-15 (toll reserved at start, released if the run never lands) and CAP-16 (server names the activity) added; settlement-protocol.md gained 'The toll: taken at the start, returned if nothing lands'; open questions cut to three.
- (note) BMAD process failure on my part, recorded so it is not repeated: spec-charges was created and revised three times without reading the sibling specs in _bmad-output/specs/. There are six, not two. spec-monetisation (2026-08-31) and spec-sync both overlap this work.
- (note) spec-monetisation FORWARD-REFERENCES spec-charges as authoritative in five places: gold-flows.md:6 and SPEC.md:139 cite spec-charges/economy-constants.md for the monetary classification; gold-flows.md:30 and brownfield.md:68 cite spec-charges CAP-3 for plasma settlement; brownfield.md:17 assigns STANDARD_HEART_SLOT to spec-charges. Capability ids here are therefore externally referenced and must never be renumbered.
- (note) Consequence for the withdrawn taxonomy: the user called the motivational/intermediate/monetary split 'just my framing', but spec-monetisation now cites economy-constants.md as the normative source for gold being monetary class. The table has to stay as a reference even though it is not itself a build item — which is what it was already reduced to.
- (question) CONFLICT, owner's decision: plasma price. spec-monetisation states 10 gold in three places (gold-flows.md:30, brownfield.md:16, box-economy.md:31) and BUILDS ITS PRICING-POWER ARGUMENT ON IT. This spec says 1 gold, ceiling raised 1->3, and plasma now regenerates free every 24h. Combined that is not a price cut but the near-removal of one of only four gold sinks.
- (note) The arithmetic behind the conflict, from box-economy.md: a box yields ~0.019 gold and the honest ceiling is two boxes a day, so a free player earns ~1 gold per 26 days. At 10 gold and ceiling 1, a plasma charge is 260 days of free play — a real reason to buy gold. At 1 gold it is 26 days, and because plasma now refills itself every 24h the player never needs to buy a second one at all.
- (constraint) Charge regeneration must accept a premium multiplier. spec-monetisation CAP-4 and paid-catalogue.md:59 list 'faster charge regeneration' as a premium benefit, and this spec had no premium concept at all. The multiplier belongs in the constants document beside the periods.
- (decision) Box economy ownership goes to spec-monetisation. CAP-12 here keeps only the offline/online MECHANIC — accrues offline, opens online, verified at sync — and cites box-economy.md for every rate and ceiling. The two-boxes-a-day ceiling is monetisation's constraint, not this spec's to restate or contradict.
- (decision) spec-sync CAP-2 (any state-changing action performed offline reaches the server) is the substrate CAP-5 and CAP-6 build on. Declared as an adopted companion rather than restated.
- (event) Cross-spec reconciliation folded in: three adopted companions added to frontmatter (spec-monetisation box-economy.md and gold-flows.md, spec-sync SPEC.md), three constraints added (ids externally referenced, regeneration takes a premium multiplier, box economy belongs to monetisation), CAP-12 narrowed to the mechanic, open questions re-cut with the plasma-price conflict as blocking.
- (note) Coherence pass caught a self-contradiction introduced in round 2 and carried through round 3: the constraint 'A charge is a whole unit ... never partially' directly contradicts CAP-13, where an ordinary lesson costs 33 points — a third of a charge. Rewritten to say what is actually true: a hint or skip is indivisible, a toll is quoted in points and may be fractional. This is why the re-validate exists.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6 + 8) for rounds 2-3: PASS after one fix. 16 capabilities, every one carrying both intent and success, ids unique and now externally pinned by spec-monetisation. 11 constraints, 6 non-goals, all required sections present. The one failure found — the whole-unit constraint against CAP-13 — is fixed.
- (event) Self-validate pass 2 (preservation) for rounds 2-3: PASS. Round-2 claims all land: price list per activity (CAP-13, economy-constants.md), no plasma cap (constants table), tournaments online (CAP-14), taxonomy demoted to rationale (CAP-11 rewritten, economy-constants.md note), trophies paid (classification table). Round-3 claims all land: server-derived activity kind (CAP-16), toll at start with return (CAP-15, settlement-protocol.md), dormant-row markers (open question 2, story 5 dispatch note). Wrapper-only drops: none this round.
- (event) Cross-spec check, which the first two rounds skipped entirely: six sibling specs read. One blocking conflict (plasma price 10 vs 1 gold), two ownership overlaps resolved by ceding (box economy to spec-monetisation, offline delivery to spec-sync), one missing interaction found (premium multiplies charge regeneration). All folded in.
- (event) stories.yaml amended: story 3 now carries the contested-price warning in its dispatch note so no implementer quietly ships one gold.
- (note) An unfinished bmad-architecture run exists for the SYNC feature at planning-artifacts/architecture/architecture-schoolquiz3.0-2026-08-31/. It holds a full .memlog.md of decisions and NO rendered artifacts — the run died before producing the spine, the visual page and the work breakdown the user had asked for. Recoverable: bmad-architecture re-derives from the memlog.
- (constraint) Inherited from that run, and it binds this spec: mutations whose price or reward is decided by the server at call time — buying a charge, a nickname, a listing, a logo — stay SYNCHRONOUS and online-only. No optimistic local debit, because the client cannot predict the price. CAP-9 must say so; today it does not.
- (note) Two more decisions from that run touch settlement: the server gains a mutationId idempotency key for queued mutations (today dedup exists only for attempts and ratings via doc-id), and a sent queue row is DELETED locally rather than tombstoned. CAP-6's unsettled-claim bookkeeping has to sit on top of whichever form wins.
- (event) CAP-9 amended: purchases are synchronous and online-only, never queued, per the sync architecture run's decision. The three-gold figure removed from the success criterion since the price itself is contested.
- (direction) User correction: the plasma price is a LADDER, not a flat per-charge price — first slot 1 gold, second 2, third 3. This was in the original brain dump ('один заряд, пусть будет одно золото, два заряда, два золота, три заряда стоят три золота') and I read it as linear. It is the same shape as the standard-charge ladder.
- (decision) Plasma is bought as a SLOT, like the standard charge, not as a consumable: the purchase raises the ceiling and the slot then refills on its own every 24h. That is what makes the ladder coherent and matches the owner's framing of a maximum number of charges one can buy.
- (decision) Plasma price ladder [1, 2, 3] gold indexed by slots already owned, mirroring standardHeartSlotCost. Filling all three costs 1+2+3 = 6 gold, once, for permanent regenerating capacity.
- (decision) RESOLVES the blocking conflict with spec-monetisation, though it does not vanish. The plasma gold sink goes from 10 gold to 6, not to 1 as the linear reading implied — a 40% reduction rather than a 90% one. Against box-economy.md's ~1 gold per 26 days of free play, six gold is 156 days versus 260. spec-monetisation still has to be updated: three places say 10 flat, and the truth is a 1/2/3 ladder.
- (direction) User clarification round 4, resolving the blocking plasma-price conflict.
- (decision) CORRECTS my earlier misreading. Plasma is priced on a LADDER indexed by current holdings, not flat: the 1st charge costs 1 gold, the 2nd 2, the 3rd 3. Filling an empty stock of 3 costs 6 gold. Same shape as the existing standardHeartSlotCost(currentHearts) ladder, so the mechanism already exists in code.
- (note) Reconciliation with spec-monetisation, which priced plasma at 10 gold. The comparison is not 6 against 10, because the two models differ in kind. The old plasma was a ONE-TIME purchase with a ceiling of 1: lifetime sink per player, 10 gold, ever. The new one regenerates every 24h, so a patient player pays nothing and an impatient one pays 1-3 gold per top-up, repeatedly and indefinitely. A player topping up weekly at an average of 2 gold drains 8 gold a month against 10 gold once.
- (note) So the sink is shallower per event but recurring, which for a live economy is the better shape — it turns a one-off purchase into a subscription-shaped drain. The risk flips accordingly: the sink now rests on impatience rather than scarcity, and collapses if 24h regeneration is generous enough that nobody ever feels pressure. That is the number to watch, and it is a server constant, so it is tunable without a release.
- (note) CONCURRENCY: this repository has four other interactive sessions live, and at least one of them is editing spec-charges and spec-monetisation directly — economy-constants.md gained the plasma priceLadder [1,2,3] and CAP-9 was rewritten to slots-that-refill by a session other than this one, committed in 31342fa0 alongside this session's work. Whole-file writes from two sessions are last-writer-wins, so this session stopped writing to the spec rather than race. The memlog is append-only and remains safe to write.
- (event) Истории 4 и 5 реализованы на сервере (functions/activity-kind.js): вид попытки выводится из полок квеста, прочитанных сервером, цена берётся из серверной таблицы; объявление клиента остаётся диагностикой. Контрольная/экзамен ждут своих сессий (spec-theme-exams) — маркер в activity-kind.js. Хвосты: sourceShelf клиента ещё маршрутизирует грязные оценки; lessonAttemptLifeCost в профиле мёртв на клиенте.
- (event) Чистые ядра 7/8/9 реализованы: ChargeClaimMask (client) + charge-claims.js (server) с общим набором фикстур; OfflineChargeLedger (available = слово сервера − неучтённое, плазма ждёт учёта); overspendVerdict/overspendRecord. Проводка в index.js и правило audit/ ждут — файлы заняты другой сессией. Решение: 'P' на ненулевой цифре — испорченная маска (SKIP_ON_ANSWERED), а не неоплата: иначе клиентская 9 прошла бы бесплатно.
- (decision) Учёт заявок на клиенте: заявки идущей попытки — не долг (расчёт только в конце попытки), поэтому три пропуска за забег проходят; долгом становятся по runEnded(), и любой долг любого вида запирает плазму (CAP-8 — про аккаунт). Потолок восстановления и перерасхода — купленные слоты, не выше maxOwned; перерасход считается в очках, а не в порознь округлённых зарядах.
- (decision) Открытый вопрос 4 (sweep, история 12) — ответ: пересчитать нолики из истории нельзя; сервер пишет только инкременты, награда коробки случайна и не записывается, покупки не журналируются. История 12 начинается с append-only журнала начислений, sweep — после него.
- (decision) Коробки (история 10): серия рвётся пропущенным днём («подряд»), за визит после отсутствия — один день, не N; при синхронизации заявка устройства ограничивается прошедшими сутками по серверным часам (серия ≤ было+дни, коробок ≤ дни серии от 10-го и ≤ 1/день), излишек не начисляется и записывается; без слова сервера принимается один день.
- (decision) Вид попытки выводится из урока, не из объявленного questId: сервер поднимается lessons→themes→sections→quests; опубликованный урок побеждает объявленную область, приватный подтверждается только по своему пути, иначе вид неизвестен и цена — по самой дорогой ставке. Покупка слотов и восстановление на сервере читают таблицу.
- (decision) Ручка премиального восстановления (premiumRegenDivisor) стоит в таблице у обоих видов заряда и по умолчанию равна единице: спека запрещает периоду быть голой константой, а решает монетизация. Период считается regenMsFor(rules, hasPremium) на обеих сторонах. Профиль перестал отдавать lessonAttemptLifeCost, UserProfile.canAffordLessonAttempt заменён на canAfford(pricePoints): плоской цены попытки больше нет.
- (decision) Плазма хранится как обычный заряд: goldHearts — купленные слоты (вместимость), plasmaPoints + plasmaPointsUpdatedAtMs — заряд в них. Раньше одно число значило и то и другое, и суточное восстановление было только на бумаге. Купленный слот приходит заряженным: плазма стоит золота, и отдать за деньги пустой слот на сутки значит продать ожидание.
- (decision) Клиент повторяет правило вида активности (ActivityKindRule, зеркало activity-kind.js, общий набор фикстур): нужно, чтобы назвать цену ДО запуска. Списывает по-прежнему сервер по своим документам — зеркало только показывает. Проводка 7/8/9 заблокирована колонкой под маску в lesson_attempts: persistence занята чужой миграцией 8.
- (decision) Заявка на заряд не требует колонки в lesson_attempts: она едет в payload строки очереди, а строка пишется в одной транзакции с попыткой (AD-23), значит переживает перезапуск ровно так же. Локально маску никто не читает — платит по ней сервер. Вид заявки определяет сложность попытки, а не вызывающий; на сложной попытке заявка пока не ставится, потому что сегодняшнее управление показывает ответ, а плазменная заявка означает «пропущено и засчитано верным».
- (decision) scoreVerified остаётся сверкой присланного процента с присланными цифрами, а не с послерасчётной строкой: иначе честный игрок, которому не хватило заряда, терял бы награду за всю попытку, тогда как излишек и так не оплачен (спека запрещает наказывать дважды). Подделку 'я поставил себе 9 под пропуском' ловит SKIP_ON_ANSWERED при разборе маски.
- (decision) Записи о перерасходе закрыты правилами и на чтение — в отличие от admin/, куда рецензент заглядывает по квалификации. Показать подделывающему, что именно его выдало, значит объяснить, как подделывать дальше; честному игроку это обвинение, которого он не запрашивал. Читает оператор из консоли.
- (decision) Время в турнирной таблице стоит НИЖЕ числа групп и числа соперников, а не сразу под процентом: выше них оно подняло бы быстрого одиночку над тем, про кого известно больше. Так оно решает ровно тот случай, ради которого заведено, — два одинаковых прогона по одному уроку. Прогон без замера считается неизмеренным (бесконечность), а не мгновенным.
- (question) Серверная сессия турнира (CAP-14) отложена: exam-protocol.md соседней спеки описывает ту же машинерию, но в коде её нет. Строить её ради турниров — значит построить общую машинерию сессий и потом переиспользовать для экзаменов; правильный порядок обратный. Кто берёт spec-theme-exams, строит её один раз на обоих.
- (decision) Сверки списка показанного с цифрами мало: codeAnswer из одной девятки со списком на один вопрос согласован сам с собой, а награда считается по уроку целиком. Поэтому сервер сверяет ЧИСЛО показанных с набором урока (lessonPoolSize — тот же отбор, что у отведённого времени). Попытка без списка показанного сохраняется, но не оплачивается: живых установок нет (AD-16), нынешний клиент список шлёт всегда.
- (decision) Открытый вопрос 4 закрыт: истории для сверки не было, поэтому она заведена. functions/currency-ledger.js — строка на каждое движение валюты, которое сделал сервер: кому, когда, что, на сколько, почему и по какому действию. Идентификатор строки выводится из владельца, валюты, повода и действия, поэтому переигранная транзакция даёт одну строку, а не две. Пишется в той же транзакции, что и само движение. Находка сверки несёт coversFromMs: разница до первой записи объясняется тем, что книги не было, а не игроком.
