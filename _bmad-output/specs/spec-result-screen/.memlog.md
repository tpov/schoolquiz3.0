---
topic: Result screen: discussion row, recommended-first block, pinned actions
updated: 2026-08-29T13:50
---

- (direction) Owner: the drawing is the source of truth; the prose brief has contradicted it and the drawing wins.
- (constraint) Direction 4A 'contrast only' was chosen: blocks separate by fill step, never by a 1px stroke — at this palette a white-alpha edge reads grey.
- (capability) CAP-1 Discussion is one tappable row at the foot: title, comment count with how many are new, action word. Opens the existing discussion.
- (capability) CAP-2 A recommendation sits under it — kicker RECOMMENDED FIRST in accent, the lesson that closes the gap, chevron. Already computed by resultAdvice().
- (capability) CAP-3 Two actions pinned to the bottom, not scrolled with content: AGAIN on the left, NEXT LESSON on the right.
- (constraint) The screen must not scroll: everything above the pinned actions fits 812dp.
- (assumption) Comment count comes from component.comments, already a StateFlow on LessonRunnerRootComponent.
- (question) NEXT LESSON has no destination in code — the next lesson in the theme is not resolved anywhere. Needs a decision.
- (question) '3 new' requires a last-read marker per lesson. Nothing stores one.
- (note) Existing: score card, best-mark scale, reward strip, gold-band top 3, advice block. Missing: the three capabilities above.
- (decision) Q1 answered: NEXT LESSON goes to the next lesson by order within the current theme. When none is left, the action becomes EXAM instead of disappearing.
- (note) Exams exist as a ladder: after every lesson of a theme, after every theme, after every section; the final one issues a certificate.
- (constraint) SessionMode.EXAM already exists and changes behaviour — nothing is revealed, the timer differs — but no screen or navigation ever creates an EXAM session. The exam is unreachable today.
- (decision) Q2 answered: no last-read marker. The row shows the total number of comments and nothing about what is new. The discussion is a modal list, not a chat.
- (question) Launching an exam is a new capability, not wiring. Scope it separately: which lessons it draws from, how it is scored, when the certificate is issued.
- (note) Correction to my own earlier entry: onNextLesson already resolves the next lesson by order within the theme and navigates to it. Only the no-next case is wrong — it silently goes back instead of offering the exam.
- (decision) Scope now: comments row with the total, pinned actions, and the action labelled for what it does. The exam action waits on the exam feature.
