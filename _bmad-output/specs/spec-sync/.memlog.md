---
topic: Синхронизация приложения: клиент и сервер — приведение sync-контракта в соответствие с реальностью и закрытие дыр offline-first
updated: 2026-09-01T10:56
---

- (direction) Пользователь: сначала зафиксировать в документах все расхождения доков/легаси/кода, потом писать код клиента и сервера. BMAD-вход — bmad-spec.
- (note) Вход спеки — _bmad-output/specs/spec-sync/brownfield.md: аудит на срезе 2026-08-30 (HEAD 342c7c2c), 9 расхождений ADR↔код, 3 неточности ADR↔легаси, 7 дефектов, 7 вопросов.
- (decision) Язык SPEC — русский, вопреки core.document_output_language=English: companion brownfield.md и корректируемый ADR-0004 на русском, смешивать языки внутри одной папки хуже.
- (decision) brownfield.md остаётся companion, а не source: он несёт доказательную базу с file:line, которую downstream должен читать.
- (decision) ADR-0004 и docs/invariants.md — adopted companions; ADR-0004 читается через brownfield.md, пока CAP-1 не закрыт.
- (capability) CAP-1: документ соответствует коду — каждое расхождение либо описано как есть, либо явно помечено нереализованным планом.
- (capability) CAP-2: очередь исходящих мутаций — любое действие, меняющее серверное состояние, переживает отсутствие сети.
- (capability) CAP-3: backoff и карантин — устойчиво отвергаемая запись не блокирует остальную очередь.
- (capability) CAP-4: сброс и починка курсоров — полный ресинк восстанавливает базу, даже если курсор ушёл вперёд.
- (capability) CAP-5: очередь привязана к аккаунту — запись uid A не уходит под uid B.
- (capability) CAP-6: состояние sync видно игроку — время последнего успеха и число неотправленных.
- (capability) CAP-7: версию назначает сервер, конфликт двух устройств обнаружим, а не молчалив.
- (capability) CAP-8: сиды и прод пишут sync_changes по одной схеме id.
- (capability) CAP-9: каденции ведут себя как обещано — профильная запускает профильный воркер.
- (note) capabilities logged
- (constraint) Клиент не пишет серверно-защищённые поля (квалификации, сертификаты, полки) — правило ADR-0004 сохраняется.
- (constraint) Каждая операция очереди идемпотентна: повтор доставки не удваивает эффект. Прецедент — дедуп по attemptId в submitLessonResultEvents.
- (constraint) Провал sync = повтор, а не откат локальной работы игрока (ADR-0004, No hard rollback).
- (constraint) Контракты живут в shared/core/sync commonMain без Android/SDK типов; платформенное — в platform/*. Правила clean-architecture.md и domain-models.md.
- (constraint) Сервер — functions/index.js; server/workers/* пусты и не считаются местом реализации без отдельного решения.
- (constraint) Инвариант 8 docs/invariants.md распространяется на состояние sync: user-specific Flow re-subscribe на смену аккаунта.
- (constraint) Схема Room меняется миграцией, не пересозданием базы — прецедент a6df38ce.
- (decision) Non-goals: смена бэкенда, Protobuf вместо JSON, переписывание legacy/, проектирование server-authoritative сессии экзамена, iOS-реализация.
- (assumption) Решение ADR-0004 «version инкрементирует сервер» остаётся в силе; сегодня version пишет клиент (functions/index.js:2290), это дефект, а не альтернативное решение.
- (assumption) Бэкенд остаётся Firestore + Cloud Functions.
- (assumption) Продовый logout появится, поэтому B-3 проектируется заранее, а не после появления пути выхода.
- (question) Журнал sync_changes или каскад contentsVersion — что канонично? Проигравший уходит из ADR и с сервера.
- (question) Одна обобщённая очередь с payload:JsonElement и серверной валидацией — или типизированные очереди на класс действия?
- (question) Нужен ли FCM сейчас? Без него ADR-0006 и ADR-0007 не выполнимы в заявленной задержке.
- (question) Что игрок видит про sync и где — шторка, настройки, экран результата?
- (question) Смена аккаунта: какие курсоры сбрасывать, что делать с непустой очередью прежнего аккаунта.
- (question) Порог карантина: сколько отказов до карантина и что игрок про карантинную запись видит.
- (question) Точка стыка с server-authoritative сессией экзамена (E2/E4): что перестаёт синхронизироваться, когда ответы hard уходят с устройства.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6,8): PASS. 9 capabilities, все с intent+success; 8 constraints, каждый что-то исключает; 6 non-goals; success signal — воспроизводимый сценарий. Замечание: CAP-7 intent слегка механистичен (кто назначает version), оставлен так, потому что решение унаследовано от принятого ADR-0004.
- (event) Self-validate pass 2 (preservation): PASS после трёх правок. Найдено и исправлено: §4 аудита (неточности ADR о легаси) не покрывалось CAP-1 — добавлено в success; B-5 (выборка изменений без limit) не покрывалось ничем — добавлен constraint; §6 (легаси-возможности без переноса) могло читаться как требование — добавлено в non-goals. Wrapper-only content: нет, аудит целиком содержательный.
- (event) SPEC.md отрендерен. Companions: brownfield.md (spec-authored), ADR-0004 и docs/invariants.md (adopted).
- (decision) OQ-1 закрыт: канонический механизм обнаружения изменений — журнал sync_changes. Каскад contentsVersion уходит из ADR, поле перестаёт писаться сервером и удаляется из клиентских моделей. Причина: журнал работает и проверен, каскад никогда не был реализован и оплачивался вхолостую.
- (decision) OQ-2 закрыт: общая очередь для новых действий (покупки, ник, разблокировка, ревью); три существующие типизированные очереди остаются как есть; сверху общий движок повторов и карантина. Работающее не переписываем.
- (decision) OQ-3 закрыт пользователем: FCM в объёме, но узко — только критические уведомления (сертификат, оффер квалификации, продвижение квеста), не как канал передачи данных.
- (decision) Порядок работ: сначала ADR-0004 приводится в соответствие с кодом, код после. Подтверждено пользователем.
- (capability) CAP-10: критическое событие доходит до игрока при закрытом приложении через push.
- (constraint) Push несёт повод сходить за данными, а не сами данные: полезная нагрузка уведомления не является источником истины.
- (decision) CAP-8 расширен: один механизм обнаружения изменений везде — журнал по единой схеме id в проде и сидах, contentsVersion удалён с обеих сторон.
- (question) Какие именно события шлют push и что игрок видит в уведомлении.
- (event) SPEC перерисован после решений пользователя: CAP-10 (push для критических событий) добавлен, CAP-8 расширен до удаления contentsVersion, 4 новых constraint, OQ-1/2/3 закрыты, добавлен OQ про состав push-событий. Осталось 5 открытых вопросов.
- (note) Исправлена фактическая ошибка аудита: каскад contentsVersion не «никогда не был реализован», а был построен в 5140ae3b (2026-04-25, CascadingSyncOrchestrator 205 строк + 464 строки тестов) и удалён в 1cb9f366 (2026-04-30). Решение о замене принято в коде и не дошло до ADR. Исправлено в brownfield.md D-3 и в ADR.
- (event) CAP-1 закрыт: docs/architecture/0004-sync-contract.md переписан. Каждое решение помечено [РАБОТАЕТ]/[ЧАСТИЧНО]/[РЕШЕНО]; добавлены разделы про журнал sync_changes, семантику удаления, расписание; Amendment 2026-04-21 отозван; три неточности про легаси исправлены; правила 1,3,4 переписаны.
- (event) docs/features/home-and-my-quests/README.md помечен superseded в части sync: заголовок фичи и статус implemented вводили в заблуждение сильнее всего после самого ADR. Остальные файлы фичи оставлены как исторический record.
- (decision) ПЕРЕСМОТР CAP-2 и Success signal: покупки и смена ника исключены из офлайн-сценария. Основание (спайн AD-1): их цена рождается на сервере в момент вызова — цена сердца зависит от текущего числа сердец, цена ника от реестра, лот может уйти другому покупателю. Отложенная покупка показала бы игроку цену, которой не будет. В офлайн-сценарии остаются: прохождение урока, оценка квеста, разблокировка урока, действие ревьюера, перенос квеста на полку.
- (assumption) ПОДТВЕРЖДЕНО ВЛАДЕЛЬЦЕМ: живых установок приложения нет, оно существует только локально. Это снимает требование обратной совместимости клиента.
- (decision) ПЕРЕСМОТР constraint про миграции: разрешено одно пересоздание локальной схемы, потому что живых установок нет. С этого момента каждая миграция обязательна и покрыта тестом runMigrationsAndValidate, fallbackToDestructiveMigration запрещён. Основание: история миграций сегодня недостоверна — схема 1.json совпадает с 2.json вплоть до identityHash, наращивать цепочку на неё бессмысленно.
- (decision) ПЕРЕСМОТР constraint «три существующие очереди не переписываются»: они приводятся к общей форме. Основание: заявка арены уходит прямой записью в Firestore, а не через callable, поэтому ключ идемпотентности mutationId на ней не работает в принципе. Общий движок поверх четырёх разных форм не даёт того, ради чего он заводится.
- (event) SPEC обновлён по итогам сверки со спайном: три противоречия сняты, ни одно не осталось молчаливым.
- (event) Спайн и разбивка работы приняты в companions SPEC-sync: architecture-schoolquiz3.0-2026-08-31/ARCHITECTURE-SPINE.md и epic-breakdown.md. AD-идентификаторы стабильны, downstream может на них ссылаться.
- (event) Созданы эпики и истории: _bmad-output/planning-artifacts/epics.md — 10 эпиков, 58 историй, FR1-FR29 покрыты полностью. Два прогона критика: первый нашёл 4 обратные межэпичные зависимости и 7 дублирований, второй — 10 расхождений от самих правок. Все закрыты.
- (decision) Карантин: локальное изменение откатывается всегда, для всех мутаций. Риск потери реально пройденного урока принят владельцем; смягчение — будущая система резервных копий, сегодня её нет.
- (event) Ворота готовности: CONCERNS (не PASS). Три пункта: нет UX-артефакта при трёх историях с игровой поверхностью; эпик 4 показывает счётчик конфликтов, разрешение которых приходит эпиком 8; снятие скалярного курсора из 7.3 не отнесено ни к одному эпику. Ни один не блокирует эпики 1-3. Трекер сгенерирован: _bmad-output/implementation-artifacts/sprint-status.yaml, 68 записей + 10 ретроспектив.
- (event) КОЛЛИЗИЯ ПУТЕЙ: 2026-09-01 параллельная работа по монетизации перезаписала _bmad-output/planning-artifacts/epics.md своим содержимым (путь по умолчанию у обеих). Синхронизация восстановлена под отдельными именами: epics-sync.md и sprint-status-sync.yaml. Спайн, спека и аудит не пострадали — у них свои пути.
- (event) bmad-build по истории 1.1: код уже написан параллельной работой — модули shared/core/network и shared/core/outbox существуют, тесты зелёные. Вместо повторной реализации трекер сверен с кодом: эпик 1 done целиком, эпик 2 in-progress (2.1, 2.2, 2.3 done). Расхождение со спайном: вместо одного пересоздания схемы по AD-16 параллельная работа пошла настоящими миграциями, версия базы 5, схемы 1-5.json. Это защитимо и лучше AD-16, но AD-16 и NFR11 надо пересмотреть.
- (event) История 3.1 закрыта: два срока вынесены в единственное объявление config/sync-params.json — сервер читает его require'ом, клиент держит копию в OutboxLimits под тестом-стражем SyncParamsContractTest, который падает при расхождении (проверено подменой числа). Числа владельца проставлены: возраст записи 7 дней, хранение ключа 30 дней, порог 5 отказов, backoff 1с..1час.
- (event) История 3.5 закрыта: единственный callable-приёмник submitMutation добавлен в functions/index.js — резерв ключа до эффекта, повтор отдаёт сохранённое, чужой ключ отвергается, ожидание чужой попытки помечается повторяемым precondition-pending. Реестр операций намеренно пуст: жизненный цикл отлаживается на нём, а не под первой денежной операцией. logo-images.js, logos.js, tournament-ranking.js добавлены в npm run lint — проверялось девять файлов из двенадцати.
