package com.tpov.schoolquiz.shared.feature.app_shell.domain.model

/**
 * Enum representing the four bottom-navigation tabs.
 * Domain rule: always one of these four values.
 */
enum class Tab {
    LOCAL,
    INTERNET,
    EVENTS,
    SHOP,
}
