<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Settings Theme -->
    <style name="SettingsTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/settings_accent</item>
        <item name="colorPrimaryDark">@color/settings_background</item>
        <item name="colorAccent">@color/settings_accent</item>
        <item name="android:windowBackground">@color/settings_background</item>
        <item name="android:statusBarColor">@color/settings_background</item>
        <item name="android:navigationBarColor">@color/settings_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Card Style for Settings -->
    <style name="SettingsCardStyle">
        <item name="cardBackgroundColor">@color/settings_card_background</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">0dp</item>
        <item name="contentPadding">0dp</item>
    </style>

    <!-- Save Button Style -->
    <style name="SettingsSaveButton" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/settings_button_background</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:paddingStart">32dp</item>
        <item name="android:paddingEnd">32dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Toolbar Style -->
    <style name="SettingsToolbarStyle" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:background">@color/settings_background</item>
        <item name="titleTextColor">@color/settings_text_primary</item>
        <item name="android:elevation">0dp</item>
    </style>
</resources> 