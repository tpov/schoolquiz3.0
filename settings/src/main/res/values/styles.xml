запуст<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Settings Theme -->
    <style name="SettingsTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/settings_accent</item>
        <item name="colorPrimaryDark">@color/settings_background</item>
        <item name="colorAccent">@color/settings_accent</item>
        <item name="android:windowBackground">@color/settings_background</item>
        <item name="android:statusBarColor">@color/settings_background</item>
        <item name="android:navigationBarColor">@color/settings_background</item>
        <item name="preferenceTheme">@style/SettingsPreferenceTheme</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Preference Theme -->
    <style name="SettingsPreferenceTheme" parent="@style/PreferenceThemeOverlay.v14.Material">
        <!-- Use default styles for now -->
    </style>

    <!-- Preference Category Style -->
    <style name="SettingsPreferenceCategory" parent="@style/Preference.Category">
        <item name="android:layout">@layout/preference_category_custom</item>
    </style>

    <!-- EditText Preference Style -->
    <style name="SettingsEditTextPreference" parent="@style/Preference.DialogPreference">
        <item name="android:layout">@layout/preference_custom_layout</item>
    </style>

    <!-- Switch Preference Style -->
    <style name="SettingsSwitchPreference" parent="@style/Preference.SwitchPreferenceCompat">
        <item name="android:layout">@layout/preference_custom_layout</item>
    </style>

    <!-- List Preference Style -->
    <style name="SettingsListPreference" parent="@style/Preference.DialogPreference">
        <item name="android:layout">@layout/preference_custom_layout</item>
    </style>

    <!-- Add missing standard styles that are referenced -->
    <style name="Preference.EditTextPreference" parent="@style/Preference.DialogPreference">
        <item name="android:dialogLayout">@layout/preference_dialog_edittext</item>
    </style>

    <style name="Preference.ListPreference" parent="@style/Preference.DialogPreference">
        <item name="android:positiveButtonText">@android:string/ok</item>
        <item name="android:negativeButtonText">@android:string/cancel</item>
    </style>

    <!-- Card Style for Settings -->
    <style name="SettingsCardStyle">
        <item name="cardBackgroundColor">@color/settings_card_background</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">0dp</item>
        <item name="contentPadding">0dp</item>
    </style>

    <!-- Save Button Style -->
    <style name="SettingsSaveButton" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/settings_button_background</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:paddingStart">32dp</item>
        <item name="android:paddingEnd">32dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Toolbar Style -->
    <style name="SettingsToolbarStyle" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:background">@color/settings_background</item>
        <item name="titleTextColor">@color/settings_text_primary</item>
        <item name="android:elevation">0dp</item>
    </style>
</resources> 